63.md — Worker Architecture and Execution Isolation Contract, Worker Lifecycle, Worker Pool, Job-to-Worker Ownership, Worker Capability Boundary, Cancellation, Timeout, Panic/Crash Containment, Worker Restart, FD Ownership, Resource Limits, CPU Scheduling, Memory Limits, Operation Isolation, Worker Result Delivery, Unknown Outcome, Crash Recovery, Backpressure, Fairness, Worker Health, Poisoned Worker Handling, Graceful Shutdown, dan Secure Worker Execution Model

Tujuan

Worker adalah execution container untuk satu unit Job lifecycle.

Worker bukan:

```
Scheduler
Policy Engine
Authorization Service
Safety Authority
Configuration Authority
```

Worker hanya menjalankan:

```
JobExecutionContext
```

yang telah diberikan oleh ApplicationController dan Scheduler.

Alur:

```
Scheduler
   │
   ▼
Job
   │
   ▼
WorkerPool
   │
   ├── Worker A
   ├── Worker B
   └── Worker C
          │
          ▼
     Job Pipeline
          │
          ├── Scanner
          ├── Safety
          ├── Policy
          ├── Planner
          ├── Authorization
          ├── Executor
          ├── Verifier
          └── Aggregator
                 │
                 ▼
             JobResult
```

1. Worker hanya boleh menerima Job yang telah:

   admitted

2. Worker tidak menentukan:

   apakah request boleh masuk queue

3. Worker tidak menentukan:

   apakah user memiliki capability

4. Worker tidak menentukan:

   apakah target protected

5. Worker tidak menentukan:

   policy

6. Worker tidak boleh menerima:

   arbitrary executable command

7. Worker tidak boleh menerima:

   shell command

8. Worker tidak boleh menerima:

   arbitrary filesystem path

9. Worker menerima:

   typed JobId
   AttemptId
   JobExecutionContext

10. JobExecutionContext berisi referensi typed terhadap:

    ConfigSnapshot
    CatalogSnapshot
    PlatformSnapshot
    ReadinessSnapshot
    JobBudget
    CancellationToken
    Deadline
    RequestOrigin

11. Context harus:

    immutable

12. Worker tidak boleh memodifikasi:

    ConfigSnapshot

13. Worker tidak boleh memodifikasi:

    CatalogSnapshot

14. Worker tidak boleh memodifikasi:

    AuthorizedPlan

15. Worker tidak boleh mengubah:

    policy rules

16. Worker Lifecycle

Lifecycle minimal:

```
Idle
  ↓
Assigned
  ↓
Preparing
  ↓
Running
  ↓
Completing
  ↓
Idle
```

17. Failure branch:

```
Running
   ↓
Failed
```

18. Crash branch:

```
Running
   X
   ↓
Lost
```

19. Shutdown branch:

```
Assigned
   ↓
Cancelling
   ↓
Stopped
```

20. Worker tidak boleh mengambil Job berikutnya sebelum:

```
ownership job sebelumnya selesai
```

21. Worker Ownership

Setiap execution memiliki:

```
WorkerId
```

22. Assignment memiliki:

```
WorkerId
JobId
AttemptId
```

23. Assignment harus:

```
unique
```

24. Worker tidak boleh menjalankan:

```
dua destructive Job sekaligus
```

25. Worker dapat menjalankan read-only operation paralel hanya jika:

```
resource policy mengizinkan
```

26. Default v1:

```
one execution context per worker
```

27. Worker Pool

Pool terdiri dari:

```
WorkerSupervisor
WorkerRegistry
Worker
```

28. WorkerSupervisor bertanggung jawab:

```
spawn
health
restart
shutdown
```

29. WorkerRegistry menyimpan:

```
WorkerId
state
current JobId
health
last heartbeat
```

30. Worker tidak boleh memiliki:

```
scheduler authority
```

31. WorkerSupervisor tidak boleh:

```
bypass authorization
```

32. Pool Size

Pool size harus:

```
bounded
```

33. Tidak boleh:

```
spawn per request tanpa limit
```

34. Pool size configurable tetapi:

```
maximum hard bounded
```

35. Example:

```
max_workers = N
```

36. Destructive concurrency tetap dapat lebih rendah:

```
max_active_cleanup = 1
```

37. Jadi:

```
worker count
   ≠
destructive concurrency
```

38. Read-only scanning dapat menggunakan:

```
multiple workers
```

39. Mutation execution dapat menggunakan:

```
dedicated constrained worker
```

40. Worker Classes

Arsitektur dapat membedakan:

```
ReadWorker
CleanupWorker
MaintenanceWorker
```

41. CleanupWorker memiliki:

```
mutation capability
```

42. ReadWorker tidak boleh memiliki:

```
mutation capability
```

43. MaintenanceWorker hanya dapat menjalankan:

```
typed maintenance operations
```

44. Capability Boundary

Capability worker harus:

```
minimum
```

45. Worker tidak menerima:

```
global root access
```

46. Worker menerima:

```
operation-scoped authority
```

47. Example conceptual:

```
Worker
   ↓
AuthorizedPlan
   ↓
Operation
   ↓
TrustedRoot
   ↓
RelativePath
```

48. Worker tidak boleh mengubah:

```
AuthorizedPlan.scope
```

49. Worker tidak boleh memperluas:

```
target list
```

50. Worker tidak boleh menambahkan:

```
new operation
```

51. Closed Execution Set

AuthorizedPlan menentukan:

```
exact execution set
```

52. Worker hanya boleh execute:

```
operations ∈ AuthorizedPlan
```

53. Jika worker menemukan object baru:

```
do not dynamically add it
```

54. New object membutuhkan:

```
new scan
new classification
new plan
new authorization
```

55. Cancellation

CancellationToken harus:

```
cooperative
```

56. Cancellation checkpoints:

```
before scan
during scan
before plan
before authorization
before execution
between operations
during verification
```

57. Cancellation tidak otomatis berarti:

```
mutation stopped safely
```

58. Jika cancellation terjadi:

```
sebelum mutation:

Cancelled
```

59. Jika terjadi:

```
selama mutation
```

outcome dapat:

```
Unknown
```

60. Unknown harus:

```
reconciled
```

61. Executor tetap bertanggung jawab atas:

```
atomic mutation boundary
```

62. Worker bertanggung jawab memastikan:

```
cancellation signal delivered
```

63. Cancellation latency harus:

```
bounded
```

64. Long directory scan harus memeriksa:

```
cancellation periodically
```

65. Long verification juga:

```
cancellation-aware
```

66. Timeout

Worker memiliki:

```
job deadline
```

67. Deadline menggunakan:

```
MonotonicClock
```

68. Tidak menggunakan:

```
wall clock
```

69. Timeout sebelum mutation:

```
Cancelled/TimedOut
```

70. Timeout saat mutation:

```
Unknown
```

71. Timeout saat verification:

```
VerificationIncomplete
atau
Unknown
```

sesuai operation state.

72. Timeout tidak boleh:

    automatically replay mutation

73. Hard Timeout

Jika cooperative cancellation tidak berhasil:

```
supervisor may terminate worker
```

74. Forced termination setelah mutation dimulai:

```
requires recovery
```

75. Worker termination tidak membuktikan:

```
mutation failure
```

76. Worker Crash

Jika worker crash:

```
current Job = Lost
```

77. Job state harus menjadi:

```
RecoveryRequired
```

78. Supervisor tidak boleh:

```
silently retry
```

79. Recovery flow:

```
Worker crash
   ↓
mark attempt lost
   ↓
persist recovery state
   ↓
restart worker
   ↓
reconciliation
   ↓
finalize JobResult
```

80. Worker Restart

Restart harus:

```
fresh WorkerId
```

81. Old WorkerId:

```
historical
```

82. Old worker cannot:

```
report completion after restart
```

83. Completion message must include:

```
WorkerId
JobId
AttemptId
```

84. Supervisor rejects:

```
stale worker message
```

85. Worker Identity

WorkerId must be:

```
unique
```

86. WorkerId dapat berupa:

```
monotonic ID
random opaque ID
```

87. Do not expose internal worker details unnecessarily.

88. Heartbeat

Long-running worker dapat mengirim:

```
heartbeat
```

89. Heartbeat includes:

```
WorkerId
JobId
AttemptId
monotonic timestamp
```

90. Heartbeat tidak menjadi:

```
execution proof
```

91. Missing heartbeat:

```
health warning
```

92. Timeout:

```
supervisor investigates
```

93. Worker may be:

```
Hung
```

94. Hung Worker

Supervisor may:

```
request cancellation
```

95. Jika tidak merespons:

```
force stop
```

96. Forced stop after mutation:

```
RecoveryRequired
```

97. Worker Health

States:

```
Healthy
Busy
Cancelling
Suspect
Hung
Poisoned
Stopping
Dead
```

98. Poisoned Worker

Worker menjadi:

```
Poisoned
```

jika repeatedly mengalami:

```
panic
invalid internal state
protocol violation
```

99. Poisoned worker tidak boleh:

```
menerima new job
```

100. Supervisor harus:

```
isolate
```

101. Panic Containment

Worker panic tidak boleh:

```
crash scheduler
```

102. Recommended boundary:

```
worker execution task
```

103. Panic menjadi:

```
WorkerPanic
```

104. Job menjadi:

```
Failed
atau
RecoveryRequired
```

tergantung mutation phase.

105. Panic Before Mutation

Jika panic sebelum mutation:

```
Failed
```

106. Panic During/After Mutation:

```
RecoveryRequired
```

107. Panic During Verification:

```
result may be Unknown
```

108. Rust-specific implementation

Jika menggunakan Rust:

```
catch_unwind
```

dapat digunakan di boundary yang sesuai.

109. Tetapi:

```
catch_unwind bukan security sandbox
```

110. Process Isolation

Jika threat model membutuhkan:

```
process boundary
```

worker dapat dijalankan sebagai:

```
child process
```

111. Process isolation memberikan:

```
crash containment
resource control
privilege separation
```

112. Thread isolation lebih ringan tetapi:

```
tidak membatasi memory/process privilege secara kuat
```

113. Architecture v1 dapat:

```
begin with bounded worker threads
```

114. Namun destructive worker harus memiliki:

```
explicit capability boundary
```

115. Jika privilege tinggi diperlukan:

```
separate process is preferred
```

116. Worker Process Model

Conceptual:

```
Main Daemon
   │
   ├── Scheduler
   ├── IPC
   ├── Runtime
   └── WorkerSupervisor
          │
          ├── Worker Process A
          ├── Worker Process B
          └── Worker Process C
```

117. Worker process communication menggunakan:

```
typed IPC/channel
```

118. Worker process tidak menerima:

```
arbitrary serialized executable data
```

119. Protocol harus:

```
versioned
bounded
validated
```

120. Worker Message

Messages:

```
AssignJob
CancelJob
Shutdown
Ping
```

121. Worker response:

```
Started
Progress
Completed
Failed
Cancelled
Lost
```

122. Unknown mutation outcome tetap:

```
durable job state
```

bukan hanya:

```
worker message
```

123. Backpressure

Worker result channel harus:

```
bounded
```

124. Progress events dapat:

```
coalesce
```

125. Progress tidak boleh:

```
block mutation completion
```

126. If progress channel full:

```
drop/coalesce progress
```

127. Never drop:

```
final result
```

128. Final result delivery harus:

```
durable
```

129. Worker output size:

```
bounded
```

130. Worker tidak boleh menghasilkan:

```
unbounded logs
```

131. Error Details

Internal error dapat berisi:

```
syscall class
errno
object identity
```

132. Public result hanya expose:

```
safe reason code
```

133. Worker logs harus:

```
privacy-aware
```

134. No arbitrary path dump.

135. FD Ownership

Worker tidak boleh mewarisi:

```
unrelated file descriptors
```

136. On worker start:

```
close unrelated FDs
```

137. FD inventory harus:

```
explicit
```

138. Executor-owned FD:

```
owned by execution scope
```

139. Setelah operation:

```
close
```

140. No FD leak across jobs.

141. Worker process restart:

```
naturally closes process FDs
```

142. Thread worker:

```
requires strict RAII ownership
```

143. TrustedRoot FD:

```
scoped
```

144. It must not become:

```
global mutable worker state
```

145. Resource Limits

Worker dapat memiliki:

```
CPU budget
memory budget
operation budget
metadata budget
execution deadline
```

146. Resource budget berasal dari:

```
EffectivePolicy
```

147. Worker tidak boleh menaikkan:

```
budget
```

148. Scheduler global budget dapat:

```
further restrict
```

149. Memory

Large scan harus:

```
streaming
```

150. Avoid:

```
load entire filesystem tree
```

151. Candidate set harus:

```
bounded
```

152. If memory budget exceeded:

```
BudgetExceeded
```

153. Never:

```
continue unbounded
```

154. CPU

Long-running work dapat:

```
yield
```

155. Worker priority harus:

```
conservative
```

156. High CPU priority must not be granted by client request.

157. CPU scheduling policy:

```
server-controlled
```

158. I/O

Worker harus menghormati:

```
I/O budget
```

159. Cleanup harus menghindari:

```
uncontrolled parallel filesystem mutation
```

160. Worker Resource Accounting

Track:

```
elapsed time
operations
bytes
metadata reads
directories visited
candidates processed
```

161. Accounting harus:

```
monotonic
```

162. Budget exhaustion:

```
typed result
```

163. Partial execution dapat:

```
occur
```

164. Partial result harus:

```
explicit
```

165. Operation Isolation

Setiap operation harus:

```
independent where possible
```

166. One operation failure:

```
should not corrupt unrelated operation state
```

167. But dependency graph remains:

```
enforced
```

168. Parent directory removal depends on:

```
child completion
```

169. Worker cannot reorder:

```
operation graph
```

170. Executor receives:

```
planned ordering
```

171. Worker cannot skip:

```
dependency
```

172. Worker cannot introduce:

```
parallelism that violates resource locks
```

173. Per-Operation Safety

Before each mutation:

```
final safety gate
```

174. Worker cannot say:

```
"already checked earlier"
```

175. Why

Filesystem may change:

```
between operations
```

176. Generation changes:

```
stale
```

177. Mount changes:

```
stale
```

178. Root identity changes:

```
stale
```

179. Package identity changes:

```
stale
```

180. Worker must stop when:

```
safety invalidated
```

181. Worker Result Contract

Worker returns:

```
WorkerResult
```

182. Fields:

```
JobId
AttemptId
WorkerId
phase
status
OperationResults
verification results
resource accounting
failure reason
```

183. WorkerResult must be:

```
immutable after emission
```

184. Duplicate result:

```
idempotently handled
```

185. Conflicting result:

```
corruption
```

186. Result Persistence

Worker should not be sole durable owner.

187. Main daemon or ResultStore persists:

```
authoritative state
```

188. Worker can persist:

```
checkpoints
```

if required.

189. But final JobResult follows:

```
Aggregator contract
```

190. Worker Crash During Result

Possible:

```
operation completed
worker crashed
result not delivered
```

191. State:

```
Unknown
```

192. Recovery:

```
Verifier reconciliation
```

193. Never:

```
assume failed
```

194. Worker Crash Before Operation

If durable state says:

```
operation intent not committed
```

then:

```
no external mutation assumed
```

but recovery must still verify where ambiguity exists.

195. Durable Execution Protocol

Conceptual:

```
persist intent
     ↓
worker executes
     ↓
persist operation outcome
     ↓
verify
     ↓
persist final result
```

196. Worker cannot skip:

```
durable intent
```

197. Worker cannot mark:

```
Completed
```

before result persistence protocol.

198. Job-to-Worker Assignment

Assignment:

```
JobId
AttemptId
WorkerId
assignment_sequence
```

199. Sequence prevents:

```
stale message confusion
```

200. Only current assignment may:

```
mutate job execution state
```

201. Worker Result after cancellation:

```
must still be processed
```

202. Cancellation is:

```
request
```

not:

```
guaranteed stop
```

203. Worker Shutdown

Shutdown command:

```
StopAccepting
     ↓
CancelAssigned
     ↓
Wait
     ↓
ForceStop if required
     ↓
Mark recovery states
     ↓
Exit
```

204. Worker must not:

```
accept new job after stop barrier
```

205. Worker Supervisor

Supervisor lifecycle:

```
Starting
   ↓
Ready
   ↓
Supervising
   ↓
Draining
   ↓
Stopped
```

206. Supervisor startup:

```
validate worker executable/context
```

207. Worker startup failure:

```
bounded retry
```

208. Worker restart loop must have:

```
restart backoff
```

209. Infinite crash loop:

```
forbidden
```

210. Crash threshold:

```
worker pool degraded
```

211. Degraded Pool

Scheduler may:

```
reduce admission
```

212. Destructive execution can be:

```
deferred
```

213. Read-only status may remain:

```
available
```

214. Worker Pool Recovery

After stable period:

```
re-enable worker
```

215. Stability must be:

```
measured
```

216. Worker Capability Separation

Conceptual:

```
┌──────────────────────────┐
│ Main Daemon              │
│                          │
│ Scheduler                │
│ Policy                   │
│ Authorization            │
└────────────┬─────────────┘
             │
             │ Authorized Job
             ▼
┌──────────────────────────┐
│ Cleanup Worker            │
│                          │
│ Exact AuthorizedPlan     │
│ Exact Resources          │
│ Exact Budget             │
└────────────┬─────────────┘
             │
             ▼
      Executor / FS
```

217. Worker cannot call:

```
Scheduler API to create another job
```

218. Worker cannot call:

```
Authorization API to elevate itself
```

219. Worker cannot reload:

```
config
```

220. Worker cannot alter:

```
catalog
```

221. Worker cannot grant:

```
capabilities
```

222. Worker cannot execute:

```
unplanned operation
```

223. Worker cannot access:

```
unrelated target
```

224. Worker Cannot Escape Scope

If operation says:

```
TargetA / relative/path
```

worker cannot switch to:

```
TargetB
```

225. Scope is enforced again by:

```
Filesystem Safety Layer
```

226. Defense in Depth

Worker boundary:

```
first restriction
```

227. Authorization:

```
capability restriction
```

228. Safety:

```
filesystem restriction
```

229. Executor:

```
mutation restriction
```

230. Kernel/platform adapter:

```
primitive restriction
```

231. Worker should therefore not be:

```
sole safety boundary
```

232. Progress Reporting

Progress event may include:

```
phase
completed_operations
total_operations
completed_bytes
estimated_remaining
```

233. Avoid:

```
raw path
```

234. Progress is:

```
best effort
```

235. Missing progress:

```
not failure
```

236. Final result:

```
authoritative
```

237. Worker Telemetry

Track:

```
worker_start
worker_ready
job_assigned
job_started
job_cancel_requested
job_finished
worker_panic
worker_crash
worker_restart
worker_hung
worker_poisoned
worker_stopped
```

238. Telemetry must use:

```
typed IDs
```

239. Sensitive internals should be:

```
redacted
```

240. Health Monitoring

Supervisor periodically checks:

```
heartbeat
queue state
worker state
memory
CPU
crash count
```

241. Health monitor itself must be:

```
bounded
```

242. Health monitor failure:

```
scheduler enters degraded mode
```

243. No unsafe assumption:

```
"worker probably alive"
```

244. Worker Protocol Fuzzing

Fuzz:

```
malformed AssignJob
unknown message type
oversized payload
truncated payload
invalid JobId
invalid AttemptId
invalid operation
invalid budget
invalid scope
duplicate sequence
```

245. Property:

```
invalid worker message never executes mutation
```

246. Crash Injection

Inject crash:

```
before intent
after intent
before mutation
during mutation
after mutation
before verification
after verification
before result persistence
```

247. Expected:

```
no blind replay
```

248. Race Tests

Test:

```
cancellation + mutation
shutdown + mutation
worker crash + result
worker restart + stale result
generation change + worker
mount change + worker
```

249. Resource Tests

Verify:

```
memory bounded
queue bounded
result bounded
FD bounded
worker count bounded
```

250. Fairness Tests

Create:

```
long-running job
short jobs
```

Ensure:

```
short jobs eventually receive execution opportunity
```

251. Destructive Fairness

Ensure:

```
no more than configured destructive concurrency
```

252. Poison Test

Repeated panic:

```
worker quarantined
```

253. Restart Test

Healthy worker crash:

```
restart
```

254. Crash Loop Test

Repeated crash:

```
degraded mode
```

255. Shutdown Test

No new assignment after:

```
stop barrier
```

256. Worker Security Invariants

Invariant 1:

```
Worker cannot create authority.
```

257. Invariant 2:

```
Worker cannot expand scope.
```

258. Invariant 3:

```
Worker cannot add operations.
```

259. Invariant 4:

```
Worker cannot bypass final safety.
```

260. Invariant 5:

```
Worker cannot bypass authorization.
```

261. Invariant 6:

```
Worker cannot directly interpret arbitrary client paths.
```

262. Invariant 7:

```
Worker crash cannot crash control plane.
```

263. Invariant 8:

```
Worker timeout cannot imply mutation failure.
```

264. Invariant 9:

```
Unknown outcome cannot be replayed blindly.
```

265. Invariant 10:

```
Worker queue is bounded.
```

266. Invariant 11:

```
Worker resources are bounded.
```

267. Invariant 12:

```
Worker FDs are scoped.
```

268. Invariant 13:

```
Worker cannot execute outside AuthorizedPlan.
```

269. Invariant 14:

```
Worker restart invalidates old WorkerId.
```

270. Invariant 15:

```
Stale worker result cannot mutate current Job state.
```

271. Invariant 16:

```
Poisoned worker cannot receive new work.
```

272. Invariant 17:

```
Forced termination during mutation requires reconciliation.
```

273. Invariant 18:

```
Cancellation is not assumed to stop an in-flight mutation.
```

274. Invariant 19:

```
Final result is durable outside worker memory.
```

275. Invariant 20:

```
Worker cannot turn progress events into authorization.
```

276. Suggested Structure

```
src/worker/
    mod.rs
    worker.rs
    supervisor.rs
    pool.rs
    assignment.rs
    lifecycle.rs
    cancellation.rs
    deadline.rs
    health.rs
    capability.rs
    protocol.rs
    result.rs
    resource.rs
    isolation.rs
    recovery.rs
```

277. Jika menggunakan process worker:

```
src/worker/process/
    mod.rs
    spawn.rs
    protocol.rs
    sandbox.rs
    lifecycle.rs
```

278. Interface utama:

```
WorkerPool
```

279. Methods:

```
acquire
assign
cancel
inspect
drain
shutdown
```

280. WorkerSupervisor interface:

```
start
stop
restart
health
quarantine
```

281. WorkerExecutor interface:

```
execute
```

282. WorkerExecutor tidak menerima:

```
raw CLI request
```

283. WorkerExecutor menerima:

```
JobExecutionContext
```

284. ApplicationController tetap menjadi:

```
orchestration owner
```

285. Scheduler tetap menjadi:

```
scheduling owner
```

286. WorkerSupervisor menjadi:

```
execution infrastructure owner
```

287. Executor tetap menjadi:

```
mutation owner
```

288. Verifier tetap menjadi:

```
postcondition owner
```

289. ResultAggregator tetap menjadi:

```
result semantics owner
```

290. Arsitektur akhir:

```
┌────────────────────────────────────────────┐
│                 CONTROL PLANE              │
│                                            │
│ Runtime → IPC/CLI → Controller → Scheduler │
└──────────────────────┬─────────────────────┘
                       │
                       ▼
┌────────────────────────────────────────────┐
│                  JOB PLANE                 │
│                                            │
│ Scanner → Safety → Policy → Planner        │
│                    ↓                       │
│               Authorization                │
└──────────────────────┬─────────────────────┘
                       │
                       ▼
┌────────────────────────────────────────────┐
│                WORKER PLANE                │
│                                            │
│ WorkerSupervisor                          │
│       │                                    │
│       ├── Worker A                         │
│       ├── Worker B                         │
│       └── Worker C                         │
└──────────────────────┬─────────────────────┘
                       │
                       ▼
┌────────────────────────────────────────────┐
│               EXECUTION PLANE              │
│                                            │
│ Executor → Filesystem / Kernel / Platform  │
└──────────────────────┬─────────────────────┘
                       │
                       ▼
┌────────────────────────────────────────────┐
│                 RESULT PLANE               │
│                                            │
│ Verifier → Aggregator → Durable Result     │
└────────────────────────────────────────────┘
```

291. Prinsip Worker

```
Worker
   ≠
Authority

Worker
   =
Bounded Execution Context
```

292. Worker tidak menjawab:

```
"bolehkah ini dilakukan?"
```

293. Worker hanya menjalankan:

```
"ini adalah pekerjaan yang
 sudah dinyatakan boleh dilakukan"
```

294. Tetapi Worker tetap melakukan:

```
final safety enforcement
```

sebagai defense-in-depth.

295. Jika konteks execution berubah:

```
stop
```

296. Jika authorization stale:

```
stop
```

297. Jika target identity berubah:

```
stop
```

298. Jika mount berubah:

```
stop
```

299. Jika budget habis:

```
stop
```

300. Jika cancellation:

```
stop pada safe boundary
```

301. Jika mutation outcome tidak diketahui:

```
recovery
```

302. Jika worker crash:

```
recovery
```

303. Jika worker restart:

```
old assignment invalid
```

304. Jika queue penuh:

```
backpressure
```

305. Jika worker poisoned:

```
quarantine
```

306. Jika shutdown:

```
drain
```

307. Definition of Done

```
[x] Worker lifecycle
```

```
[x] Worker pool
[x] Worker ownership
[x] Assignment identity
[x] Worker capability boundary
[x] Closed execution set
[x] Cancellation
[x] Timeout
[x] Deadline
[x] Panic containment
[x] Worker crash recovery
[x] Worker restart
[x] Worker health
[x] Heartbeat
[x] Hung worker handling
[x] Poisoned worker
[x] Bounded worker pool
[x] Backpressure
[x] Resource limits
[x] Memory limits
[x] CPU limits
[x] I/O accounting
[x] FD ownership
[x] Operation isolation
[x] Result delivery
[x] Unknown outcome
[x] Durable execution protocol
[x] Progress reporting
[x] Telemetry
[x] Worker protocol
[x] Protocol fuzzing
[x] Crash injection
[x] Race testing
[x] Shutdown behavior
[x] Security invariants
```

308. Posisi Setelah 63.md

```
Runtime
   │
   ▼
Scheduler
   │
   ▼
ApplicationController
   │
   ▼
WorkerSupervisor
   │
   ▼
Worker
   │
   ▼
Job Pipeline
   │
   ├── Scanner
   ├── Safety
   ├── Policy
   ├── Planner
   ├── Authorization
   ├── Executor
   └── Verifier
          │
          ▼
      Aggregator
          │
          ▼
      JobResult
```

309. Boundary yang sekarang sudah jelas:

```
Runtime
   = event/control

Scheduler
   = when

Controller
   = orchestration

Worker
   = bounded execution

Scanner
   = discovery

Safety
   = filesystem safety

Policy
   = eligibility

Planner
   = operation plan

Authorization
   = permission/capability

Executor
   = mutation

Verifier
   = postcondition

Aggregator
   = result semantics
```

310. Arah Berikutnya

64.md sebaiknya mengunci:

```
Resource Management and Concurrency Control Contract
```

dengan fokus pada:

```
┌──────────────────────────────────────────┐
│          RESOURCE MANAGER               │
└──────────────────┬───────────────────────┘
                   │
      ┌────────────┼────────────┐
      ↓            ↓            ↓
  CPU Resource  Memory      I/O
      │            │            │
      └────────────┼────────────┘
                   ↓
             Resource Locks
                   │
      ┌────────────┼─────────────┐
      ↓            ↓             ↓
   Target       Storage       Kernel
      │            │             │
      └────────────┼─────────────┘
                   ↓
              WorkerPool
                   │
                   ↓
                Executor
```

Fokus 64.md:

```
Global Resource Budget
Per-Job Resource Budget
Per-Operation Budget
Resource Tokens
CPU Budget
Memory Budget
I/O Budget
Filesystem Concurrency
Storage Concurrency
Target Locks
User Locks
Package Locks
Mount Locks
Block Device Locks
Kernel Maintenance Locks
Lock Ordering
Deadlock Prevention
Lock Timeout
Resource Starvation
Fairness
Priority Interaction
Admission Control
Backpressure
Resource Pressure
Thermal Pressure
Memory Pressure
Storage Pressure
Resource Accounting
Resource Reservation
Reservation Expiration
Cancellation
Recovery
Resource Leak Detection
Resource State Persistence
Crash Recovery
Deterministic Resource Arbitration
Testing
Fuzzing
Race Testing
dan Security Invariants
```
