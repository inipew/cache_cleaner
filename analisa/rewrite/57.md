57.md — Cache Cleanup Planning Semantics, Candidate-to-Operation Mapping, File vs Directory Deletion, Nested Candidate Graph, Parent/Child Dependency, Operation Deduplication, Conflict Detection, Hard-Link Handling, Symlink Handling, Empty Directory Planning, Byte/Operation Budget, Partial Plan, Deterministic Ordering, Plan Explainability, Plan Immutability, dan Cache Cleanup Plan Contract

Tujuan

Dokumen ini mengunci bagaimana candidate yang sudah lolos:

```
Discovery
    │
    ▼
Classification
    │
    ▼
Safety
    │
    ▼
Policy
```

diubah menjadi:

```
PlannedOperation
```

Planner tidak melakukan deletion.

Planner hanya membuat:

```
deterministic
immutable
dependency-aware
budget-bounded
```

execution plan.

1. Posisi Planner

Alur utama:

```
Candidate
   │
   ▼
SafetyValidatedCandidate
   │
   ▼
PolicyApprovedCandidate
   │
   ▼
Planner
   │
   ▼
PlannedOperation
   │
   ▼
PlannedPlan
   │
   ▼
Authorization
   │
   ▼
AuthorizedPlan
   │
   ▼
Executor
```

2. Planner Bukan Executor

Planner tidak boleh:

```
unlink
rmdir
rename
chmod
chown
write
```

3. Planner Tidak Menyentuh Filesystem Mutation

Planner hanya menggunakan:

```
candidate metadata
target metadata
policy
budget
generation
```

4. Input Planner

Minimal:

```
PolicyApprovedCandidate
CatalogSnapshot
PlatformSnapshot
EffectivePolicy
CleanupBudget
```

5. Output Planner

Minimal:

```
PlannedPlan
```

6. PlannedPlan

Harus immutable setelah dibuat.

7. Plan Identity

Setiap plan memiliki:

```
PlanId
```

8. Operation Identity

Setiap operation memiliki:

```
OperationId
```

9. Plan Generation

Plan terikat:

```
ConfigGeneration
CatalogGeneration
PlatformGeneration
```

10. Why

Plan yang dibuat berdasarkan state lama tidak boleh dieksekusi tanpa:

```
generation validation
```

11. Operation Type

Minimum:

```
RemoveFile
RemoveEmptyDirectory
```

12. No Generic Delete

Forbidden:

```
Delete(path)
```

13. Typed Operation

Contoh:

```
RemoveFile {
    target
    expected_identity
}
```

dan:

```
RemoveEmptyDirectory {
    target
    expected_identity
}
```

14. Operation Target

Target harus berupa:

```
TargetId
RelativePath
ObjectIdentity
```

15. No Absolute Mutation Path

PlannedOperation tidak boleh bergantung pada:

```
arbitrary absolute path
```

16. RelativePath

Semua candidate path:

```
relative to TrustedRoot
```

17. Target Root

TrustedRoot sendiri tidak menjadi:

```
deletion operation
```

18. Root Protection

Invariant:

```
TargetRoot
   ↓
never Remove
```

19. File Candidate

Jika:

```
ObjectType = RegularFile
```

dan policy approve:

```
RemoveFile
```

20. Symlink Candidate

Jika policy mengizinkan symlink deletion:

```
RemoveSymlinkEntry
```

atau typed equivalent.

21. Avoid Ambiguous Type

Jangan menggunakan:

```
RemoveFile
```

untuk:

```
Directory
```

22. Special File

Default:

```
no operation
```

23. Directory Candidate

Directory deletion hanya direncanakan sebagai:

```
RemoveEmptyDirectory
```

24. Why

Planner tidak boleh membuat:

```
RecursiveDelete
```

25. RecursiveDelete

Forbidden.

26. Directory Dependency

Untuk:

```
cache/
  ├── a
  └── nested/
        └── b
```

operation graph:

```
RemoveFile(b)
      │
      ▼
RemoveEmptyDirectory(nested)
      │
      ▼
RemoveFile(a)
      │
      ▼
RemoveEmptyDirectory(cache)
```

27. Child Before Parent

Parent directory deletion:

```
depends on all planned children
```

28. DAG

Plan harus membentuk:

```
Directed Acyclic Graph
```

29. Operation Graph

```
O1 ──────┐
         ▼
O2 ───► O4
         ▲
O3 ──────┘
```

30. Topological Ordering

Executor menerima:

```
valid topological order
```

31. Determinism

Graph ordering harus deterministic.

32. Stable Sort

Gunakan stable ordering berdasarkan:

```
TargetId
RelativePath
ObjectType
OperationId
```

atau aturan deterministik yang ditetapkan.

33. Lexical Path

Path ordering dapat menggunakan:

```
bytewise canonical component ordering
```

34. Locale

Jangan menggunakan:

```
locale-dependent sorting
```

35. Unicode

Path component ordering harus menggunakan:

```
canonical internal representation
```

bukan:

```
UI locale
```

36. Duplicate Candidate

Candidate yang sama dapat muncul karena:

```
scanner overlap
retry
event race
duplicate enumeration
```

37. Deduplication

Planner harus melakukan:

```
deterministic deduplication
```

38. Dedup Key

Minimum:

```
TargetId
RelativePath
ObjectIdentity
```

39. Identity Difference

Jika path sama tetapi identity berbeda:

```
jangan merge
```

40. Why

Scenario:

```
/cache/a
    │
    ├── old inode
    └── new inode
```

Identity berbeda berarti:

```
distinct observations
```

41. Duplicate Same Identity

Jika:

```
same TargetId
same RelativePath
same identity
same operation type
```

boleh deduplicate.

42. Conflicting Types

Contoh:

```
Candidate A:
    path = x
    type = File

Candidate B:
    path = x
    type = Directory
```

Plan harus:

```
reject conflict
```

43. Conflict Reason

```
PlanConflict::TypeMismatch
```

44. Identity Conflict

Same path:

```
identity A
identity B
```

result:

```
PlanConflict::IdentityMismatch
```

45. Parent/Child Conflict

Contoh:

```
planned delete parent
planned delete child
```

Jika parent operation:

```
RemoveEmptyDirectory
```

maka dependency:

```
child → parent
```

46. Parent Candidate Not Planned

Jika parent directory tidak eligible:

```
child operations tetap dapat dijalankan
```

47. Example

```
cache/
  ├── old.tmp
  └── keep.tmp
```

Policy:

```
old.tmp eligible
keep.tmp excluded
```

Plan:

```
RemoveFile(old.tmp)
```

Tidak:

```
RemoveEmptyDirectory(cache)
```

48. Why

cache/ masih berisi:

```
keep.tmp
```

49. Directory Operation

Directory operation hanya dibuat jika:

```
directory itself eligible
all relevant children are absent/removed
```

50. Planning Knowledge

Planner mengetahui:

```
planned children
```

tetapi tidak boleh mengklaim:

```
runtime directory definitely empty
```

51. Final Truth

Executor + verifier tetap menentukan.

52. Directory Race

Setelah planning:

```
new file created
```

directory removal:

```
NotEmpty
```

53. No Recursive Recovery

Executor tidak boleh:

```
discover new file
delete it
retry parent
```

54. Why

New file bukan bagian dari:

```
AuthorizedPlan
```

55. Authorization Scope

Every operation:

```
explicit
```

56. Hard Links

Candidate identity:

```
same inode
```

dapat muncul pada:

```
multiple paths
```

57. Planner Semantics

Do not deduplicate distinct directory entries solely by inode.

58. Example

    cache/
    ├── a
    └── b

    a ──┐
    ├── same inode
    b ──┘

Deleting:

```
a
```

tidak otomatis berarti:

```
b
```

59. Operation Count

Plan count:

```
1
```

if only a is eligible.

60. Inode-wide Delete

Forbidden.

61. Symlink

Symlink is a:

```
directory entry
```

62. Symlink Target

Planner tidak follows:

```
symlink target
```

63. Symlink Operation

Operation semantics:

```
remove entry
```

64. Symlink Directory

Symlink pointing to directory:

```
still symlink
```

65. No Directory Recursion Through Symlink

Forbidden.

66. Mount Boundary

Planner harus menghormati:

```
candidate mount identity
```

67. Cross-Mount Candidate

If policy forbids:

```
no operation
```

68. Mount Change

If generation/mount identity changes:

```
plan stale
```

69. Filesystem Change

If target filesystem changes:

```
plan stale
```

70. Package Change

If package identity changes:

```
plan stale
```

71. User Change

If user storage state changes:

```
plan stale
```

72. Config Change

If cleanup policy changes:

```
old plan remains bound to old config generation
```

73. No Policy Reinterpretation

Old plan tidak boleh:

```
silently adopt new policy
```

74. Config Reload

Active plan:

```
unchanged
```

New request:

```
new plan
```

75. Budget

Planner harus menghormati:

```
max_operations
max_bytes
max_duration_estimate
```

76. Operation Budget

Jika:

```
max_operations = 5000
```

planner tidak membuat:

```
5001
```

77. Byte Budget

Candidate size:

```
contributes to estimated bytes
```

78. Directory Size

Directory operation:

```
size contribution dapat berupa:

0
```

karena actual recursive bytes sudah dihitung dari children.

79. Avoid Double Counting

Example:

```
file a = 10 MB
directory cache = contains a
```

Plan byte estimate:

```
10 MB
```

bukan:

```
20 MB
```

80. Unknown Size

If size unavailable:

```
estimated_bytes = Unknown
```

81. Unknown Budget

Jika byte budget membutuhkan exact size:

```
no operation
```

atau:

```
partial plan
```

sesuai policy.

82. Conservative Budget

Never treat:

```
Unknown
```

as:

```
zero
```

83. Overflow

Byte aggregation:

```
checked arithmetic
```

84. Duration Estimate

Planner dapat menggunakan:

```
operation cost model
```

85. Unknown Duration

Jika unknown:

```
conservative estimate
```

86. Duration Budget

Plan harus tidak melebihi:

```
maximum planned duration
```

87. Planner vs Actual Duration

Estimated duration:

```
planning constraint
```

Actual duration:

```
executor runtime budget
```

88. Two Budgets

```
Planning Budget
     │
     ▼
Planned Operations

Execution Budget
     │
     ▼
Actual Operations
```

89. Partial Plan

Jika candidate count > budget:

```
planner dapat menghasilkan partial plan
```

90. Partial Plan Rule

Partial plan harus:

```
deterministic
```

91. Which Candidates First

Priority berdasarkan:

```
explicit policy
deterministic ordering
```

92. No Random Selection

Forbidden.

93. Priority

Priority tidak boleh:

```
bypass safety
```

94. Example

```
protected candidate
    ↓
never planned
```

95. Explainability

Plan harus dapat menjelaskan:

```
why candidate became operation
```

96. Operation Reason

Example:

```
EligibleBecause:
    TargetClass = AppCache
    Age >= 7d
    OwnershipValid
    PolicyAllowed
```

97. No Sensitive Metadata

Explanation harus mengikuti:

```
privacy boundary
```

98. Plan Summary

Plan dapat menyimpan:

```
operation_count
estimated_bytes
target_count
partial
policy_generation
```

99. Plan Fingerprint

Optional:

```
deterministic plan digest
```

100. Fingerprint

Useful untuk:

```
audit
debugging
crash recovery
```

101. Fingerprint Tidak Menjadi Security Proof

Digest hanya:

```
consistency aid
```

102. Plan Immutability

Setelah:

```
PlannedPlan
```

dibuat:

```
no mutation
```

103. Mutable Scheduler State

Scheduler dapat menyimpan:

```
JobState
```

tetapi:

```
Plan tetap immutable
```

104. Cancellation

Planner dapat dihentikan:

```
CancellationToken
```

105. Cancellation During Planning

Result:

```
PlanCancelled
```

106. No Partial Implicit Execution

Partial internal planner state:

```
never sent to executor
```

107. Plan Publication

Only complete:

```
PlannedPlan
```

dipublish.

108. Plan Validation

Before publication:

```
graph valid
dependencies valid
targets valid
budgets valid
generations consistent
```

109. Graph Cycle

If cycle:

```
PlannerError::CycleDetected
```

110. Example Cycle

```
O1 → O2
O2 → O1
```

Forbidden.

111. Self Dependency

```
O1 → O1
```

Forbidden.

112. Missing Dependency

Operation referencing unknown dependency:

```
invalid plan
```

113. Duplicate Operation ID

```
invalid plan
```

114. Duplicate Target Conflict

Conflicting operations on same directory entry:

```
invalid plan
```

115. Operation Scope

Operation must belong to:

```
one TargetId
```

116. Cross-target Operation

One operation cannot span:

```
TargetA
TargetB
```

117. Why

Scope ambiguity.

118. Cross-target Dependency

Allowed only if architecture explicitly defines it.

Default:

```
forbidden
```

119. Current Cache Cleaner

Filesystem cleanup should remain:

```
target-local
```

120. Package Boundary

Operation cannot silently cross:

```
PackageId
```

121. User Boundary

Operation cannot silently cross:

```
UserId
```

122. Storage Boundary

Operation cannot silently cross:

```
StorageClass
```

123. Target Boundary

Operation cannot silently cross:

```
TrustedRoot
```

124. Planner Scope Check

Every operation:

```
Target
   │
   ├── package
   ├── user
   ├── storage
   └── root
          │
          ▼
      candidate
```

125. Candidate Outside Scope

No operation.

126. Protected Ancestor

If ancestor is:

```
Protected
```

descendants are:

```
protected from recursive interpretation
```

127. Important

Planner does not use:

```
"delete parent"
```

to implicitly delete children.

128. Child Authorization

Every deletion operation must be:

```
explicitly authorized
```

129. Directory Authorization

Even:

```
RemoveEmptyDirectory
```

is its own operation.

130. Why

Prevents:

```
implicit recursive deletion
```

131. Plan Serialization

If persisted:

```
versioned DTO
```

132. Persisted Plan

Must contain:

```
PlanId
JobId
generation
operations
dependencies
budget
fingerprint
```

133. Raw FD

Plan must never persist:

```
file descriptor
```

134. FD Reopen

After restart:

```
executor re-resolves through trusted root
```

135. No Persisted FD Assumption

FD numbers are:

```
process-local
ephemeral
```

136. Crash Recovery

Plan state:

```
durable
```

but execution must still:

```
revalidate
```

137. Stale Plan

If:

```
generation mismatch
```

result:

```
Stale
```

138. Unknown Plan

If plan corruption:

```
no execution
```

139. Plan Integrity

Persisted plan requires:

```
schema version
integrity verification
```

140. No Partial Plan Persistence

Either:

```
complete plan
```

or:

```
no usable plan
```

141. Operation Status

Execution state is separate from:

```
PlannedOperation
```

142. PlannedOperation

Immutable intent.

143. OperationRecord

Durable lifecycle:

```
Planned
Authorized
Started
OutcomeKnown
Verified
```

144. Planner Does Not Set Started

Only Executor/runtime persistence layer.

145. Ordering

Preferred ordering:

```
dependencies first
    ↓
deterministic sibling order
```

146. Example

```
b
a
nested/c
```

Could become:

```
nested/c
a
b
nested/
```

depending on dependency graph and stable ordering.

147. Ordering Rule

Define one canonical algorithm.

Do not rely on:

```
HashMap iteration order
```

148. Data Structures

Use deterministic collections where needed.

149. Rust Consideration

Avoid assuming:

```
HashMap order
```

150. Plan Stability

Same input snapshot:

```
same plan
```

151. Plan Reproducibility

Useful untuk:

```
testing
audit
debugging
```

152. Candidate Snapshot

Planner tidak boleh mengambil:

```
live mutable candidate
```

153. Snapshot

Input harus:

```
immutable
```

154. Policy Snapshot

Policy:

```
immutable
```

155. Budget Snapshot

Budget:

```
immutable
```

156. Catalog Snapshot

Catalog:

```
immutable
```

157. Platform Snapshot

Platform:

```
immutable
```

158. Planning Context

```
PlanningContext
     │
     ├── candidates
     ├── catalog
     ├── platform
     ├── policy
     ├── budget
     └── generation
            │
            ▼
         Planner
            │
            ▼
         PlannedPlan
```

159. No Live Lookup

Planner tidak melakukan:

```
dynamic platform discovery
```

160. Why

Dynamic lookup dapat membuat:

```
nondeterministic plan
```

161. Refresh

Jika platform changes:

```
invalidate planning context
```

162. Replan

```
stale
  │
  ▼
fresh snapshot
  │
  ▼
rescan/reclassify
  │
  ▼
replan
```

163. No Blind Replan From Old Candidates

Candidates juga bisa stale.

164. Correct Recovery

```
Stale Plan
```

```
   │
   ▼
New CatalogSnapshot
   │
   ▼
New Scan
   │
   ▼
New Classification
   │
   ▼
New Policy
   │
   ▼
New Plan
```

165. Why

Prevents:

```
old candidate
new target state
```

mismatch.

166. Planner Error

Minimum:

```
InvalidCandidate
InvalidTarget
GenerationMismatch
BudgetExceeded
Conflict
CycleDetected
DuplicateOperation
UnsupportedOperation
Cancelled
SerializationFailure
```

167. Failure Semantics

Planner failure:

```
no executable plan
```

168. Partial Planning Failure

Partial internal state:

```
discarded
```

unless explicitly represented as:

```
valid PartialPlan
```

169. PartialPlan

PartialPlan harus menjelaskan:

```
included
excluded
reason
budget boundary
```

170. No Silent Truncation

Forbidden:

```
silently stop at N candidates
```

171. Explicit Partial

Result:

```
PartialPlan
```

172. Audit

Plan creation should record:

```
JobId
PlanId
generation
operation count
estimated bytes
partial status
planner version
```

173. Metrics

Metrics:

```
plans_created_total
plans_partial_total
plans_rejected_total
planner_conflicts_total
planner_cycles_total
planner_budget_exceeded_total
operations_planned_total
directory_operations_planned_total
file_operations_planned_total
```

174. Explain API

Explain request dapat:

```
show candidate
   ↓
classification
   ↓
policy
   ↓
planned operation
   ↓
dependency
```

175. Example

```
Candidate:
    old.tmp

Classification:
    Cache

Policy:
    Eligible

Planner:
    RemoveFile

Dependency:
    none
```

176. Directory Example

```
Candidate:
    cache/nested

Classification:
    Cache

Policy:
    Eligible

Planner:
    RemoveEmptyDirectory

Dependencies:
    child operations first
```

177. Rejection Example

```
Candidate:
    cache/keep.dat

Classification:
    Cache

Policy:
    AgeTooYoung

Planner:
    no operation
```

178. Conflict Example

```
same path:
    File identity A
    Directory identity B
```

Planner:

```
reject plan
```

179. Hard Link Example

```
cache/a
cache/b
```

same inode.

Plan:

```
RemoveFile(cache/a)
```

does not create:

```
RemoveFile(cache/b)
```

unless b independently qualifies.

180. Symlink Example

```
cache/link → /important
```

Plan:

```
RemoveSymlinkEntry(cache/link)
```

Never:

```
traverse /important
```

181. Directory Example

```
cache/nested/
    │
    ├── old.tmp
    └── keep.tmp
```

Only old.tmp eligible.

Plan:

```
RemoveFile(old.tmp)
```

No:

```
RemoveEmptyDirectory(nested)
```

182. Final Planning Rule

Planner creates only:

```
explicit operations
```

183. Final Safety Rule

Every operation remains subject to:

```
final safety validation
```

184. Final Authorization Rule

Every operation remains subject to:

```
authorization
```

185. Final Executor Rule

Executor never expands plan scope.

186. No Dynamic Expansion

Executor cannot:

```
scan new children
discover new targets
add operations
```

187. Why

AuthorizedPlan is:

```
closed set of operations
```

188. Closed Set

If:

```
AuthorizedPlan = {O1, O2, O3}
```

Executor may execute:

```
O1
O2
O3
```

but never:

```
O4
```

189. Security Invariant

```
Planner
   ↓
Explicit Operation Set
   ↓
Authorization
   ↓
Closed Execution Scope
```

190. Definition of Done

57.md selesai jika:

```
[x] Candidate-to-operation mapping didefinisikan
[x] Typed operations didefinisikan
[x] File deletion didefinisikan
[x] Symlink deletion didefinisikan
[x] Directory deletion didefinisikan
[x] Recursive deletion dilarang
[x] Parent/child dependency didefinisikan
[x] DAG didefinisikan
[x] Topological ordering didefinisikan
[x] Deterministic ordering didefinisikan
[x] Deduplication didefinisikan
[x] Hard-link semantics didefinisikan
[x] Conflict detection didefinisikan
[x] Mount boundary didefinisikan
[x] Target boundary didefinisikan
[x] Package/user boundary didefinisikan
[x] Operation budget didefinisikan
[x] Byte budget didefinisikan
[x] Duration budget didefinisikan
[x] Partial plan didefinisikan
[x] Plan immutability didefinisikan
[x] Generation binding didefinisikan
[x] Crash persistence didefinisikan
[x] No dynamic execution expansion didefinisikan
[x] Explainability didefinisikan
[x] Audit/metrics didefinisikan
[x] Testing invariants didefinisikan
```

191. Arah Berikutnya

Sekarang alur utama sudah semakin lengkap:

```
54.md
Secure Filesystem Primitive
      │
      ▼
55.md
Android Target Discovery
      │
      ▼
56.md
Cache Classification
      │
      ▼
57.md
Cleanup Planning
      │
      ▼
Authorization
      │
      ▼
Executor
      │
      ▼
Verifier
      │
      ▼
Result
```

Lapisan berikutnya perlu mengunci authorization secara lebih konkret pada level:

```
operation scope
capability
role
request origin
protected target
budget
generation
expiration
final approval
```

Fokus 58.md:

```
58.md — Authorization Implementation Contract,
       Operation-Level Authorization,
       Capability Intersection,
       Request Origin,
       Role Mapping,
       Scope Restriction,
       Protected Target Enforcement,
       Budget Authorization,
       Generation Binding,
       Plan Expiration,
       Final Approval,
       Authorization Decision,
       Capability Downgrade,
       IPC/CLI/Scheduler Authorization,
       Audit,
       Fail-Closed Behavior,
       dan AuthorizedPlan Construction Contract
```
