56.md — Cache Classification and Eligibility Contract, Cache Type Semantics, File/Directory Eligibility, Age Rules, Size Rules, Protected Files, Active/Recently Used Data, Package State, Cache Ownership, Temporary File Semantics, Code Cache Semantics, Directory Deletion Eligibility, Unknown Metadata, Timestamp Reliability, Candidate Classification, Exclusion Reasons, dan Formal Cache Eligibility Model

Tujuan

Dokumen ini mengunci definisi:

```
"Apa yang boleh dianggap sebagai cache?"
```

Filesystem entry yang ditemukan Scanner:

```
TIDAK OTOMATIS
menjadi target deletion.
```

Alur:

```
TargetDescriptor
      │
      ▼
   Scanner
      │
      ▼
   Candidate
      │
      ▼
Classification
      │
      ├── Cache
      ├── NotCache
      ├── Protected
      ├── Unknown
      └── Deferred
      │
      ▼
   Policy
      │
      ▼
PolicyApprovedCandidate
      │
      ▼
   Planner
```

1. Cache Adalah Semantic Classification

Cache bukan sekadar:

```
directory bernama cache
```

atau:

```
file lama
```

Cache harus mempunyai:

```
known target semantics
known ownership
known scope
valid safety boundary
```

2. Target Class

Target sudah menentukan konteks:

```
AppCache
CodeCache
Temporary
```

3. Candidate Class

Candidate menentukan object aktual:

```
RegularFile
Directory
Symlink
Special
Unknown
```

4. Target Tidak Sama Dengan Candidate

Contoh:

```
Target
  AppCache
     │
     ├── a.tmp
     ├── image.bin
     └── subdir/
              │
              └── old.dat
```

Target adalah:

```
AppCache
```

Candidate adalah:

```
a.tmp
image.bin
subdir/
```

5. Classification

Classification menjawab:

```
apakah candidate memenuhi semantic definition
dari target?
```

6. Classification Tidak Menghapus

Classifier:

```
read-only
```

7. Classification Tidak Memberi Authorization

Classification:

```
bukan authorization
```

8. Classification Tidak Memberi Policy Approval

Classification:

```
bukan policy
```

9. Separation

```
Discovery
   │
   ▼
Classification
   │
   ▼
Safety
   │
   ▼
Policy
   │
   ▼
Planning
   │
   ▼
Authorization
   │
   ▼
Execution
```

10. Classification Result

Minimal:

```
Cache
NotCache
Protected
Unknown
Deferred
```

11. Cache

Berarti:

```
semantic cache candidate
```

12. NotCache

Berarti:

```
candidate berada di target area
tetapi tidak memenuhi cache semantics
```

13. Protected

Berarti:

```
candidate tidak boleh dihapus
```

14. Unknown

Berarti:

```
evidence tidak cukup
```

15. Deferred

Berarti:

```
candidate mungkin eligible
tetapi kondisi runtime belum memungkinkan keputusan
```

16. Unknown Is Not Allow

Invariant:

```
Unknown
   ↓
NOT deletion
```

17. Protected Is Stronger

Invariant:

```
Protected
   ↓
NEVER deletion
```

18. AppCache Semantics

AppCache dianggap:

```
disposable application-generated data
```

tetapi hanya jika:

```
ownership valid
target valid
object berada dalam target scope
```

19. CodeCache Semantics

CodeCache merupakan:

```
application-generated compiled/cache artifacts
```

tetapi tetap mengikuti:

```
package scope
user scope
target boundary
```

20. Temporary Semantics

Temporary data harus mempunyai:

```
explicit target definition
```

21. Arbitrary Temporary File

Nama:

```
tmp123
temporary.dat
```

tidak cukup untuk menentukan:

```
cache
```

22. Cache Filename

Filename pattern bukan authority.

23. Directory Name

Directory name bukan authority.

24. Extension

Extension bukan authority.

25. MIME Type

MIME type bukan authority.

26. Content Inspection

Default:

```
tidak membaca content
```

untuk menentukan cache eligibility.

27. Why

Content inspection:

```
mahal
privacy-sensitive
tidak selalu reliable
```

28. Metadata

Classification dapat menggunakan:

```
object type
size
timestamps
ownership metadata
target class
package/user state
storage state
```

29. Metadata Reliability

Metadata memiliki confidence.

30. Timestamp

Timestamp dapat digunakan:

```
hanya jika policy mengizinkan
```

31. Timestamp Tidak Mutlak

Timestamp tidak boleh dianggap:

```
proof of inactivity
```

32. Atime

atime tidak dapat dianggap reliable secara universal karena:

```
mount configuration
relatime
noatime
filesystem behavior
```

33. Mtime

mtime dapat menunjukkan:

```
last modification
```

tetapi tidak selalu berarti:

```
last use
```

34. Ctime

ctime menunjukkan perubahan metadata tertentu, bukan:

```
application usage time
```

35. Birth Time

Jika tersedia:

```
optional metadata
```

tetapi bukan mandatory assumption.

36. Timestamp Policy

Policy harus mendefinisikan:

```
timestamp source
acceptable age
unknown behavior
```

37. Age

Konseptual:

```
now - timestamp >= minimum_age
```

38. Clock

Age calculation menggunakan:

```
trusted WallClock
```

39. Monotonic Clock

Monotonic clock digunakan untuk:

```
timeout
duration
budget
```

40. Wall Clock

Wall clock digunakan untuk:

```
filesystem timestamps
user-facing timestamps
```

41. Clock Anomaly

Jika:

```
timestamp > now + tolerance
```

jangan otomatis menganggap:

```
very old
```

42. Future Timestamp

Possible result:

```
Unknown
Deferred
NotEligible
```

43. Timestamp Zero

Jika timestamp invalid:

```
Unknown
```

44. Timestamp Missing

Jika policy membutuhkan timestamp:

```
Unknown
```

45. Unknown Timestamp

Default:

```
no deletion
```

46. Minimum Age

Age threshold merupakan:

```
Policy rule
```

47. Scanner vs Policy

Scanner dapat mengumpulkan:

```
timestamp
```

tetapi Policy menentukan:

```
apakah age cukup
```

48. Example

```
Candidate
   │
   ├── mtime = T
   └── now = N
          │
          ▼
     age = N - T
          │
          ▼
      Policy
```

49. Negative Age

Jika age negatif secara tidak wajar:

```
Unknown
```

50. Age Boundary

Jika:

```
age == minimum_age
```

harus ada definisi inclusive/exclusive yang konsisten.

51. Default

Disarankan:

```
age >= minimum_age
```

52. Size

Size dapat digunakan untuk:

```
budgeting
reporting
policy
```

53. Size Tidak Menentukan Cache

File besar bukan otomatis:

```
cache
```

54. Zero Size

File zero-byte dapat:

```
eligible
non-eligible
```

tergantung semantics.

55. Unknown Size

Jika size tidak tersedia:

```
Unknown
```

jika policy memerlukan size.

56. Directory Size

Directory size bukan:

```
total recursive content size
```

57. Recursive Size

Jika diperlukan:

```
scanner menghitung berdasarkan children
```

58. Budget

Recursive size calculation harus memiliki:

```
entry budget
duration budget
metadata budget
```

59. Size Overflow

Arithmetic harus:

```
checked
```

60. Integer Overflow

Forbidden:

```
unchecked byte aggregation
```

61. Maximum Candidate Size

Policy dapat menentukan:

```
max_candidate_size
```

62. Minimum Candidate Size

Policy dapat menentukan:

```
min_candidate_size
```

63. Size Rule

Size rule harus:

```
deterministic
```

64. Ownership

Candidate harus berada dalam:

```
target ownership scope
```

65. Ownership Source

Ownership dapat berasal dari:

```
TargetDescriptor
Package metadata
Platform metadata
Filesystem metadata
```

66. Ownership Unknown

Default:

```
not eligible
```

67. Package Ownership

Package cache:

```
must belong to expected package scope
```

68. User Ownership

Candidate:

```
must belong to expected user scope
```

69. User Mismatch

Result:

```
NotCache
Protected
atau
Unknown
```

bergantung pada evidence.

70. Storage Scope

Candidate tidak boleh keluar dari:

```
expected StorageClass
```

71. CE/DE

Candidate dari:

```
CredentialProtected
```

tidak boleh dianggap setara dengan:

```
DeviceProtected
```

72. Locked Storage

Jika storage locked:

```
classification may be Deferred
```

73. Package State

Package state memengaruhi eligibility.

74. Installed

Normal cache cleanup dapat:

```
proceed
```

jika semua rule lain terpenuhi.

75. Updating

Default:

```
Deferred
```

76. Disabled

Policy menentukan.

Disabled package tidak otomatis:

```
orphaned
```

77. Suspended

Tidak otomatis:

```
safe to delete
```

78. Uninstalled

Orphan cleanup memerlukan:

```
explicit policy
```

79. Unknown Package State

Result:

```
Unknown
```

80. Package Reinstalled

Jika identity berbeda:

```
old candidate becomes stale
```

81. Package Generation

Candidate dapat membawa:

```
PackageGeneration
```

82. Generation Mismatch

Result:

```
Stale
```

83. Protected Files

Certain files may always be:

```
Protected
```

84. Protection Source

Protection dapat berasal dari:

```
Platform
Target definition
Policy
```

85. Platform Protection

Tidak dapat di-overrule oleh policy.

86. Policy Protection

Policy dapat memperluas exclusion.

87. Config Protection

Config dapat mempersempit scope.

88. Protection Precedence

    Platform Protected
    │
    ▼
    Safety
    │
    ▼
    Config
    │
    ▼
    Policy
    │
    ▼
    Authorization

89. Protected Filename

Filename-based protection boleh digunakan sebagai:

```
additional defense
```

tetapi bukan satu-satunya security proof.

90. Protected Directory

Directory yang protected:

```
seluruh subtree protected
```

91. Protected Ancestor

Jika ancestor protected:

```
child tidak eligible
```

92. Protected Marker

Protection harus direpresentasikan secara typed:

```
ProtectionReason
```

93. Example Reasons

```
SystemCritical
PlatformManaged
PackageManagerState
SecuritySensitive
UnknownOwnership
ExplicitlyExcluded
```

94. Active Data

"Recently used" tidak boleh disimpulkan hanya dari:

```
mtime
```

95. Active Package

Package state dapat menjadi signal.

96. Active Process

Jika platform menyediakan informasi proses:

```
dapat menjadi additional policy signal
```

tetapi:

```
bukan filesystem safety proof
```

97. Running Application

Default policy dapat memilih:

```
defer cleanup
```

untuk package aktif.

98. Why

Application dapat sedang:

```
writing cache
reading cache
creating files
```

99. Runtime Condition

```
PackageRunning
     │
     ▼
   Defer
```

100. Running State Unknown

```
Unknown
   ↓
conservative
```

101. Race

Package dapat mulai running setelah scan.

102. Final Check

Executor tidak boleh mengandalkan:

```
scan-time process state
```

103. Authorization

Jika policy membutuhkan:

```
package not running
```

final authorization/runtime readiness harus memeriksa ulang.

104. Runtime Readiness

Contoh:

```
ScreenOn
ThermalHigh
UserActive
StorageLocked
PackageBusy
```

dapat menyebabkan:

```
Deferred
```

105. Deferred Is Not Failure

Deferred berarti:

```
operation tidak dilakukan
```

106. Retry

Scheduler dapat:

```
retry later
```

107. Cache Directory

Directory candidate tidak otomatis dihapus.

108. Directory Deletion

Directory dapat dihapus hanya jika:

```
policy allows
directory identity valid
directory is empty
target scope valid
```

109. Recursive Directory Cleanup

Planner harus membuat:

```
child operations
   ↓
parent operation
```

110. Empty Directory

Jika directory sudah kosong:

```
AlreadySatisfied
```

111. Non-empty Directory

Result:

```
NotEmpty
```

112. Race During Directory Removal

Jika child muncul:

```
NotEmpty
```

113. Child Creation

Executor tidak boleh:

```
recursively delete newly created child
```

114. Why

That would turn:

```
bounded planned operation
```

menjadi:

```
uncontrolled recursive deletion
```

115. Directory Candidate

Classification dapat:

```
Cache
```

tetapi deletion plan tetap:

```
explicit
```

116. Directory Tree

```
cache/
  │
  ├── a.tmp
  ├── b.tmp
  └── nested/
        └── old.dat
```

Planner:

```
delete old.dat
    ↓
delete nested/
    ↓
delete a.tmp
    ↓
delete b.tmp
    ↓
delete cache/
```

117. Root Target Directory

Target root sendiri tidak otomatis eligible untuk deletion.

118. Target Root Protection

Target root default:

```
Protected
```

119. Why

Cleanup removes:

```
contents
```

not:

```
authorized boundary
```

120. Root Directory

Never delete:

```
TrustedRoot
```

121. Candidate Path

Candidate must be:

```
relative to target root
```

122. Candidate Escape

If candidate resolves outside root:

```
Safety rejection
```

123. Candidate Mount Crossing

If policy forbids:

```
reject
```

124. Candidate Symlink

Symlink candidate may be:

```
removable as directory entry
```

without following target.

125. Symlink Target

Target contents are never recursively scanned through symlink by default.

126. Symlink Age

Symlink metadata can be used if supported.

127. Broken Symlink

Broken symlink is still:

```
symlink
```

not:

```
directory
```

128. Special Files

Default:

```
Protected / NotEligible
```

129. Device Node

Never delete through generic cache cleaner.

130. FIFO

Default:

```
NotEligible
```

131. Socket

Default:

```
NotEligible
```

132. Unknown Object

Default:

```
Unknown
```

133. Why

Special objects can have:

```
runtime semantics
IPC semantics
security implications
```

134. Regular File

Regular files are primary cache candidates.

135. Directory

Directories require:

```
explicit directory semantics
```

136. Symlink

Symlink deletion requires:

```
explicit policy
```

137. Hard Link

Removing candidate entry:

```
does not remove inode elsewhere
```

138. Cache Semantics

Hard link does not grant permission to:

```
search and delete other links
```

139. Candidate Identity

Candidate stores:

```
ObjectIdentity
```

140. ObjectIdentity

Conceptual:

```
filesystem identity
inode identity
object type
```

141. Metadata Snapshot

Candidate captures:

```
size
timestamp
identity
type
```

142. Snapshot

Snapshot is:

```
immutable
```

143. Classification Result

Conceptual:

```
CandidateClassification {
    class
    reason
    confidence
}
```

144. Reason

Examples:

```
AgeTooYoung
ProtectedTarget
UnknownOwnership
PackageRunning
StorageLocked
UnsupportedType
OutsideScope
TimestampUnknown
SizeLimitExceeded
PolicyExcluded
```

145. Confidence

Potential:

```
Certain
Supported
Unknown
```

146. Do Not Convert Unknown to Certain

Unknown evidence must remain:

```
Unknown
```

147. Classification Determinism

Same:

```
Candidate
PlatformSnapshot
Policy inputs
```

must produce:

```
same classification
```

148. No Current Time Dependency Without Input

For deterministic tests:

```
"now" should be explicit input
```

149. Classification Context

Conceptual:

```
ClassificationContext {
    candidate
    target
    package_state
    user_state
    storage_state
    policy
    wall_clock
}
```

150. Pure Function

Ideal:

```
classify(context)
    →
ClassificationResult
```

151. No Side Effect

Classification must not:

```
delete
rename
chmod
chown
modify metadata
```

152. No Arbitrary Filesystem Access

Classifier should operate on:

```
Candidate snapshot
```

153. Additional Metadata

If metadata is missing:

```
classifier may request through controlled read interface
```

154. Metadata Budget

Additional metadata reads must count toward:

```
scan/classification budget
```

155. Budget Exhaustion

Result:

```
Deferred
Unknown
```

not:

```
allow
```

156. Candidate Ordering

Classification should preserve:

```
deterministic candidate identity/order
```

157. Deduplication

Same directory entry discovered twice:

```
deduplicate
```

158. Identity Dedup

Do not globally deduplicate by inode unless semantics explicitly require it.

159. Why

Hard links may represent:

```
separate directory entries
```

160. Directory Entry Semantics

Cleanup operation targets:

```
directory entry
```

161. Cache Eligibility Formula

Conceptual:

```
Eligible =
    RecognizedTarget
    AND
    ValidOwnership
    AND
    ValidStorageScope
    AND
    NotProtected
    AND
    SupportedObjectType
    AND
    MetadataSufficient
    AND
    AgeRuleSatisfied
    AND
    SizeRuleSatisfied
    AND
    PackageStateAllowed
    AND
    RuntimeStateAllowed
    AND
    PolicyAllows
    AND
    SafetyValid
```

162. Unknown Propagation

If critical input is:

```
Unknown
```

then:

```
Eligible = false
```

163. Protected Propagation

If:

```
Protected = true
```

then:

```
Eligible = false
```

164. Safety Precedence

Even if:

```
PolicyAllows = true
```

but:

```
SafetyValid = false
```

result:

```
no operation
```

165. Policy Cannot Override Safety

Invariant:

```
Safety Deny
    >
Policy Allow
```

166. Platform Protection

Invariant:

```
Platform Protected
    >
Policy Allow
```

167. Ownership

Invariant:

```
Unknown Ownership
    >
Allow
```

168. Storage

Invariant:

```
Unknown Storage
    >
Allow
```

169. Timestamp

If age is mandatory:

```
Unknown Timestamp
    >
Allow
```

170. Object Type

Invariant:

```
Unsupported Type
    >
Allow
```

171. Package Running

If policy says:

```
must not cleanup running package
```

then:

```
PackageRunning
    →
Deferred
```

172. Package State Unknown

```
Unknown
   ↓
no destructive operation
```

173. Storage Locked

```
Locked
   ↓
Deferred
```

174. Catalog Degraded

If target identity cannot be trusted:

```
no destructive operation
```

175. Classification + Policy

Classification:

```
semantic facts
```

Policy:

```
decision
```

176. Example

```
Candidate
   │
   ├── TargetClass = AppCache
   ├── Type = RegularFile
   ├── Age = 14 days
   ├── Size = 20 MB
   ├── Ownership = Valid
   ├── Storage = CE
   └── Protected = false
          │
          ▼
     Classification
          │
          ▼
         Cache
          │
          ▼
       Policy
          │
          ▼
       Eligible
```

177. Example — Too Young

```
Candidate
   │
   └── Age = 2 hours
          │
          ▼
   Classification = Cache
          │
          ▼
   Policy minimum_age = 7 days
          │
          ▼
   Excluded: AgeTooYoung
```

178. Example — Unknown Ownership

```
Candidate
   │
   └── ownership = Unknown
          │
          ▼
   Classification = Unknown
          │
          ▼
   Policy
          │
          ▼
   No operation
```

179. Example — Protected

```
Candidate
   │
   └── protected = true
          │
          ▼
   Protected
          │
          ▼
   Policy cannot override
          │
          ▼
   No operation
```

180. Example — Running Package

```
Candidate
   │
   ▼
Cache
   │
   ▼
PackageRunning
   │
   ▼
Deferred
   │
   ▼
Scheduler Retry
```

181. Example — Unknown Timestamp

```
Candidate
   │
   ▼
Cache
   │
   ▼
Timestamp Unknown
   │
   ▼
Policy requires age
   │
   ▼
Excluded / Unknown
```

182. Explainability

Setiap non-eligible candidate sebaiknya memiliki:

```
reason code
```

183. User Explanation

Contoh:

```
AgeTooYoung
"Candidate lebih muda dari minimum cache age."
```

184. Internal Explanation

Dapat memiliki:

```
target id
package id
user id
reason
metadata confidence
```

185. Privacy

User-facing output tidak harus menampilkan:

```
absolute internal path
```

186. Audit

Audit mencatat:

```
classification
policy decision
reason
```

187. Metrics

Metrics:

```
candidates_classified_total
cache_candidates_total
non_cache_candidates_total
protected_candidates_total
unknown_candidates_total
deferred_candidates_total
age_excluded_total
size_excluded_total
ownership_unknown_total
timestamp_unknown_total
package_running_deferred_total
```

188. Budget

Classification budget harus:

```
bounded
```

189. Worst Case

Jangan membuat:

```
one candidate
```

memicu:

```
unlimited metadata lookup
```

190. Cancellation

Classification harus menghormati:

```
CancellationToken
```

191. Cancellation

Jika cancelled:

```
stop classification
```

192. Partial Scan

Partial classification harus dilaporkan sebagai:

```
Cancelled
Partial
```

bukan:

```
Complete
```

193. Error

Classification error:

```
typed
```

194. Error Types

Contoh:

```
MetadataUnavailable
OwnershipUnavailable
PackageStateUnavailable
StorageStateUnavailable
InvalidCandidate
BudgetExceeded
Cancelled
```

195. Error Policy

Critical classification error:

```
no destructive operation
```

196. Retry

Transient metadata failure:

```
dapat Deferred
```

197. Permanent Failure

Unsupported metadata semantics:

```
NotEligible
Unsupported
```

198. Cache Policy Version

Policy semantics harus memiliki:

```
policy version
```

199. Why

Future version dapat mengubah:

```
age interpretation
target semantics
protected rules
```

200. Backward Compatibility

Stored classification result tidak boleh dipakai sebagai:

```
permanent authority
```

201. Reclassification

New job:

```
classify again
```

202. Crash Recovery

Do not resume deletion solely from:

```
old classification
```

203. Generation Binding

Classification context terikat:

```
ConfigGeneration
CatalogGeneration
PlatformGeneration
```

204. Generation Mismatch

Classification result:

```
Stale
```

205. Planner

Planner menerima:

```
PolicyApprovedCandidate
```

bukan:

```
raw Candidate
```

206. Policy Output

Conceptual:

```
PolicyApprovedCandidate {
    candidate
    policy_generation
    reasons
}
```

207. Immutable

Tidak boleh diubah setelah approval.

208. No Planner Override

Planner tidak boleh mengatakan:

```
"policy excluded but planner wants it"
```

209. Authorization

Authorization dapat further restrict:

```
scope
capability
budget
generation
```

210. Executor

Executor tidak menerima:

```
ClassificationResult
```

secara langsung.

Executor menerima:

```
AuthorizedPlan
```

211. Security Chain

```
Candidate
   │
   ▼
Classification
   │
   ▼
Safety
   │
   ▼
Policy
   │
   ▼
Planner
   │
   ▼
Authorization
   │
   ▼
Final Safety
   │
   ▼
Executor
```

212. Formal Non-Bypass Rule

Tidak boleh ada:

```
Candidate
   ─────────────────────────► Executor
```

Tidak boleh ada:

```
Classification
   ─────────────────────────► Executor
```

Tidak boleh ada:

```
Policy
   ─────────────────────────► Executor
```

Hanya:

```
AuthorizedPlan
      │
      ▼
Final Safety Gate
      │
      ▼
   Executor
```

213. Testing

Unit tests:

```
age boundary
timestamp missing
timestamp future
size boundary
ownership mismatch
package states
storage states
protected candidates
object types
```

214. Property Tests

Property:

```
Unknown never becomes Eligible
```

215. Property

Property:

```
Protected never becomes Eligible
```

216. Property

Property:

```
Age below minimum never becomes Eligible
```

217. Property

Property:

```
Ownership mismatch never becomes Eligible
```

218. Property

Property:

```
Unsupported object type never becomes Eligible
```

219. Property

Property:

```
Target root itself never becomes deletion candidate
```

220. Property

Property:

```
Policy exclusion cannot be overridden by Planner
```

221. Property

Property:

```
Safety rejection cannot be overridden by Policy
```

222. Race Tests

Simulate:

```
candidate replaced
package updated
package uninstalled
user locked
mount changed
root replaced
```

223. Expected

All produce:

```
safe rejection
stale
deferred
unknown
```

never:

```
unsafe deletion
```

224. Fuzzing

Fuzz:

```
timestamps
sizes
package state
user state
target classes
candidate metadata
policy expressions
```

225. Overflow

Age/size calculations must use:

```
checked arithmetic
```

226. Time Representation

Prefer:

```
Duration
SystemTime
```

with explicit conversion rules.

227. Serialization

Classification DTO must be:

```
versioned
```

228. Do Not Serialize Raw Internal Types

IPC should expose:

```
reason code
status
safe summary
```

229. Result Retention

Historical classification:

```
informational
```

not:

```
authorization authority
```

230. Definition of Done

56.md selesai jika:

```
[x] Cache semantic definition didefinisikan
[x] Target vs Candidate didefinisikan
[x] Classification didefinisikan
[x] Cache/NotCache/Protected/Unknown/Deferred didefinisikan
[x] Unknown-safe behavior didefinisikan
[x] AppCache didefinisikan
[x] CodeCache didefinisikan
[x] Temporary semantics didefinisikan
[x] Age semantics didefinisikan
[x] Timestamp reliability didefinisikan
[x] Size semantics didefinisikan
[x] Ownership validation didefinisikan
[x] Package state didefinisikan
[x] Storage state didefinisikan
[x] Protected candidate didefinisikan
[x] Active/running package behavior didefinisikan
[x] Directory deletion semantics didefinisikan
[x] Symlink/special file semantics didefinisikan
[x] Hard-link semantics didefinisikan
[x] Unknown metadata handling didefinisikan
[x] Deterministic classification didefinisikan
[x] Policy separation didefinisikan
[x] Generation binding didefinisikan
[x] Explainability didefinisikan
[x] Budget/cancellation didefinisikan
[x] Property/fuzz/race testing didefinisikan
[x] Security invariants didefinisikan
```

231. Arah Berikutnya

Sekarang kita sudah memiliki:

```
54.md
    Filesystem Primitive
        │
        ▼
55.md
    Android Target Discovery
        │
        ▼
56.md
    Cache Classification
        │
        ▼
    Policy
        │
        ▼
    Planner
        │
        ▼
    Authorization
        │
        ▼
    Executor
```

Masalah berikutnya adalah bagaimana Planner mengubah banyak candidate menjadi operasi deletion yang benar-benar aman, terutama ketika terdapat:

```
nested directories
parent/child dependency
duplicate candidates
hard links
symlink
directory becoming non-empty
operation budget
byte budget
partial plan
deterministic ordering
```

Fokus 57.md:

```
57.md — Cache Cleanup Planning Semantics,
       Candidate-to-Operation Mapping,
       File vs Directory Deletion,
       Nested Candidate Graph,
       Parent/Child Dependency,
       Operation Deduplication,
       Conflict Detection,
       Hard-Link Handling,
       Symlink Handling,
       Empty Directory Planning,
       Byte/Operation Budget,
       Partial Plan,
       Deterministic Ordering,
       Plan Explainability,
       Plan Immutability,
       dan Cache Cleanup Plan Contract
```
