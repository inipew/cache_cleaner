53.md — Platform Abstraction Contract, Android/Linux Capability Discovery, Storage Discovery, Package/User Discovery, Mount Discovery, Kernel Feature Detection, Security Observation, Platform Snapshot, Platform Generation, Adapter Isolation, Capability Catalog, and Platform Consistency Contract

Tujuan

Dokumen ini mengunci boundary antara domain engine dengan Android/Linux.

Domain tidak boleh mengetahui detail:

```
Android framework API
PackageManager
/proc
/sys
/dev
mount table
kernel ioctl
SELinux implementation
Android user storage layout
kernel version quirks
```

Semua informasi tersebut harus diterjemahkan oleh:

```
Platform Adapter
Kernel Adapter
Storage Adapter
Package Adapter
User Adapter
```

menjadi:

```
typed domain information
```

Arsitektur:

```
Android / Linux
      │
      ├── Package Manager
      ├── User Manager
      ├── Mount Table
      ├── Filesystems
      ├── Block Devices
      ├── Kernel Interfaces
      └── Security State
                │
                ▼
         Platform Adapters
                │
                ▼
         Capability Discovery
                │
                ▼
         PlatformSnapshot
                │
                ▼
          Target Catalog
                │
                ▼
   Safety / Policy / Planner
                │
                ▼
           Executor
```

1. Tujuan Platform Layer

Platform layer menyediakan:

```
discovery
observation
capability detection
identity mapping
readiness information
platform-specific execution adapters
```

2. Platform Layer Bukan Policy

Platform layer tidak menentukan:

```
"cache ini boleh dihapus"
```

3. Platform Layer Bukan Authorization

Platform layer tidak menentukan:

```
"caller ini boleh menjalankan TRIM"
```

4. Platform Layer Bukan Scheduler

Platform layer tidak menentukan:

```
"job dijalankan sekarang"
```

5. Platform Layer Sebagai Translation Boundary

```
Android/Linux reality
      │
      ▼
typed platform model
```

6. Domain Tidak Memanggil Android API

Forbidden:

```
domain -> Android API
```

7. Domain Tidak Membaca Raw /proc

Forbidden:

```
domain -> /proc
```

8. Domain Tidak Membaca Raw /sys

Forbidden:

```
domain -> /sys
```

9. Domain Tidak Mengakses Raw /dev

Forbidden:

```
domain -> /dev
```

10. Adapter

Adapter menerjemahkan:

```
platform-specific representation
    │
    ▼
domain-neutral representation
```

11. PlatformSnapshot

Snapshot adalah immutable representation dari kondisi platform pada suatu waktu.

Konseptual:

```
PlatformSnapshot {
    generation
    timestamp
    users
    packages
    mounts
    filesystems
    devices
    capabilities
    readiness
    security_observations
}
```

12. Snapshot Bukan Live View

Snapshot tidak menjamin kondisi platform tidak berubah.

13. Snapshot Adalah Assumption Boundary

Snapshot menjawab:

```
"Apa yang diketahui sistem ketika snapshot dibuat?"
```

14. Generation

Setiap snapshot memiliki:

```
PlatformGeneration
```

15. Generation Change

Jika platform berubah secara material:

```
new generation
```

16. Generation Monotonic

Generation harus:

```
monotonically increasing
```

17. Generation Tidak Harus Wall Clock

Gunakan:

```
typed generation counter
```

18. Snapshot Immutability

Setelah published:

```
snapshot tidak dimutasi.
```

19. Refresh

```
Current Snapshot
      │
      ▼
Platform Discovery
      │
      ▼
Validate
      │
      ▼
New Snapshot
      │
      ▼
Atomic Publish
```

20. Failed Refresh

Jika discovery gagal:

```
retain previous valid snapshot
```

atau:

```
mark platform state degraded
```

21. Jangan Publish Partial Snapshot

Contoh buruk:

```
users = new
packages = old
mounts = failed
```

tanpa consistency semantics.

22. Snapshot Consistency

Semua fields harus memiliki:

```
observation context
```

23. Observation Context

Contoh:

```
discovery_start
discovery_end
source generations
```

24. Cross-Source Inconsistency

Package Manager dapat berubah ketika mount discovery berlangsung.

Maka snapshot harus memiliki:

```
explicit consistency model
```

25. Strong Consistency

Jika platform dapat menyediakan atomic snapshot:

```
gunakan.
```

26. Practical Android/Linux

Sering kali tidak ada atomic snapshot lintas:

```
package
mount
user
kernel
```

27. Maka

Gunakan:

```
bounded discovery window
identity validation
generation comparison
retry on inconsistency
```

28. Discovery Retry

Jika sumber berubah selama discovery:

```
retry boundedly
```

29. Retry Limit

Jangan:

```
retry forever
```

30. Discovery Budget

Gunakan:

```
max discovery duration
max retries
max records
```

31. Degraded Snapshot

Jika consistency tidak dapat dibuktikan:

```
degraded
```

32. Destructive Impact

Degraded snapshot dapat menyebabkan:

```
destructive jobs blocked/deferred
```

33. Read-Only Impact

Status/diagnostic masih dapat menggunakan:

```
partial observations
```

34. Package Discovery

Package adapter menyediakan:

```
PackageId
package name
installation identity
user scope
storage mapping
package state
```

35. Package Name

Package name:

```
human/platform identifier
```

36. PackageId

Typed:

```
PackageId
```

37. Installation Identity

Package reinstall dapat menghasilkan:

```
new installation identity
```

38. Why

Package name sama:

```
com.example.app
```

tetapi filesystem state dapat berbeda.

39. Package Generation

Package changes dapat increment:

```
package/catalog generation
```

40. Package Events

Contoh:

```
installed
uninstalled
replaced
data cleared
storage moved
package disabled/enabled
```

41. User Discovery

User adapter menyediakan:

```
UserId
user state
unlock state
availability
```

42. User States

Contoh:

```
Unknown
Stopped
Running
Locked
Unlocked
Removing
Removed
```

43. Unknown User State

Conservative behavior:

```
no destructive access
```

44. CE Storage

Credential Encrypted storage memerlukan:

```
appropriate unlock state
```

45. DE Storage

Device Encrypted storage memiliki lifecycle berbeda.

46. No Unlock Bypass

Platform layer tidak boleh menyediakan:

```
"force access locked CE"
```

47. User Mapping

Target harus memiliki:

```
UserId
```

untuk mencegah:

```
cross-user confusion
```

48. Package/User Relation

Package dapat exist untuk:

```
multiple users
```

49. Identity

Jangan menyamakan:

```
PackageId
UserId
```

50. Storage Discovery

Storage adapter menyediakan:

```
filesystem identity
mount identity
device identity
mount point
filesystem type
read/write state
availability
```

51. Filesystem Identity

Identity dapat mencakup:

```
filesystem UUID
device identity
statfs information
mount identity
```

52. Path

Mount path adalah:

```
location
```

bukan:

```
permanent identity
```

53. Mount Identity

Mount instance dapat berubah walaupun:

```
mount path sama
```

54. Device Identity

Block device identity harus typed.

55. Filesystem Type

Typed:

```
Ext4
F2fs
Tmpfs
Other
Unknown
```

56. Unknown Filesystem

Unknown filesystem:

```
conservative behavior
```

57. Mount State

Contoh:

```
Mounted
Unmounted
ReadOnly
NoExec
NoSuid
Unknown
```

58. ReadOnly

Destructive operations:

```
denied/deferred
```

59. Mount Boundary

Target cannot silently cross:

```
filesystem/mount boundary
```

60. Bind Mount

Bind mount requires explicit identity handling.

Path similarity tidak cukup.

61. External Storage

External/shared storage must not automatically become:

```
app cache target
```

62. Storage Class

Typed:

```
Internal
CredentialEncrypted
DeviceEncrypted
Shared
External
Temporary
Unknown
```

63. Target Mapping

Platform adapter produces:

```
StorageTargetDescriptor
```

64. Target Descriptor

Contains:

```
TargetId
PackageId
UserId
StorageClass
TrustedRoot
filesystem identity
mount identity
availability
```

65. Target Catalog

Catalog builder consumes:

```
PlatformSnapshot
```

66. Platform Snapshot → Catalog

```
PlatformSnapshot
      │
      ▼
CatalogBuilder
      │
      ▼
CatalogSnapshot
```

67. Catalog Owns Target Semantics

Platform snapshot says:

```
"what exists"
```

Catalog says:

```
"what can be represented as a cleanup target"
```

68. Capability Discovery

Capability catalog answers:

```
"What operations does this environment support?"
```

69. Capability ≠ Policy

Example:

```
TRIM supported
```

does not mean:

```
TRIM allowed.
```

70. Capability Categories

```
Filesystem
Block
Kernel
Memory
Platform
Security
```

71. Filesystem Capability

Examples:

```
F2FS maintenance
filesystem stat
mount observation
```

72. Block Capability

Examples:

```
discard/TRIM
```

73. Kernel Capability

Examples:

```
specific ioctl/interface
```

74. Memory Capability

Examples:

```
ZRAM operation
```

75. Platform Capability

Examples:

```
package discovery
user state discovery
```

76. Security Capability

Examples:

```
ability to observe required security state
```

77. Capability Descriptor

Conceptual:

```
CapabilityDescriptor {
    id
    supported
    version
    scope
    constraints
    evidence
}
```

78. Evidence

Capability should include:

```
detection source
```

79. Example

```
Capability::Trim
supported = true
evidence = BlockDeviceInterface
```

80. Capability Scope

Capability may apply only to:

```
specific device
filesystem
kernel
platform version
```

81. Global Capability

Some capabilities are global.

82. Scoped Capability

TRIM support may be:

```
device-specific
```

83. Never Generalize

If:

```
device A supports TRIM
```

do not assume:

```
device B supports TRIM
```

84. Capability Refresh

Capabilities must refresh when:

```
kernel changes
device changes
mount changes
platform changes
```

85. Kernel Version

Kernel version is observational metadata.

86. Kernel Feature Detection

Preferred:

```
actual interface probing
```

when safe and read-only.

87. No Destructive Probe

Capability detection must not mutate system.

88. Unsupported Interface

Return:

```
Unsupported
```

89. Permission Failure

Distinguish:

```
Unsupported
```

from:

```
PermissionDenied
```

90. Detection Error

If capability cannot be reliably detected:

```
Unknown
```

91. Unknown Capability

Policy should not treat:

```
Unknown
```

as:

```
Supported
```

92. Capability State

```
Supported
Unsupported
Unknown
Restricted
```

93. Restricted

Interface exists but:

```
permission/scope prevents use.
```

94. SELinux

Platform layer may observe security state where useful.

95. SELinux Is Not a Bypass

Never attempt:

```
disable SELinux
modify enforcement
change context to bypass safety
```

96. Security Context

Security observation may provide:

```
current execution context
access constraints
```

97. Permission Model

Actual permission should be tested at:

```
operation boundary
```

98. Why

Permission may change after discovery.

99. Discovery vs Execution

Discovery:

```
"looks writable"
```

Execution:

```
"is writable now"
```

100. Final Check

Executor remains authoritative for:

```
final permission
target identity
operation preconditions
```

101. Platform Readiness

PlatformSnapshot may include:

```
screen state
idle state
thermal state
memory pressure
storage availability
user state
```

102. Readiness Snapshot

Readiness is:

```
time-sensitive
```

103. Readiness Expiration

A readiness snapshot should not be considered permanently valid.

104. Runtime Events

Events can invalidate platform snapshot:

```
UserChanged
MountChanged
PackageChanged
ThermalChanged
MemoryChanged
PermissionChanged
```

105. Event Handling

```
Event
  │
  ▼
Invalidate
  │
  ▼
Refresh
  │
  ▼
New Snapshot
```

106. Event Overflow

If event source overflows:

```
do full reconciliation
```

107. Event Source Failure

If source fails:

```
periodic reconciliation
```

108. Reconciliation

Reconciliation compares:

```
current observed platform
```

with:

```
previous snapshot
```

109. Diff

Conceptual:

```
PlatformSnapshot A
      │
      ▼
   Diff
      │
      ▼
PlatformSnapshot B
```

110. Diff Categories

```
UserAdded
UserRemoved
PackageChanged
MountAdded
MountRemoved
DeviceChanged
CapabilityChanged
SecurityChanged
```

111. Material Change

Not every observed change requires:

```
generation increment
```

112. Generation Policy

Generation increments when assumptions relevant to safety/planning change.

113. Example

Log rotation:

```
no catalog generation change
```

114. Mount Change

Usually:

```
platform/catalog generation change
```

115. Package Replacement

Usually:

```
package/catalog generation change
```

116. User Unlock

Potentially:

```
catalog/platform generation change
```

117. Thermal Change

May change:

```
readiness generation
```

rather than catalog generation.

118. Separate Generations

Possible:

```
PlatformGeneration
CatalogGeneration
ReadinessGeneration
```

119. Why

Avoid invalidating everything for:

```
temporary thermal event
```

120. Generation Relationship

```
Platform
   │
   ├── Catalog
   └── Readiness
```

121. Catalog Generation

Changes when target mapping changes.

122. Readiness Generation

Changes when runtime conditions relevant to execution change.

123. Config Generation

Independent.

```
Config
   │
   ▼
Policy assumptions
```

124. Cross-Generation Validation

AuthorizedPlan may bind:

```
ConfigGeneration
CatalogGeneration
PlatformGeneration
ReadinessGeneration
```

125. Generation Mismatch

Result:

```
stale/deferred/replan
```

depending on which generation changed.

126. Platform Snapshot Lifecycle

```
Discover
```

```
  │
  ▼
Validate
  │
  ▼
Normalize
  │
  ▼
Publish
  │
  ▼
Consume
  │
  ▼
Invalidate
  │
  ▼
Refresh
```

127. Normalization

Platform-specific values converted into:

```
typed enums
typed IDs
bounded strings
validated paths
```

128. Raw String Restriction

Raw platform strings must not leak into:

```
authorization decisions
```

without validation.

129. Package Name Validation

Package names should be:

```
syntax validated
bounded
```

130. User ID Validation

Reject:

```
negative
impossible
out-of-range
```

131. Device ID Validation

Reject malformed:

```
major/minor
```

132. Filesystem Type Parsing

Unknown values:

```
Unknown
```

133. Mount Flags

Map to:

```
typed flags
```

134. Security State

Unknown security state:

```
conservative
```

135. Platform Clock

Snapshot may contain:

```
wall-clock timestamp
```

but ordering uses:

```
monotonic runtime sequence
```

136. Observation Time

Each snapshot has:

```
observed_at
```

137. Expiration

Snapshot may define:

```
validity duration
```

138. Expiration Is Conservative

Expired snapshot:

```
cannot authorize destructive operation
```

139. Read-Only Consumers

Expired snapshot may still be used for:

```
diagnostics
```

140. Platform Adapter Failure

Failure should be typed:

```
DiscoveryFailed
PermissionDenied
Unsupported
ParseError
Inconsistent
Timeout
```

141. Adapter Failure

Adapter must not panic because platform returned unexpected data.

142. Malformed Platform Data

Treat as:

```
invalid observation
```

143. Parser Safety

Platform parser:

```
bounded
fuzzable
no side effects
```

144. Package Manager Output

If external command/API is used:

```
bounded
structured
validated
```

145. Shell Output

Never parse untrusted shell output as authority.

146. Preferred Interface

Use:

```
direct API
structured system interface
```

instead of:

```
shell command
```

147. If Command Required

Command execution must be:

```
fixed executable
fixed arguments
bounded output
validated exit status
```

148. No User-Supplied Command

Never allow:

```
command path
arguments
```

to originate directly from IPC/config.

149. Environment

Adapter should use controlled environment.

Avoid dependence on:

```
PATH
arbitrary environment variables
```

150. Locale

Parsing should not depend on:

```
locale-specific output
```

151. Numeric Parsing

Use:

```
strict parser
```

152. Unexpected Data

Unexpected data:

```
reject observation
```

153. Partial Observation

Partial observation must be explicit.

Example:

```
packages = available
mounts = unavailable
```

154. Partial Snapshot

Partial snapshot may be used for:

```
status
```

but not necessarily:

```
destructive planning
```

155. Snapshot Quality

Conceptual:

```
Complete
Partial
Degraded
Invalid
```

156. Invalid Snapshot

Never publish as current snapshot.

157. Degraded Snapshot

Publish only if consumers understand degradation semantics.

158. Catalog Requirement

Catalog building may require:

```
Complete relevant storage data
```

159. Planner Requirement

Planner may require:

```
Complete target identity
```

160. Authorization Requirement

Authorization requires:

```
current enough snapshot
```

161. Executor Requirement

Executor performs:

```
final direct observation
```

162. Platform State Drift

Even a fresh snapshot can become stale between:

```
observation
execution
```

163. Final Gate

Therefore:

```
PlatformSnapshot
      │
      ▼
Plan
      │
      ▼
Authorization
      │
      ▼
Final Platform Check
      │
      ▼
Execute
```

164. Platform Check

Final check validates:

```
target availability
capability
permission
readiness
identity
```

165. No Snapshot Trust Blindly

Snapshot is:

```
evidence
```

not:

```
permanent truth
```

166. Platform Adapter Isolation

Suggested:

```
src/platform/
├── mod.rs
├── snapshot.rs
├── capability.rs
├── package.rs
├── user.rs
├── storage.rs
├── mount.rs
├── security.rs
└── readiness.rs
```

167. Kernel Layer

```
src/kernel/
├── mod.rs
├── block.rs
├── filesystem.rs
├── f2fs.rs
├── zram.rs
├── sysfs.rs
└── procfs.rs
```

168. Adapter Interfaces

Conceptual:

```
PackageProvider
UserProvider
StorageProvider
MountProvider
CapabilityProvider
SecurityProvider
ReadinessProvider
```

169. Provider Contract

Provider:

```
observe only
```

unless explicitly designated as:

```
execution adapter
```

170. Read Provider vs Mutation Adapter

Separate:

```
discovery interfaces
```

from:

```
mutation interfaces
```

171. Example

```
BlockDeviceProvider
    ≠
BlockMaintenanceExecutor
```

172. Why

Prevents:

```
discovery code
```

from gaining:

```
mutation capability
```

173. Dependency Direction

```
domain
  ▲
  │
platform interface
  ▲
  │
platform implementation
```

174. Reverse Dependency Forbidden

Platform implementation must not call:

```
PolicyEngine
Scheduler
CLI
```

175. Platform Adapter Testing

Use:

```
fake provider
fixture
parser fuzzing
snapshot fixture
```

176. Real Device Tests

Integration tests should be:

```
explicit
isolated
read-only by default
```

177. Mutation Tests

Mutation tests belong to:

```
maintenance/kernel integration suite
```

178. Snapshot Tests

Test:

```
deterministic normalization
generation changes
partial data
malformed records
```

179. Race Tests

Simulate:

```
package replaced during discovery
mount changed during discovery
user locked during planning
device removed before execution
```

180. Expected

System produces:

```
Stale
Deferred
Retry
Unknown
```

rather than:

```
unsafe execution
```

181. Fuzzing

Fuzz:

```
package parser
mount parser
device parser
capability parser
security state parser
snapshot deserializer
```

182. No Mutation During Fuzz

Fuzz target must not access real mutation interfaces.

183. Metrics

Track:

```
platform_refresh_total
platform_refresh_failures
platform_generation_changes
catalog_invalidations
capability_unknown_total
partial_snapshot_total
reconciliation_total
adapter_parse_errors
```

184. Logging

Log:

```
provider
event
generation
failure category
```

Avoid sensitive raw platform output.

185. Explainability

System should explain:

```
"Target tidak tersedia karena user masih locked."
```

atau:

```
"TRIM tidak tersedia pada device tersebut."
```

186. Stable Reason Codes

Examples:

```
PLATFORM_DISCOVERY_FAILED
PLATFORM_SNAPSHOT_DEGRADED
TARGET_UNAVAILABLE
USER_LOCKED
MOUNT_CHANGED
DEVICE_CHANGED
CAPABILITY_UNKNOWN
CAPABILITY_UNSUPPORTED
PERMISSION_RESTRICTED
```

187. Security Invariants

Invariant 1:

```
Domain tidak mengetahui Android/Linux implementation detail.
```

Invariant 2:

```
Platform snapshot tidak memberikan authorization.
```

Invariant 3:

```
Capability tidak memberikan authorization.
```

Invariant 4:

```
Unknown capability tidak dianggap supported.
```

Invariant 5:

```
Unknown user state tidak dianggap safe.
```

Invariant 6:

```
Partial snapshot tidak boleh dipakai untuk destructive operation jika data penting hilang.
```

Invariant 7:

```
Mount identity harus diverifikasi.
```

Invariant 8:

```
Device identity harus diverifikasi.
```

Invariant 9:

```
Package identity harus dibedakan dari package name.
```

Invariant 10:

```
User identity harus typed.
```

Invariant 11:

```
Platform-specific strings harus dinormalisasi sebelum masuk domain.
```

Invariant 12:

```
Platform event hanya invalidation hint.
```

Invariant 13:

```
Event overflow memicu reconciliation.
```

Invariant 14:

```
Capability detection tidak boleh melakukan destructive probe.
```

Invariant 15:

```
Raw sysfs/procfs access tidak boleh bocor ke domain.
```

Invariant 16:

```
Shell command tidak boleh menjadi generic platform interface.
```

Invariant 17:

```
Snapshot expiry mencegah destructive execution.
```

Invariant 18:

```
Final execution melakukan revalidation.
```

Invariant 19:

```
Platform refresh tidak boleh publish partial state sebagai complete state.
```

Invariant 20:

```
Platform failure tidak boleh membuka safety bypass.
```

188. Definition of Done

53.md selesai jika:

```
[x] Platform boundary didefinisikan
[x] PlatformSnapshot didefinisikan
[x] PlatformGeneration didefinisikan
[x] Snapshot consistency didefinisikan
[x] Package discovery didefinisikan
[x] User discovery didefinisikan
[x] Storage discovery didefinisikan
[x] Mount discovery didefinisikan
[x] Device identity didefinisikan
[x] Filesystem identity didefinisikan
[x] Capability catalog didefinisikan
[x] Kernel feature detection didefinisikan
[x] SELinux/security observation didefinisikan
[x] Readiness integration didefinisikan
[x] Partial/degraded snapshot didefinisikan
[x] Event/reconciliation didefinisikan
[x] Adapter isolation didefinisikan
[x] Provider contracts didefinisikan
[x] Shell dependency restriction didefinisikan
[x] Fuzzing didefinisikan
[x] Race handling didefinisikan
[x] Security invariants didefinisikan
```

189. Arah Berikutnya

Platform abstraction sekarang sudah memiliki:

```
discovery
identity
capability
snapshot
generation
invalidation
```

Lapisan berikutnya adalah mengunci bagaimana filesystem Linux/Android benar-benar diakses secara aman oleh Scanner, Safety Layer, dan Executor.

Fokus 54.md:

```
54.md — Linux Filesystem Abstraction,
       FD-relative API,
       openat/openat2,
       fstatat,
       unlinkat,
       directory traversal,
       symlink handling,
       mount boundary,
       file identity,
       inode/device validation,
       filesystem capability,
       race-safe resolution,
       descriptor ownership,
       syscall abstraction,
       error mapping,
       dan Secure Filesystem Primitive Contract
```

Alur:

```
Scanner
   │
   ▼
Filesystem Interface
   │
   ▼
TrustedRoot
   │
   ▼
FD-relative Resolution
   │
   ├── openat/openat2
   ├── fstatat
   ├── readdir
   └── unlinkat
   │
   ▼
Identity Revalidation
   │
   ▼
Authorized Mutation
   │
   ▼
Verifier
```
