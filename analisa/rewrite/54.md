54.md — Linux Filesystem Abstraction, FD-relative API, openat/openat2, fstatat, unlinkat, Directory Traversal, Symlink Handling, Mount Boundary, File Identity, Inode/Device Validation, Race-safe Resolution, Descriptor Ownership, Syscall Abstraction, Error Mapping, and Secure Filesystem Primitive Contract

Tujuan

Dokumen ini mengunci abstraction filesystem Linux/Android yang digunakan oleh:

```
Scanner
Safety Layer
Executor
Verifier
Platform Adapter
```

Filesystem abstraction adalah boundary terakhir sebelum:

```
kernel syscall
```

Prinsip utama:

```
Domain
  │
  ▼
Filesystem Interface
  │
  ▼
Safety / Executor
  │
  ▼
FD-relative Primitive
  │
  ▼
Linux Kernel
```

Tidak boleh ada jalur:

```
Domain
  │
  ▼
arbitrary filesystem path
  │
  ▼
syscall
```

1. Filesystem Abstraction

Filesystem abstraction menyediakan operasi yang dibutuhkan sistem tanpa membocorkan:

```
raw syscall details
libc details
errno representation
arbitrary path APIs
```

2. Filesystem Interface

Konseptual:

```
Filesystem {
    open_root
    read_dir
    stat
    open_child
    remove_entry
    filesystem_identity
    mount_identity
}
```

3. Read vs Mutation

Interface harus memisahkan:

```
read-only operations
```

dari:

```
mutation operations
```

4. Read-only Operations

Contoh:

```
open_root
read_dir
stat
filesystem_info
```

5. Mutation Operations

Contoh:

```
unlink
remove_empty_directory
```

6. No Generic Remove

Forbidden:

```
remove(path)
```

7. Typed Mutation

Gunakan:

```
RemoveFile
RemoveEmptyDirectory
```

8. Why

Typed operation mencegah:

```
file/dir confusion
```

9. Path Model

Filesystem access menggunakan:

```
TrustedRoot
RelativePath
PathComponent
```

10. TrustedRoot

TrustedRoot merupakan:

```
authorized filesystem boundary
```

11. Root Identity

TrustedRoot menyimpan identity yang harus diverifikasi kembali.

12. Root FD

Jika memungkinkan:

```
TrustedRoot
```

direpresentasikan oleh:

```
owned directory FD
```

13. FD Lifetime

Root FD hidup selama:

```
operation/scan context
```

14. No Raw FD Sharing

FD ownership harus eksplisit.

15. Descriptor Ownership

Konseptual:

```
OwnedFd
    │
    ├── Scanner
    ├── Safety
    └── Executor
```

Ownership tidak boleh:

```
implicit
```

16. Borrowed FD

Jika API membutuhkan borrow:

```
lifetime harus jelas
```

17. Close

FD harus ditutup secara deterministic.

18. FD Leak

FD leak dianggap:

```
correctness bug
```

19. Root Opening

Root opening harus:

```
validate type
validate permissions
capture identity
capture filesystem identity
capture mount identity
```

20. Root Must Be Directory

Jika root bukan directory:

```
reject
```

21. Root Symlink

TrustedRoot tidak boleh diam-diam menjadi:

```
symlink
```

22. Root Mount

Mount identity harus dicapture.

23. Root Filesystem

Filesystem identity harus dicapture.

24. Root Revalidation

Sebelum destructive operation:

```
current root
    == expected root
```

25. RelativePath

RelativePath tidak boleh mengandung:

```
absolute root
empty semantic component
".."
```

26. Path Component

PathComponent tidak boleh:

```
"."
".."
"/"
NUL
```

27. NUL

NUL harus:

```
rejected
```

28. Empty Component

Repeated separators harus dinormalisasi atau ditolak sesuai contract.

Security-critical path resolution sebaiknya:

```
reject ambiguous representation
```

29. Absolute Path

Forbidden:

```
/data/...
```

di executor API.

30. Parent Traversal

Forbidden:

```
../
```

31. Prefix Confusion

String prefix tidak boleh digunakan sebagai boundary proof.

Contoh:

```
/safe/root2
```

tidak otomatis berada di:

```
/safe/root
```

32. FD-relative Resolution

Path resolution:

```
root_fd
   │
   ▼
component
   │
   ▼
child_fd
   │
   ▼
component
   │
   ▼
target
```

33. Why FD-relative

Mengurangi race antara:

```
check path
use path
```

34. openat

openat memungkinkan:

```
relative lookup
```

berdasarkan:

```
directory FD
```

35. openat2

Jika tersedia, openat2 dapat memberikan stronger resolution constraints.

36. openat2 Hardening

Potential restrictions:

```
RESOLVE_BENEATH
RESOLVE_NO_SYMLINKS
RESOLVE_NO_XDEV
```

Actual flags depend on required semantics and platform support.

37. Compatibility

Android/Linux environments mungkin berbeda.

Filesystem abstraction harus:

```
detect capability
```

38. Fallback

Jika openat2 tidak tersedia:

```
use carefully validated FD-relative primitives
```

39. No Unsafe Fallback

Fallback tidak boleh berubah menjadi:

```
arbitrary absolute path traversal
```

40. Symlink Policy

Default:

```
do not follow symlinks
```

41. Symlink Detection

Use:

```
lstat/fstatat with no-follow semantics
```

42. Symlink Target

Scanner dapat record:

```
ObjectType::Symlink
```

43. Deletion

Deleting symlink means:

```
remove directory entry
```

bukan:

```
follow target
```

44. Symlink Race

Sequence:

```
inspect
   │
   ▼
attacker replaces
   │
   ▼
execute
```

must result in:

```
safe rejection
or
operation against validated entry
```

45. Hard Link

Hard link can make same inode reachable elsewhere.

46. Hard Link Semantics

Deleting one directory entry:

```
does not imply deleting inode globally.
```

47. Safety Requirement

Safety concerns:

```
intended directory entry
```

not:

```
global inode deletion
```

48. File Identity

Identity may include:

```
st_dev
st_ino
object type
```

49. Optional Identity Fields

Depending on platform:

```
generation
mount ID
birth metadata
```

50. Identity Snapshot

Candidate stores:

```
expected identity
```

51. Revalidation

Before deletion:

```
current identity
    ==
expected identity
```

52. Changed Identity

Mismatch:

```
ChangedSinceScan
```

53. Object Type

Typed:

```
RegularFile
Directory
Symlink
Special
Other
```

54. Type Validation

Expected:

```
RegularFile
```

Current:

```
Directory
```

Result:

```
TypeChanged
```

55. Directory Safety

Removing directory requires:

```
explicit empty-directory semantics
```

56. No Recursive Remove

Forbidden primitive:

```
recursive_remove(path)
```

57. Why

Recursive removal creates:

```
huge race surface
scope ambiguity
unexpected traversal
```

58. Child-before-Parent

Planner may generate:

```
child deletion
    ↓
directory deletion
```

59. Executor

Executor only removes directory when:

```
currently empty
identity matches
```

60. Directory Becomes Non-empty

Result:

```
NotEmpty
```

or:

```
Changed
```

61. Mount Boundary

Traversal must respect:

```
mount boundary policy
```

62. Cross-filesystem

Default:

```
no cross-filesystem traversal
```

63. Device Check

Compare:

```
st_dev
```

where appropriate.

64. Mount ID

If available:

```
mount identity
```

should be checked too.

65. Bind Mount

Bind mount may create:

```
same filesystem
different mount context
```

Therefore:

```
st_dev alone may be insufficient
```

66. Mount-aware Validation

Use:

```
mount identity
```

where platform supports it.

67. Unknown Mount Identity

If policy requires mount identity but cannot determine it:

```
conservative failure
```

68. Filesystem Identity

Filesystem identity should include enough data to detect:

```
root replacement
mount replacement
```

69. statfs

statfs can provide filesystem-level information.

70. statfs Limitation

statfs alone does not prove:

```
target path identity
```

71. Combined Identity

Use:

```
root identity
filesystem identity
mount identity
object identity
```

72. Root + Relative Path

Security proof:

```
trusted root
  +
validated relative path
  +
identity revalidation
```

73. Revalidation Timing

As close as possible to mutation.

74. TOCTOU

The architecture cannot claim:

```
zero race
```

75. Goal

Minimize race window and fail safely when identity changes.

76. Atomic Kernel Operation

Where kernel provides atomic directory-entry operation:

```
prefer it
```

77. unlinkat

unlinkat operates relative to:

```
directory FD
```

78. unlinkat Directory

Directory removal can use:

```
AT_REMOVEDIR
```

79. File Removal

Regular file/symlink removal:

```
unlinkat without AT_REMOVEDIR
```

80. Type Confusion

If expected file but kernel reports directory:

```
reject
```

81. errno Mapping

Raw errno must not leak into domain.

82. Error Type

Examples:

```
AlreadyGone
PermissionDenied
NotEmpty
TypeChanged
ChangedSinceScan
CrossFilesystem
InvalidPath
IoFailure
ResourceBusy
Stale
```

83. ENOENT

Could map to:

```
AlreadyGone
```

84. EACCES/EPERM

Map:

```
PermissionDenied
```

85. ENOTEMPTY

Map:

```
NotEmpty
```

86. EISDIR

Map according to expected operation:

```
TypeChanged
```

87. ENOTDIR

Map:

```
TypeChanged
or
InvalidPath
```

88. EXDEV

Potential:

```
CrossFilesystem
```

89. EBUSY

Potential:

```
ResourceBusy
```

90. EINTR

Must be interpreted carefully.

Do not blindly retry destructive operation unless operation semantics guarantee safe retry.

91. Unknown Syscall Outcome

If syscall interruption creates uncertainty:

```
UnknownOutcome
```

92. AlreadyGone

If target disappeared before mutation:

```
operation can be idempotently satisfied
```

93. Idempotency

Removal of an already-absent directory entry is:

```
semantically idempotent
```

but audit should distinguish:

```
AlreadySatisfied
```

from:

```
Executed
```

94. Changed Since Scan

Target exists but identity differs:

```
no mutation
```

95. Type Changed

Target type differs:

```
no mutation
```

96. Parent Changed

Parent directory identity differs:

```
stale
```

97. Root Changed

Trusted root identity differs:

```
stale
```

98. Mount Changed

Mount identity differs:

```
stale
```

99. Permission Changed

Current permission insufficient:

```
PermissionDenied
```

100. Race-safe Flow

```
Candidate
   │
   ▼
SafetyValidated
   │
   ▼
PolicyApproved
   │
   ▼
PlannedOperation
   │
   ▼
AuthorizedPlan
   │
   ▼
Resolve Parent FD
   │
   ▼
Validate Root
   │
   ▼
Validate Parent Identity
   │
   ▼
Validate Target Identity
   │
   ▼
Validate Type
   │
   ▼
Validate Mount/Filesystem
   │
   ▼
unlinkat()
   │
   ▼
Verify
   │
   ▼
Result
```

101. Parent FD

Executor should resolve parent directory as:

```
owned FD
```

102. Why Parent FD

Target path resolution through an already validated parent reduces:

```
namespace race
```

103. Parent Identity

Parent directory itself must be checked.

104. Parent Replacement

If parent replaced:

```
ChangedSinceScan
```

105. Child Replacement

If target entry replaced:

```
ChangedSinceScan
```

106. Rename

Rename can invalidate assumptions.

Verifier should detect:

```
target absent
identity elsewhere
```

107. Cross-directory Rename

Operation scoped to:

```
intended directory entry
```

108. No Search-for-Inode Recovery

Do not search entire filesystem for same inode and delete another path.

109. Why

That would violate:

```
path-entry scope
```

110. File Creation Race

Expected:

```
file A
```

Attacker:

```
delete A
create B as A
```

Identity check catches:

```
changed identity
```

111. Symlink Replacement Race

Expected:

```
regular file
```

Attacker:

```
replace with symlink
```

Type/identity validation rejects.

112. Directory Replacement Race

Expected:

```
empty directory
```

Attacker:

```
replace with another directory
```

Identity validation rejects.

113. Mount Replacement Race

Expected:

```
mount A
```

Current:

```
mount B
```

Reject as:

```
Stale
```

114. Bind Mount Race

If mount identity changes:

```
reject
```

115. Permission Race

Permission may change after planning.

Final operation:

```
current permission
```

116. SELinux Race

Security policy may change.

Operation must accept:

```
PermissionDenied
```

117. No Permission Escalation

Filesystem abstraction must not:

```
chmod
chown
setenforce
```

to make operation succeed.

118. Metadata Mutation

Cleanup executor should not modify:

```
ownership
permissions
security labels
```

unless explicitly authorized operation.

119. File Content

Cleanup deletion does not require:

```
reading file content
```

120. Privacy

Scanner should avoid reading:

```
file contents
```

121. Metadata Budget

Scanner budgets:

```
stat calls
directory reads
total entries
```

122. Filesystem Interface Budget

Adapter should expose bounded operations.

123. Directory Iteration

Conceptual:

```
DirectoryCursor
```

124. Cursor

Owns:

```
directory FD
iteration state
```

125. Cursor End

End-of-directory:

```
normal result
```

126. Malformed Dirent

Unexpected dirent data:

```
parser error
```

127. Dirent Names

Must be validated before entering:

```
PathComponent
```

128. "." and ".."

Never expose as ordinary:

```
PathComponent
```

129. Directory Stream

Scanner should not construct:

```
arbitrary concatenated absolute paths
```

130. Relative Representation

Use:

```
RelativePath
```

131. Maximum Depth

Filesystem traversal must enforce:

```
max_depth
```

132. Maximum Entries

Enforce:

```
max_entries
```

133. Maximum Metadata

Enforce:

```
max_metadata_reads
```

134. Maximum Duration

Enforce:

```
max_duration
```

135. Cancellation

Directory iteration must check:

```
CancellationToken
```

136. Cancellation Semantics

Cancellation:

```
stop discovery
```

without:

```
partial unsafe mutation
```

137. Executor Cancellation

For one atomic unlink:

```
cancellation cannot undo syscall
```

138. After Mutation

Result is recorded normally.

139. FD Closure

Cancellation must still:

```
close all owned FDs
```

140. Error Cleanup

Every error path:

```
release descriptors
```

141. Descriptor Tree

```
RootFd
  │
  ├── ParentFd
  │     └── Target operation
  │
  └── DirectoryCursor
          └── ChildFd
```

Ownership must be explicit.

142. No Global FD Registry

Avoid hidden global state.

143. Thread Safety

Owned FDs must not be concurrently mutated without explicit ownership.

144. Executor Concurrency

Default:

```
one mutation operation per resource
```

145. Scanner Concurrency

Read-only scans may use bounded concurrency.

146. Shared FD

Sharing FD across threads should require:

```
explicit synchronization
```

147. Filesystem Backend Trait

Conceptual:

```
trait Filesystem {
    type Root;
    type Directory;
    type Error;

    fn open_root(...);
    fn open_child(...);
    fn read_dir(...);
    fn stat(...);
    fn unlink(...);
}
```

148. Trait Safety

Trait must not expose:

```
raw arbitrary path mutation
```

149. Kernel Backend

Linux implementation:

```
LinuxFilesystem
```

150. Test Backend

```
FakeFilesystem
```

151. Fake Filesystem

Must simulate:

```
identity changes
mount changes
symlink
permission failure
disappearance
type replacement
```

152. Race Injection

Example:

```
on_validate_target:
    replace_target()
```

153. Expected

Executor:

```
detects mismatch
performs no unsafe mutation
```

154. Syscall Mocking

Unit tests can mock:

```
openat
fstatat
unlinkat
```

155. Integration

Integration tests use:

```
isolated temporary filesystem
```

156. Privileged Integration

Only explicit tests may access:

```
mount namespace
special filesystem
real block device
```

157. Android Compatibility

Filesystem adapter must account for:

```
Android libc differences
kernel feature availability
restricted filesystem access
SELinux enforcement
```

158. API Capability

Do not assume:

```
openat2
```

exists everywhere.

159. Runtime Detection

Capability catalog can expose:

```
OpenAt2Available
```

160. Adapter Selection

```
capability
   │
   ▼
secure implementation
```

161. Fallback Contract

Fallback must preserve:

```
same security invariants
```

162. If Security Cannot Be Preserved

Return:

```
Unsupported
```

163. No Degraded Security Mode

Forbidden:

```
"openat2 unavailable, use unsafe absolute paths"
```

164. Filesystem Mount Changes

Event system from 49.md can notify:

```
MountChanged
```

165. Reaction

Invalidate:

```
root identity
catalog
active plan
```

166. Active Operation

If mount changes during operation:

```
outcome may become Unknown
```

167. Verifier

Read-only verification determines:

```
Verified
Changed
Stale
Unknown
```

168. Persistent Recovery

If daemon crashes:

```
Started operation
```

becomes:

```
Unknown
```

169. Recovery Reconciliation

Use:

```
current root
current mount
current object identity
```

170. No Blind Replay

Critical invariant remains:

```
Unknown
   X
   ▼
unlinkat()
```

171. Storage Integration

Before mutation:

```
durable OperationStarted
```

172. After syscall:

```
durable OperationOutcome
```

173. Persistence Failure

If outcome cannot be persisted:

```
Unknown
```

174. Job State

Executor failure should propagate through:

```
OperationResult
   │
   ▼
Verifier
   │
   ▼
Aggregator
   │
   ▼
JobResult
```

175. Filesystem Result Taxonomy

Minimum:

```
Removed
AlreadyGone
Changed
TypeChanged
NotEmpty
PermissionDenied
ResourceBusy
CrossFilesystem
Stale
Unknown
IoFailure
```

176. Error Classification

Classification must be:

```
deterministic
```

177. Human Error

Human-readable text is secondary.

Primary:

```
reason code
```

178. Telemetry

Metrics:

```
filesystem_operations_total
unlink_success_total
already_gone_total
changed_identity_total
type_changed_total
permission_denied_total
resource_busy_total
unknown_outcome_total
fd_leak_detected_total
```

179. Logging

Do not log:

```
file contents
```

Prefer:

```
target id
operation id
reason code
```

180. Path Logging

Path logging should follow:

```
privacy policy
```

181. Audit

Audit should distinguish:

```
planned
authorized
started
executed
verified
```

182. Security Boundary

Filesystem abstraction must never become:

```
generic privileged filesystem API
```

183. Generic API Warning

Dangerous:

```
write_file(path, bytes)
delete_path(path)
chmod(path, mode)
chown(path, uid)
```

184. Better

Expose only operations required by architecture:

```
read metadata
enumerate directory
open authorized target
remove authorized entry
```

185. Capability Narrowing

Each interface should expose:

```
minimum necessary operation set
```

186. Separate Interfaces

Possible:

```
FilesystemReader
FilesystemResolver
FilesystemMutator
FilesystemVerifier
```

187. Scanner Dependency

Scanner:

```
FilesystemReader
FilesystemResolver
```

188. Safety Dependency

Safety:

```
FilesystemReader
FilesystemResolver
```

189. Executor Dependency

Executor:

```
FilesystemResolver
FilesystemMutator
```

190. Verifier Dependency

Verifier:

```
FilesystemReader
```

191. No Scanner Mutation

Scanner must not depend on:

```
FilesystemMutator
```

192. No Verifier Mutation

Verifier must not depend on:

```
FilesystemMutator
```

193. Dependency Graph

```
Scanner
   │
   ▼
Reader / Resolver

Safety
   │
   ▼
Reader / Resolver

Executor
   │
   ▼
Resolver / Mutator

Verifier
   │
   ▼
Reader
```

194. Raw Syscall Boundary

Only:

```
kernel/filesystem implementation
```

may call:

```
openat
openat2
fstatat
unlinkat
getdents/readdir
```

195. Domain Boundary

Domain sees:

```
RelativePath
TargetIdentity
ObjectType
FilesystemIdentity
MountIdentity
OperationResult
```

196. Domain Does Not See

```
errno
fd integer
raw stat struct
raw dirent
ioctl number
```

197. Fuzzing

Fuzz:

```
RelativePath parser
PathComponent parser
dirent parser
stat conversion
mount identity parser
filesystem identity parser
syscall error mapping
```

198. Security Property

For arbitrary malformed input:

```
no path escape
no arbitrary mutation
no panic
no unbounded allocation
```

199. Race Property

For any identity replacement between:

```
scan
validate
execute
```

system must:

```
reject
or
safely classify outcome
```

200. Definition of Done

54.md selesai jika:

```
[x] Filesystem abstraction didefinisikan
[x] Read/mutation separation didefinisikan
[x] TrustedRoot didefinisikan
[x] FD-relative resolution didefinisikan
[x] openat didefinisikan
[x] openat2 didefinisikan
[x] secure fallback didefinisikan
[x] Symlink policy didefinisikan
[x] Hard-link semantics didefinisikan
[x] Object identity didefinisikan
[x] Mount boundary didefinisikan
[x] Filesystem identity didefinisikan
[x] Parent identity didefinisikan
[x] Race handling didefinisikan
[x] unlinkat didefinisikan
[x] Directory removal semantics didefinisikan
[x] Recursive delete dilarang
[x] Error mapping didefinisikan
[x] FD ownership didefinisikan
[x] FD leak handling didefinisikan
[x] Cancellation didefinisikan
[x] Persistence integration didefinisikan
[x] Crash recovery didefinisikan
[x] Fake filesystem didefinisikan
[x] Race/fuzz testing didefinisikan
[x] Security invariants didefinisikan
```

201. Arah Berikutnya

Filesystem primitive sekarang sudah dikunci sampai level:

```
path
FD
identity
mount
syscall
race
error
recovery
```

Lapisan berikutnya perlu mengunci bagaimana cache Android ditemukan dan diklasifikasikan secara konkret tanpa memasukkan Android path knowledge ke domain.

Fokus 55.md:

```
55.md — Android Cache Target Discovery,
       Package Cache Mapping,
       App Cache,
       Code Cache,
       App-Specific Temporary Data,
       Device-Protected vs Credential-Protected Storage,
       User Scope,
       Package Identity,
       Cache Eligibility,
       Protected Paths,
       Target Root Construction,
       Storage Mapping,
       OEM/Android Version Differences,
       Discovery Fallback,
       Unknown Layout Handling,
       dan Android Cache Catalog Contract
```

Alur:

```
Android Platform
      │
      ▼
Package/User Discovery
      │
      ▼
Storage Mapping
      │
      ▼
Android Cache Adapter
      │
      ▼
Target Catalog
      │
      ├── AppCache
      ├── CodeCache
      ├── Temporary
      └── Other Explicit Target
      │
      ▼
   Scanner
      │
      ▼
   Safety
      │
      ▼
   Policy
```
