61.md — Result Aggregation Implementation Contract, OperationFinalResult, JobResult State Model, Success/Partial/Failure Semantics, Error Precedence, Retryability Classification, Byte and Operation Accounting, Durable Result Publication, Audit Correlation, Historical Result Immutability, User-facing Result DTO, IPC/CLI Reporting, Explain Integration, Result Pagination, Retention, Crash-safe Publication, dan Job Completion Barrier

Tujuan

Dokumen ini mengunci Aggregator dan Result Model.

Aggregator berada setelah Executor dan Verifier:

```
Executor
   │
   ▼
OperationResult
   │
   ▼
Verifier
   │
   ▼
VerificationResult
   │
   ▼
OperationFinalResult
   │
   ▼
JobResult
   │
   ▼
Durable Publication
   │
   ├── CLI
   ├── IPC
   └── Explain
```

1. Aggregator tidak melakukan mutation.

2. Aggregator tidak melakukan:

   filesystem discovery
   policy evaluation
   authorization
   retry execution

3. Aggregator bertugas:

   collect
   correlate
   normalize
   classify
   aggregate
   publish

4. Aggregator menerima:

   OperationResult
   VerificationResult
   Job metadata

5. Output:

   immutable JobResult

6. JobResult harus deterministic.

7. Input yang sama harus menghasilkan:

   JobResult yang sama

kecuali metadata seperti timestamp memang secara eksplisit non-deterministic.

8. OperationFinalResult

OperationFinalResult adalah hasil final satu operation setelah:

```
mutation outcome
verification outcome
```

digabungkan.

9. Struktur konseptual:

   OperationFinalResult {
   JobId
   OperationId
   OperationType
   TargetId
   MutationStatus
   VerificationStatus
   FinalStatus
   ReasonCode
   Attempt
   Accounting
   AuditReference
   }

10. MutationStatus

Minimal:

```
NotAttempted
Succeeded
Failed
Unknown
Cancelled
```

11. VerificationStatus

Minimal:

```
Verified
AlreadySatisfied
NotSatisfied
Changed
Stale
Unknown
Cancelled
BudgetExceeded
```

12. FinalStatus

Minimal:

```
Completed
AlreadySatisfied
Failed
Cancelled
Stale
Unknown
Skipped
```

13. FinalStatus tidak boleh hanya:

```
success / failure
```

14. Semantic distinction penting.

15. Example:

    unlinkat = ENOENT
    verification = AlreadyAbsent

FinalStatus:

```
AlreadySatisfied
```

16. Example:

```
unlinkat = success
verification = Verified
```

FinalStatus:

```
Completed
```

17. Example:

```
unlinkat = success
verification = Unknown
```

FinalStatus:

```
Unknown
```

18. Jangan mengubah:

```
Unknown → Completed
```

19. Example:

```
unlinkat = failed
verification = target still exists
```

FinalStatus:

```
Failed
```

20. Example:

```
unlinkat = unknown
verification = target absent
```

FinalStatus:

```
AlreadySatisfied
```

21. Postcondition dapat menjadi bukti lebih kuat daripada syscall outcome.

22. Tetapi audit tetap menyimpan:

    original mutation outcome

23. Operation Correlation

Semua hasil harus memakai:

```
JobId
OperationId
```

24. OperationId harus unik dalam Job.

25. Duplicate result:

    reject
    atau
    idempotently ignore

sesuai persistence contract.

26. Conflicting duplicate:

    Corruption

27. Example:

    Operation O1
    │
    ├── Verified
    └── Failed

Ini tidak boleh dianggap normal.

28. Result Store harus mendeteksi:

    conflicting terminal states

29. Operation Ordering

Aggregator tidak bergantung pada:

```
arrival order
```

30. Worker completion dapat out-of-order.

31. Aggregator menggunakan:

    OperationId
    plan order
    dependency metadata

32. Final result order harus:

    deterministic

33. Recommended:

    plan operation order

34. Partial Execution

Example:

```
O1 Completed
O2 Completed
O3 Failed
O4 NotAttempted
```

Job:

```
Partial
```

35. O4 NotAttempted karena:

```
dependency
cancellation
budget
stale state
```

harus memiliki reason.

36. Partial bukan:

    Failed

secara otomatis.

37. Partial berarti:

    sebagian intended operations reached desired state.

38. Job-level Status

Minimal:

```
Completed
Partial
Failed
Cancelled
Deferred
Stale
Unknown
RecoveryRequired
```

39. Completed

Semua required operations:

```
Completed
atau
AlreadySatisfied
```

40. Partial

Sebagian berhasil dan sebagian:

```
failed
cancelled
skipped
not satisfied
```

41. Failed

Job gagal mencapai intended state secara material.

42. Cancelled

Job dihentikan sebelum semua required operations selesai.

43. Deferred

Job belum selesai karena:

```
readiness
lock
temporary resource condition
```

44. Deferred bukan:

```
failure
```

45. Stale

Execution context tidak lagi valid.

46. Unknown

System tidak dapat menentukan final truth.

47. RecoveryRequired

Durable state menunjukkan:

```
operation outcome belum dapat direkonsiliasi.
```

48. RecoveryRequired harus diprioritaskan daripada:

```
Completed
```

49. Job terminal states:

```
Completed
Partial
Failed
Cancelled
Stale
Unknown
```

50. Non-terminal:

```
Created
Queued
Running
Deferred
RecoveryRequired
```

51. Exact Job State transition mengikuti:

```
Job State Machine Contract
```

52. Aggregator tidak boleh memaksa:

```
invalid state transition
```

53. Error Precedence

Hasil individual harus memiliki precedence.

54. Security/integrity issue memiliki prioritas tinggi.

55. Recommended semantic precedence:

    Corruption
    >
    SecurityViolation
    >
    Stale
    >
    Unknown
    >
    Failed
    >
    Cancelled
    >
    Partial
    >
    Completed

56. Namun precedence tidak boleh menghapus:

    individual operation results

57. Example:

    O1 Completed
    O2 Stale

Job:

```
Stale
```

58. O1 tetap:

```
Completed
```

59. Stale job:

```
no blind retry
```

60. Fresh job diperlukan.

61. SecurityViolation

Examples:

```
scope violation
plan tampering
identity boundary violation
```

62. SecurityViolation harus:

```
fail closed
```

63. Corruption

Examples:

```
durable state conflict
impossible operation transition
conflicting fingerprints
```

64. Corruption tidak boleh:

```
auto-repair silently
```

65. Recovery path:

```
RecoveryRequired
```

66. Unknown

Unknown adalah state epistemik:

```
system tidak tahu dengan cukup bukti
```

67. Unknown tidak sama dengan:

```
failed
```

68. Unknown tidak sama dengan:

```
successful
```

69. Reporting harus mempertahankan ini.

70. Retryability

Retryability harus menjadi:

```
explicit classification
```

71. Retry class:

```
Never
FreshScanRequired
SafeToRetry
RecoveryRequired
```

72. Never

Tidak boleh retry otomatis.

73. FreshScanRequired

Harus:

```
new job
fresh discovery
fresh classification
fresh planning
fresh authorization
```

74. SafeToRetry

Hanya boleh jika:

```
operation contract secara eksplisit membuktikan
mutation belum terjadi
```

75. RecoveryRequired

Harus:

```
reconcile durable state
```

76. Unknown

Default:

```
RecoveryRequired
atau
FreshScanRequired
```

77. Never:

```
automatic replay
```

78. Example:

```
PermissionDenied
```

Retry:

```
SafeToRetry = false
```

79. Example:

```
TargetAlreadyGone
```

Retry:

```
Never
```

karena desired state sudah tercapai.

80. Example:

    GenerationMismatch

Retry:

```
FreshScanRequired
```

81. Example:

```
LockUnavailable
```

Retry:

```
SafeToRetry
```

atau:

```
Deferred
```

jika tidak ada mutation.

82. Accounting

Aggregator harus menghitung:

```
operations planned
operations attempted
operations completed
operations already satisfied
operations failed
operations unknown
operations cancelled
operations skipped
```

83. Byte Accounting

Minimal:

```
planned_bytes
attempted_bytes
verified_bytes
unknown_bytes
```

84. Byte accounting harus:

```
deterministic
```

85. "Bytes reclaimed" harus hati-hati.

86. Recommended terminology:

    logical_bytes_affected

87. Jangan klaim:

    physical storage reclaimed

jika tidak diverifikasi.

88. File deletion:

    logical size

89. Directory removal:

    normally zero direct file payload

90. Directory recursive size tidak boleh dihitung oleh Aggregator jika Planner tidak memberikannya.

91. Unknown Size

Jika size tidak diketahui:

```
unknown
```

92. Unknown tidak boleh:

```
0 bytes
```

93. Example:

```
O1 = 4 KiB
O2 = unknown
O3 = 8 KiB
```

Total known:

```
12 KiB
```

Unknown:

```
true
```

94. Reporting:

```
"at least 12 KiB"
```

hanya jika semantics mengizinkan.

95. Simpler:

    known_bytes = 12 KiB
    bytes_unknown = true

96. Operation Count

Count operation dari:

```
AuthorizedPlan
```

97. Tidak dari:

```
executor event count
```

98. Why

Worker duplicate events tidak boleh menggandakan accounting.

99. Idempotency

Aggregator harus idempotent.

100. Replaying event:

```
same event
```

harus menghasilkan:

```
same state
```

101. Duplicate terminal event:

```
no double count
```

102. Conflicting event:

```
corruption
```

103. Durable Result

JobResult harus durable sebelum:

```
Job = Completed
```

104. Completion Barrier

```
All Operation Results
       │
       ▼
All Verification Results
       │
       ▼
Aggregate
       │
       ▼
Validate JobResult
       │
       ▼
Persist JobResult
       │
       ▼
Commit Terminal Job State
       │
       ▼
Emit Completion Event
```

105. Completion event tidak boleh muncul sebelum:

```
durable JobResult
```

106. Why

Client dapat menerima event lalu:

```
query result
```

dan result harus sudah tersedia.

107. Publication Failure

Jika result persistence gagal:

```
job tidak boleh dianggap durably completed
```

108. State:

```
RecoveryRequired
atau
PersistenceFailure
```

109. Persistence failure tidak boleh disamarkan sebagai:

```
Completed
```

110. Crash

Crash setelah:

```
result persisted
before event
```

Recovery harus:

```
publish event idempotently
atau rebuild event
```

111. Crash after event

Client menerima event.

Result sudah durable.

112. Good.

113. Crash before result

Client tidak boleh menerima:

```
Completed
```

114. Event Identity

Completion event harus memiliki:

```
JobId
ResultVersion
ResultFingerprint
```

115. Client dedup:

```
JobId + ResultVersion
```

116. Result Fingerprint

JobResult dapat memiliki:

```
deterministic fingerprint
```

117. Fingerprint input:

```
JobId
PlanId
operation final results
aggregate status
accounting
```

118. Timestamp

Timestamp tidak sebaiknya menentukan:

```
semantic fingerprint
```

jika timestamp hanya metadata.

119. Historical Immutability

JobResult terminal:

```
immutable
```

120. Tidak boleh:

```
overwrite result
```

121. Retry menghasilkan:

```
new JobId
```

122. Old Job:

```
historical
```

123. New Job:

```
new execution
```

124. Audit Link

New Job dapat menyimpan:

```
retry_of = old JobId
```

125. Historical chain:

```
Job A
   │
   ▼
Retry Job B
   │
   ▼
Retry Job C
```

126. Semua tetap immutable.

127. Explain

Explain harus dapat menjawab:

```
what happened?
why?
which operation?
which target?
what was expected?
what was observed?
```

128. Explain tidak boleh melakukan:

```
re-execution
```

129. Explain menggunakan:

```
persisted result
persisted audit
persisted plan metadata
```

130. If result unavailable:

```
"result not yet durable"
```

131. User-facing Result

Public DTO tidak harus sama dengan:

```
internal JobResult
```

132. Internal:

```
rich diagnostic information
```

133. External:

```
bounded safe information
```

134. Example Public:

```
status
operations completed
operations skipped
logical bytes affected
duration
summary
reason codes
```

135. Internal:

```
inode
device
low-level errno
internal capability
SELinux diagnostic
```

136. Sensitive details should be:

```
authorized before exposure
```

137. IPC

IPC response should return:

```
versioned ResultDTO
```

138. Never serialize:

```
internal Rust/domain structures directly
```

139. Result DTO:

```
stable
```

140. Unknown enum

Client handling must be:

```
forward compatible where possible
```

141. CLI

CLI should render:

```
Job status
operation summary
byte summary
reason
retry recommendation
```

142. CLI should not reconstruct result from:

```
raw logs
```

143. Logs are:

```
diagnostics
```

144. Result Store is:

```
source of truth
```

145. Pagination

Large operation result:

```
paginated
```

146. Never return:

```
unlimited operation list
```

147. Query:

```
page_size
cursor
```

148. Cursor should be:

```
opaque
```

149. Cursor must not expose:

```
internal database structure
```

150. Stable Ordering

Pagination order:

```
deterministic
```

151. Recommended:

```
operation sequence
```

152. Filter

Possible filters:

```
status
target
operation type
```

153. Filtering must not:

```
change historical result
```

154. Explain Pagination

Audit trail may also require:

```
bounded pagination
```

155. Retention

Results need retention policy.

156. Retention categories:

```
active
```

```
recent
historical
expired
```

157. Terminal result should remain:

```
long enough for diagnosis
```

158. Deletion of old results:

```
separate maintenance operation
```

159. Result deletion must not affect:

```
active jobs
```

160. Audit retention may be:

```
longer than verbose logs
```

161. Job metadata retention:

```
explicit policy
```

162. Retention must not be triggered:

```
during active mutation
```

163. Compaction

Result storage can compact:

```
non-authoritative derived caches
```

164. But must preserve:

```
authoritative JobResult
```

165. Audit

Audit records:

```
authorization
execution
verification
aggregation
publication
```

166. Correlation:

```
JobId
OperationId
PlanId
```

167. Audit Sequence

Each event has:

```
sequence number
```

168. Ordering:

```
deterministic within Job
```

169. Missing sequence:

```
detectable
```

170. Duplicate sequence:

```
idempotent if identical
```

171. Conflicting sequence:

```
corruption
```

172. Result Validation

Before persistence:

```
validate all operation IDs
validate all states
validate accounting
validate status precedence
validate plan correlation
```

173. Operation Count Validation

Example:

```
planned = 10
terminal = 11
```

Invalid.

174. Accounting Invariant

```
attempted <= planned
```

175. Completed invariant:

```
completed <= attempted
```

176. Verified invariant:

```
verified <= attempted
```

177. Unknown invariant:

```
unknown <= attempted
```

178. Skipped:

```
skipped <= planned
```

179. Sum semantics must be explicit because one operation can transition:

```
attempted
```

```
  →
verified
```

180. Do not double count categories.

181. Recommended partition:

```
Pending
```

```
InFlight
Terminal
```

182. Terminal categories are mutually exclusive.

183. Terminal:

```
Completed
```

```
AlreadySatisfied
Failed
Cancelled
Stale
Unknown
Skipped
```

184. One operation:

```
exactly one terminal category
```

185. Aggregator must reject:

```
two terminal states
```

186. Job status derived from:

```
terminal operation partition
job lifecycle
system state
```

187. Deferred

A deferred operation has:

```
no mutation
```

188. Deferred operation can remain:

```
non-terminal
```

189. If job itself Deferred:

```
no final result required yet
```

190. If execution already partially happened:

```
job should not become pure Deferred
```

191. Example:

```
O1 Completed
O2 blocked by thermal condition
```

Job:

```
Partial
atau
Deferred
```

depending on scheduler semantics.

192. Decision must be:

```
explicit
```

193. Cancellation

If user cancels before any mutation:

```
Cancelled
```

194. If partial mutation happened:

```
Partial
atau
CancelledWithPartialResult
```

195. Better public model:

```
status = Cancelled
progress = partial
```

196. Do not hide completed operations.

197. Example:

```
3 completed
```

```
2 cancelled
```

Result:

```
Cancelled
completed_operations = 3
```

198. Stale

If generation changes during job:

```
Stale
```

199. Already completed operations remain:

```
historical completed
```

200. Remaining operations:

```
not attempted
```

201. Do not rerun completed operations automatically.

202. Retry creates:

```
fresh job
```

203. Unknown

Unknown must expose:

```
reconciliation state
```

204. User-facing:

```
"Result could not be fully confirmed."
```

205. No misleading:

```
"Cleanup failed."
```

206. Failed

Failed requires:

```
mutation attempted or authoritative execution failure
```

207. Planning failure is not:

```
Executor failure
```

208. Pre-execution authorization denial may produce:

```
Denied / Failed request
```

not operation execution failure.

209. JobResult should distinguish:

```
RequestRejected
```

```
ExecutionFailed
```

if public model supports it.

210. Deferred

Examples:

```
storage locked
thermal restriction
resource lock unavailable
```

211. Scheduler may create:

```
future attempt
```

212. New attempt:

```
same logical request
new AttemptId
```

213. New JobId policy depends on scheduler contract.

214. Attempt

Job may have:

```
AttemptId
```

215. Historical attempt results must remain:

```
immutable
```

216. Aggregator should preserve:

```
attempt number
```

217. Retry classification:

```
result-level metadata
```

218. Scheduler consumes:

```
Retryability
```

219. Aggregator does not call:

```
Scheduler.retry()
```

220. Separation:

```
Aggregator:
    what happened?

Scheduler:
    what next?
```

221. Result Fingerprint

Equivalent result state should generate:

```
same fingerprint
```

222. This helps:

```
idempotent publication
```

223. Publication

Conceptual:

```
Aggregate
   │
   ▼
Validate
   │
   ▼
Fingerprint
   │
   ▼
Persist
   │
   ▼
Commit
   │
   ▼
Emit Event
```

224. If persist succeeds but event fails:

```
result remains durable
```

225. Runtime can retry:

```
event publication
```

226. Event publication must be:

```
idempotent
```

227. If persist fails:

```
do not emit completion
```

228. If validation fails:

```
no terminal publication
```

229. Corrupt Result

If impossible state detected:

```
RecoveryRequired
```

230. Never silently normalize corruption.

231. Example:

```
operation count mismatch
```

Result:

```
CorruptJobState
```

232. Persistence

Use:

```
ResultStore
```

233. ResultStore belongs to:

```
Durable Storage layer
```

234. Aggregator depends on:

```
ResultStore interface
```

235. Aggregator should not know:

```
SQLite
file format
journal implementation
```

236. Storage implementation follows:

```
51.md
```

237. Suggested structure:

```
src/result/
    mod.rs
    operation.rs
    aggregate.rs
    status.rs
    accounting.rs
    retry.rs
    explain.rs
    dto.rs
    publisher.rs
```

238. Result domain objects remain:

```
immutable
```

239. DTO conversion occurs at:

```
IPC/CLI boundary
```

240. Internal Result

Contains:

```
typed IDs
typed statuses
structured reasons
```

241. External DTO

Contains:

```
serializable bounded fields
```

242. Versioning

Result schema must have:

```
schema version
```

243. Future status

Old client must not interpret unknown status as:

```
Completed
```

244. Safe fallback:

```
Unknown
```

245. IPC size

Result response must be:

```
bounded
```

246. Large result:

```
pagination
```

247. Explain query:

```
bounded
```

248. Audit query:

```
authorization required
```

249. Job Ownership

User may only query jobs:

```
they are authorized to see
```

250. Job ID Guessing

Knowing:

```
JobId
```

must not automatically grant access.

251. IPC Authorization

Result access requires:

```
ReadJobResult capability
```

252. Explain access:

```
ExplainJob capability
```

253. Audit access:

```
AuditRead capability
```

254. Separation

Read result:

```
does not imply
modify job
```

255. Cancel:

```
separate capability
```

256. Result Tampering

Client cannot submit:

```
fake OperationFinalResult
```

257. Result authority is:

```
server-generated
```

258. Worker result

Worker must be authenticated by:

```
WorkerId
internal channel
```

259. External client cannot impersonate worker.

260. Worker result validation:

```
operation belongs to assigned job
```

261. Worker result cannot:

```
complete arbitrary JobId
```

262. Security Invariants

Invariant 1:

```
JobResult is server-generated.
```

263. Invariant 2:

```
JobResult is immutable after terminal publication.
```

264. Invariant 3:

```
Completion event occurs after durable result.
```

265. Invariant 4:

```
Unknown is never success.
```

266. Invariant 5:

```
Duplicate result cannot double-count.
```

267. Invariant 6:

```
Conflicting terminal result is corruption.
```

268. Invariant 7:

```
Retry never mutates historical result.
```

269. Invariant 8:

```
Result query requires appropriate read authority.
```

270. Invariant 9:

```
Result aggregation never mutates filesystem.
```

271. Invariant 10:

```
Aggregator never retries mutation.
```

272. Invariant 11:

```
Accounting cannot exceed authorized plan.
```

273. Invariant 12:

```
Result publication is idempotent.
```

274. Invariant 13:

```
Result schema is versioned.
```

275. Invariant 14:

```
Unbounded result output is prohibited.
```

276. Invariant 15:

```
Historical audit remains correlated with JobId and OperationId.
```

277. Testing

Unit tests:

```
success
already satisfied
failed
cancelled
stale
unknown
partial
deferred
```

278. Aggregation tests:

```
all success
all already absent
mixed success/failure
mixed success/unknown
cancellation after partial execution
stale after partial execution
```

279. Precedence tests:

```
security violation
corruption
stale
unknown
failure
cancellation
```

280. Accounting tests:

```
duplicate events
missing events
conflicting events
zero operations
maximum operations
```

281. Persistence tests:

```
persist success
persist failure
crash before persist
crash after persist
crash before event
duplicate event
```

282. Pagination tests:

```
stable cursor
duplicate page
missing item
large result
```

283. Authorization tests:

```
owner
non-owner
read-only
admin
unauthorized audit access
```

284. Property Test

For every operation:

```
exactly one terminal final status
```

285. Property Test

```
completed + already_satisfied + failed
+ cancelled + stale + unknown + skipped
<= planned
```

with exact partition semantics.

286. Property Test

```
final_result.operations
```

```
    =
authorized_plan.operations
```

for a fully resolved job.

287. Property Test

No result can introduce:

```
OperationId not present in plan.
```

288. Property Test

No result can report:

```
bytes > authorized/known accounting constraints
```

289. Fuzzing

Fuzz:

```
OperationResult
VerificationResult
audit events
persisted result
status combinations
malformed DTO
```

290. Fuzz invariant:

```
malformed result never becomes Completed.
```

291. Crash Injection

Crash at:

```
aggregate
validation
fingerprint
persistence
commit
publication
```

292. Expected:

```
no false terminal success
```

293. Integration

```
Executor
   ↓
Verifier
   ↓
Aggregator
   ↓
ResultStore
   ↓
EventPublisher
```

294. End-to-end test:

```
successful cleanup
```

must produce:

```
durable JobResult
then completion event
```

295. End-to-end test:

```
crash after mutation
```

must produce:

```
recovery
verification
final result
```

296. End-to-end test:

```
stale generation
```

must produce:

```
Stale
without blind retry
```

297. End-to-end test:

```
partial execution
```

must preserve:

```
individual operation outcomes
```

298. Definition of Done

61.md selesai jika:

```
[x] OperationFinalResult didefinisikan
[x] JobResult didefinisikan
[x] MutationResult + VerificationResult aggregation didefinisikan
[x] Completed semantics didefinisikan
[x] AlreadySatisfied semantics didefinisikan
[x] Partial semantics didefinisikan
[x] Failed semantics didefinisikan
[x] Cancelled semantics didefinisikan
[x] Deferred semantics didefinisikan
[x] Stale semantics didefinisikan
[x] Unknown semantics didefinisikan
[x] RecoveryRequired didefinisikan
[x] Error precedence didefinisikan
[x] Retryability didefinisikan
[x] Byte accounting didefinisikan
[x] Operation accounting didefinisikan
[x] Idempotency didefinisikan
[x] Durable publication didefinisikan
[x] Completion barrier didefinisikan
[x] Historical immutability didefinisikan
[x] Explain integration didefinisikan
[x] CLI/IPC reporting didefinisikan
[x] Pagination didefinisikan
[x] Retention didefinisikan
[x] Audit correlation didefinisikan
[x] Result authorization didefinisikan
[x] Crash-safe publication didefinisikan
[x] Security invariants didefinisikan
[x] Property tests didefinisikan
[x] Fuzz tests didefinisikan
```

299. Posisi Arsitektur Setelah 61.md

Sekarang lifecycle utama telah menjadi:

```
┌─────────────────────────────────────────┐
│             DISCOVERY                   │
│                                         │
│ Platform → Catalog → Scanner             │
└──────────────────┬──────────────────────┘
                   ↓
┌─────────────────────────────────────────┐
│             DECISION                    │
│                                         │
│ Safety → Policy → Planner → Authorization│
└──────────────────┬──────────────────────┘
                   ↓
          ┌────────────────┐
          │ AuthorizedPlan │
          └───────┬────────┘
                  ↓
┌─────────────────────────────────────────┐
│             EXECUTION                   │
│                                         │
│ Executor → Filesystem Mutation           │
└──────────────────┬──────────────────────┘
                   ↓
┌─────────────────────────────────────────┐
│             OBSERVATION                 │
│                                         │
│ Verifier → OperationFinalResult          │
└──────────────────┬──────────────────────┘
                   ↓
┌─────────────────────────────────────────┐
│             PUBLICATION                 │
│                                         │
│ Aggregator → JobResult → Durable Store   │
└──────────────────┬──────────────────────┘
                   ↓
         ┌────────────────────┐
         │ CLI / IPC / Explain│
         └────────────────────┘
```

300. Boundary Penting

Sebelum 61.md:

```
"operation selesai"
```

Sekarang maknanya menjadi:

```
Mutation
   +
Verification
   +
Aggregation
   +
Durable Publication
```

301. Sehingga:

```
syscall success
    ≠
operation success
    ≠
job success
```

302. Model yang benar:

```
syscall outcome
      ↓
verification truth
      ↓
operation final result
      ↓
job-level result
```

303. Ini menjadi dasar untuk:

```
scheduler retry
CLI status
IPC status
explain
audit
metrics
crash recovery
```

304. Arah Berikutnya

Result pipeline sekarang sudah terkunci.

Tahap berikutnya adalah mengunci Scheduler secara implementatif berdasarkan:

```
JobResult
   │
   ▼
Retryability
   │
   ▼
Deferred / Stale / Unknown
   │
   ▼
Backoff
   │
   ▼
Deduplication
   │
   ▼
Readiness
   │
   ▼
Trigger
   │
   ▼
New Job / Attempt
```

Fokus 62.md:

```
62.md — Scheduler Implementation Contract,
       Trigger Sources,
       Periodic Scheduling,
       Manual Scheduling,
       Event-triggered Scheduling,
       Job Deduplication,
       Queue Admission,
       Priority,
       Backoff,
       Retry Policy,
       Deferred Job Handling,
       Stale Job Handling,
       Unknown Result Handling,
       Readiness-aware Scheduling,
       Concurrency Limits,
       Resource Locks,
       Fairness,
       Starvation Prevention,
       Shutdown,
       Crash Recovery,
       Scheduler Persistence,
       Misfire Handling,
       Schedule Drift,
       dan Deterministic Scheduling Algorithm
```
