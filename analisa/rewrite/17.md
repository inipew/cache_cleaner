Lanjut. Sekarang kita kunci **kontrak Rust antar-layer**. Ini tahap penting sebelum implementation, karena kalau boundary type-nya benar dari awal, sebagian besar bug arsitektur bisa dicegah oleh compiler.

# 60. Prinsip ownership antar-layer

Saya sarankan dependency satu arah:

```text
                    ┌──────────────┐
                    │     CLI      │
                    └──────┬───────┘
                           │
                    ┌──────▼───────┐
                    │     IPC      │
                    └──────┬───────┘
                           │
                    ┌──────▼───────┐
                    │      App     │
                    └──────┬───────┘
                           │
              ┌────────────▼────────────┐
              │       Scheduler         │
              └────────────┬────────────┘
                           │
              ┌────────────▼────────────┐
              │       JobController     │
              └────────────┬────────────┘
                           │
       ┌───────────────────┼───────────────────┐
       ▼                   ▼                   ▼
   Scanner             Policy              Planner
       │                   │                   │
       └───────────────────┼───────────────────┘
                           ▼
                        Safety
                           │
                           ▼
                       Executor
                           │
              ┌────────────┼────────────┐
              ▼            ▼            ▼
             FS          Kernel       Android
```

Tapi ada satu koreksi penting:

**Safety tidak seharusnya menjadi tahap setelah Policy.**

Untuk security boundary, urutan konseptualnya:

```text
Scanner
   ↓
Safety validation
   ↓
Policy evaluation
   ↓
Planner
   ↓
Plan validation
   ↓
Authorization
   ↓
Executor revalidation
```

Policy hanya menerima object yang sudah terbukti aman secara teknis.

---

# 61. `domain` harus menjadi pusat model

`domain` jangan tahu:

```text
libc
Linux syscall
Android API
SELinux
epoll
UDS
PathBuf absolut
```

Domain hanya tahu konsep:

```rust
pub struct Candidate { ... }

pub struct TargetRoot { ... }

pub struct CleanupPlan { ... }

pub enum Operation { ... }

pub enum Decision { ... }

pub struct JobId(...);

pub struct TargetId(...);

pub struct OperationId(...);
```

Dengan begitu domain dapat diuji dengan unit test murni.

---

# 62. ID jangan gunakan `String`

Hindari:

```rust
pub type JobId = String;
```

Lebih baik:

```rust
#[derive(Clone, Copy, Debug, Eq, PartialEq, Hash)]
pub struct JobId(pub u64);

#[derive(Clone, Copy, Debug, Eq, PartialEq, Hash)]
pub struct TargetId(pub u64);

#[derive(Clone, Copy, Debug, Eq, PartialEq, Hash)]
pub struct OperationId(pub u64);

#[derive(Clone, Copy, Debug, Eq, PartialEq, Hash)]
pub struct PlanId(pub u64);
```

Keuntungannya:

```text
JobId
≠
TargetId
≠
OperationId
```

Compiler membantu mencegah tertukar.

---

# 63. `UserId` juga typed

Jangan:

```rust
user_id: u32
```

di seluruh project.

Gunakan:

```rust
#[derive(Clone, Copy, Debug, Eq, PartialEq, Hash)]
pub struct UserId(pub u32);
```

Demikian juga:

```rust
pub struct PackageName(String);
pub struct DeviceId(pub u64);
```

---

# 64. PackageName harus divalidasi

```rust
pub struct PackageName(String);

impl PackageName {
    pub fn parse(value: String) -> Result<Self, PackageNameError> {
        // validate Android package-name syntax
    }

    pub fn as_str(&self) -> &str {
        &self.0
    }
}
```

Jangan izinkan sembarang string masuk ke domain.

---

# 65. `RelativePath` adalah security type

Ini bukan:

```rust
type RelativePath = PathBuf;
```

Kita ingin:

```rust
pub struct RelativePath {
    components: Vec<OsString>,
}
```

Constructor:

```rust
impl RelativePath {
    pub fn parse(path: &Path) -> Result<Self, RelativePathError>;
}
```

Ia menolak:

```text
absolute path
..
empty target
invalid components
```

Dan API traversal hanya menerima `RelativePath`.

---

# 66. `TrustedRoot` bukan domain object murni

Ada sedikit konflik antara domain dan infrastructure.

`TargetRoot` adalah konsep domain:

```rust
pub struct TargetRoot {
    pub id: TargetId,
    pub kind: TargetKind,
    pub scope: TargetScope,
    pub storage: StorageDomain,
}
```

Sedangkan:

```rust
TrustedRoot
```

adalah filesystem capability.

Jadi saya sarankan:

```text
domain/
    target.rs

fs/
    trusted_root.rs
```

dan hubungan:

```text
TargetRoot
    +
filesystem resolution
    ↓
TrustedRoot
```

---

# 67. Jangan simpan `OwnedFd` di domain

`OwnedFd` adalah implementation detail Linux.

Jadi jangan:

```rust
pub struct TargetRoot {
    pub fd: OwnedFd,
}
```

di `domain`.

Lebih baik:

```text
domain::TargetRoot
        ↓
platform/fs resolver
        ↓
fs::TrustedRoot
```

---

# 68. Candidate juga jangan menyimpan FD

Scanner boleh menggunakan FD untuk menemukan object, tetapi candidate sebaiknya menyimpan **provenance**, bukan resource lifetime panjang.

```rust
pub struct Candidate {
    pub target_id: TargetId,
    pub relative_path: RelativePath,
    pub identity: ObjectIdentity,
    pub entry_type: EntryType,
    pub size: u64,
    pub timestamps: Timestamps,
}
```

Kenapa?

Karena scan bisa menghasilkan ribuan candidate.

Menyimpan ribuan open FDs akan buruk.

---

# 69. FD lifetime pendek

Pattern:

```text
scan:
    open root
    open child directory
    inspect
    close
```

Bukan:

```text
scan:
    open 100,000 files
    keep all FDs
    planner
    executor
```

Resource lifetime harus sesingkat mungkin.

---

# 70. `ObjectIdentity`

```rust
#[derive(Clone, Copy, Debug, Eq, PartialEq, Hash)]
pub struct ObjectIdentity {
    pub device: DeviceId,
    pub inode: u64,
}
```

Nanti bisa diperluas:

```rust
pub struct ObjectIdentity {
    pub device: DeviceId,
    pub inode: u64,
    pub generation: Option<u64>,
}
```

tetapi `generation` tidak selalu tersedia/berguna di semua filesystem, jadi jangan bergantung padanya.

---

# 71. `EntryType` harus eksplisit

```rust
pub enum EntryType {
    RegularFile,
    Directory,
    Symlink,
    Socket,
    Fifo,
    BlockDevice,
    CharDevice,
    Unknown,
}
```

Jangan:

```rust
is_dir: bool
is_file: bool
```

Karena itu kehilangan informasi.

---

# 72. SafetyValidated sebagai type-state

Ini salah satu bagian yang paling saya rekomendasikan.

```rust
pub struct SafetyValidated {
    candidate: Candidate,
    proof: SafetyProof,
}
```

Constructor:

```rust
pub(crate) fn validate(
    candidate: Candidate,
    context: &SafetyContext,
) -> Result<SafetyValidated, SafetyViolation>
```

Tidak ada:

```rust
pub fn SafetyValidated::new(...)
```

yang bisa dipanggil sembarang module.

---

# 73. SafetyProof

```rust
pub struct SafetyProof {
    pub root_identity: ObjectIdentity,
    pub entry_identity: ObjectIdentity,
    pub device: DeviceId,
    pub entry_type: SafeEntryType,
}
```

Misalnya:

```rust
pub enum SafeEntryType {
    RegularFile,
    Directory,
}
```

Artinya setelah Safety:

```text
Symlink
Socket
Device node
Unknown
```

sudah tidak dapat masuk ke operation normal.

---

# 74. PolicyApproval

Setelah safety:

```rust
pub struct PolicyApproval {
    candidate: SafetyValidated,
    reason: PolicyReason,
}
```

Sekarang kita punya semantic:

```text
SafetyValidated
=
technically safe

PolicyApproval
=
allowed by current policy
```

Ini jauh lebih kuat daripada satu:

```rust
bool approved
```

---

# 75. Planner menerima tipe yang tepat

```rust
pub fn plan(
    candidates: &[PolicyApproval],
    context: &PlannerContext,
) -> Result<CleanupPlan, PlannerError>
```

Planner tidak bisa menerima:

```rust
Candidate
```

secara langsung.

Dengan begitu developer tidak mudah melewati Safety.

---

# 76. CleanupPlan immutable

```rust
pub struct CleanupPlan {
    id: PlanId,
    operations: Vec<PlannedOperation>,
    summary: PlanSummary,
}
```

Field dibuat private.

Expose:

```rust
impl CleanupPlan {
    pub fn id(&self) -> PlanId;
    pub fn operations(&self) -> &[PlannedOperation];
    pub fn summary(&self) -> &PlanSummary;
}
```

Tidak boleh:

```rust
plan.operations.push(...)
```

dari luar.

---

# 77. PlannedOperation

```rust
pub struct PlannedOperation {
    id: OperationId,
    operation: Operation,
    target: OperationTarget,
    cost: OperationCost,
    preconditions: Vec<Precondition>,
    dependencies: Vec<OperationId>,
}
```

Semua field private.

Hanya Planner yang membuatnya.

---

# 78. OperationTarget

```rust
pub enum OperationTarget {
    File(FileTarget),
    Directory(DirectoryTarget),
    Filesystem(FilesystemTarget),
    Zram(ZramTarget),
    Framework(FrameworkTarget),
}
```

Ini mencegah:

```rust
RemoveFile {
    target: FilesystemTarget
}
```

secara type system.

---

# 79. `RemoveFile`

```rust
pub struct RemoveFile {
    pub target: FileTarget,
}
```

dan:

```rust
pub struct FileTarget {
    pub target_id: TargetId,
    pub relative_path: RelativePath,
    pub identity: ObjectIdentity,
}
```

Executor mendapatkan target yang sudah jelas.

---

# 80. Jangan beri Executor Candidate

Ini boundary yang bagus:

```rust
Scanner → Candidate
Safety → SafetyValidated
Policy → PolicyApproval
Planner → PlannedOperation
Authorization → AuthorizedPlan
Executor → OperationResult
```

Jadi:

```text
Candidate
```

tidak pernah sampai ke Executor.

---

# 81. Authorization

Authorization menjawab:

> “Siapa yang meminta ini dan apakah role tersebut boleh meminta operation tersebut?”

Misalnya:

```rust
pub struct AuthorizationProof {
    pub role: Role,
    pub capabilities: Capabilities,
    pub request_id: RequestId,
}
```

Role:

```rust
pub enum Role {
    Root,
    System,
    Shell,
    Unknown,
}
```

Capability:

```rust
pub enum Capability {
    ReadStatus,
    Explain,
    StartCleanup,
    DeepCleanup,
    Trim,
    F2fsMaintenance,
    ZramMaintenance,
    ReloadConfig,
    RestartDaemon,
    StopDaemon,
}
```

---

# 82. Authorization tidak boleh mengubah Safety

Ini harus eksplisit.

Bahkan:

```text
Root
```

hanya berarti:

```text
allowed to request
```

bukan:

```text
allowed to bypass safety
```

Jadi tidak ada:

```rust
if role == Root {
    skip_safety();
}
```

sama sekali.

---

# 83. AuthorizedPlan

```rust
pub struct AuthorizedPlan {
    plan: CleanupPlan,
    proof: AuthorizationProof,
}
```

Constructor:

```rust
pub(crate) fn authorize(
    plan: CleanupPlan,
    proof: AuthorizationProof,
) -> Result<AuthorizedPlan, AuthorizationError>
```

Executor hanya menerima:

```rust
&AuthorizedPlan
```

---

# 84. Tapi authorization perlu terjadi kapan?

Untuk daemon automatic cleanup:

```text
Scheduler
   ↓
internal trusted command
   ↓
policy
   ↓
plan
   ↓
authorization
   ↓
executor
```

Untuk IPC:

```text
IPC peer
   ↓
authenticate
   ↓
authorize request
   ↓
create job
   ↓
same cleanup pipeline
```

Yang penting **authorization IPC tidak menjadi pengganti Safety**.

---

# 85. Job adalah lifecycle object

```rust
pub struct Job {
    pub id: JobId,
    pub trigger: JobTrigger,
    pub profile: CleanupProfile,
}
```

State dipisahkan:

```rust
pub enum JobState {
    Queued,
    Preparing,
    Scanning,
    Planning,
    Authorized,
    Executing,
    Verifying,
    Completed,
    Cancelled,
    Failed,
}
```

---

# 86. Jangan pakai `bool cancelled`

Hindari:

```rust
struct Job {
    cancelled: bool,
}
```

Karena nanti berkembang menjadi:

```text
cancelled
paused
thermal
shutdown
user_request
storage_changed
```

Gunakan:

```rust
pub enum CancelReason {
    Shutdown,
    ScreenOn,
    Thermal,
    UserRequest,
    StorageChanged,
}
```

dan cancellation token sebagai runtime mechanism.

---

# 87. CancellationToken

Secara implementation:

```rust
pub struct CancellationToken {
    inner: Arc<AtomicBool>,
}
```

atau mekanisme yang lebih sesuai runtime.

Tetapi domain tidak perlu tahu implementasinya.

Expose trait:

```rust
pub trait Cancellation {
    fn is_cancelled(&self) -> bool;
}
```

Scanner dan Executor cukup bergantung pada trait tersebut jika memang perlu.

---

# 88. `Arc` jangan disebar sembarangan

Saya ingin menghindari:

```rust
Arc<Mutex<Everything>>
```

sebagai architecture.

Contoh buruk:

```rust
Arc<Mutex<Config>>
Arc<Mutex<Scheduler>>
Arc<Mutex<Executor>>
Arc<Mutex<Scanner>>
```

Ini biasanya tanda boundary belum jelas.

Gunakan ownership biasa, `Arc` hanya untuk resource yang memang shared:

```text
config snapshot
cancellation token
immutable platform capability
logger
```

---

# 89. `Mutex` hanya untuk state yang benar-benar mutable

Scheduler:

```rust
struct Scheduler {
    state: DaemonState,
    conditions: RuntimeConditions,
    active_job: Option<Job>,
}
```

Kalau semuanya hidup dalam satu event-loop thread:

**tidak perlu Mutex.**

Ini justru sangat cocok dengan architecture event-driven yang kita inginkan.

---

# 90. Event loop single owner

Model:

```text
epoll
  ↓
SystemEvent
  ↓
Scheduler
```

Scheduler menjadi satu-satunya owner state lifecycle.

Tidak:

```text
thread A modifies scheduler
thread B modifies scheduler
thread C modifies scheduler
```

Ini mengurangi race secara drastis.

---

# 91. Cleanup execution bagaimana?

Ada dua pilihan.

### Model A — blocking job

```text
event loop
   ↓
execute cleanup
   ↓
event loop blocked
```

Tidak ideal karena cancellation/event handling menjadi lambat.

### Model B — worker job

Saya rekomendasikan:

```text
event loop
    │
    ├── scheduler
    │
    └── cleanup worker
```

Tetapi worker tidak memiliki authority scheduler.

---

# 92. Worker boundary

```rust
pub struct JobRunner {
    scanner: Scanner,
    policy: PolicyEngine,
    planner: Planner,
    executor: Executor,
}
```

Worker menerima:

```rust
pub struct JobRequest {
    pub job_id: JobId,
    pub profile: CleanupProfile,
    pub config: ConfigSnapshot,
}
```

Kemudian:

```text
scan
 ↓
safety
 ↓
policy
 ↓
plan
 ↓
authorization
 ↓
execute
 ↓
report
```

---

# 93. Worker tidak boleh mengubah scheduler state

Worker mengirim:

```rust
pub enum JobEvent {
    Started(JobId),
    Scanning(JobId),
    Planned(JobId, PlanSummary),
    Progress(JobProgress),
    Completed(JobReport),
    Cancelled(JobId, CancelReason),
    Failed(JobId, JobError),
}
```

Scheduler menerima event tersebut.

Jadi:

```text
Worker
  ↓ events
Scheduler
  ↓ state transition
DaemonState
```

Ini sangat bersih.

---

# 94. Jangan kirim object besar lewat event

Jangan:

```rust
JobEvent::Completed {
    entire CleanupPlan,
    entire Candidate list,
    entire filesystem metadata
}
```

Event sebaiknya membawa:

```text
IDs
counters
summary
status
error
```

Detail bisa diambil dari report store bila diperlukan.

---

# 95. Progress

Untuk daemon cleaner, progress bisa:

```rust
pub struct JobProgress {
    pub job_id: JobId,
    pub completed_operations: usize,
    pub total_operations: usize,
    pub bytes_removed: u64,
}
```

Tetapi jangan menjadikan progress sebagai security state.

---

# 96. Error hierarchy

Saya sarankan tiga level.

### Infrastructure

```rust
FsError
KernelError
IpcError
PlatformError
```

### Domain

```rust
SafetyViolation
PolicyError
PlannerError
```

### Job

```rust
JobError
```

yang merangkum semantic outcome.

---

# 97. Jangan `Box<dyn Error>` di seluruh project

Hindari:

```rust
Result<T, Box<dyn Error>>
```

untuk core.

Gunakan typed error:

```rust
pub enum ScanError { ... }
pub enum SafetyViolation { ... }
pub enum PlannerError { ... }
pub enum ExecutionError { ... }
```

Kemudian `thiserror` boleh dipakai untuk mengurangi boilerplate.

---

# 98. Error jangan kehilangan context

Misalnya:

```rust
ExecutionError::PermissionDenied
```

kurang informasi untuk report.

Lebih baik:

```rust
ExecutionError::PermissionDenied {
    operation: OperationId,
    target: TargetId,
}
```

Path display bisa ditambahkan untuk diagnostic layer jika aman.

---

# 99. Config snapshot

Config jangan dibaca dari file sepanjang execution.

Gunakan:

```rust
pub struct ConfigSnapshot {
    pub generation: u64,
    pub config: Arc<ValidatedConfig>,
}
```

Job mengambil snapshot:

```text
generation = 42
```

Kemudian config reload:

```text
generation = 43
```

Job yang sedang berjalan tetap:

```text
42
```

Job berikutnya:

```text
43
```

Ini membuat behavior reproducible.

---

# 100. Trait boundary yang saya rekomendasikan

Scanner:

```rust
pub trait Scanner {
    fn scan(
        &self,
        request: &ScanRequest,
        ctx: &ScanContext,
    ) -> Result<ScanResult, ScanError>;
}
```

Policy:

```rust
pub trait PolicyEngine {
    fn evaluate(
        &self,
        candidate: &SafetyValidated,
        ctx: &PolicyContext,
    ) -> PolicyDecision;
}
```

Planner:

```rust
pub trait Planner {
    fn build_plan(
        &self,
        approvals: &[PolicyApproval],
        ctx: &PlannerContext,
    ) -> Result<CleanupPlan, PlannerError>;
}
```

Executor:

```rust
pub trait PlanExecutor {
    fn execute(
        &self,
        plan: &AuthorizedPlan,
        ctx: &ExecutionContext,
    ) -> ExecutionReport;
}
```

---

# 101. Jangan over-trait

Namun jangan membuat:

```rust
trait ScannerFactory
trait ScannerProvider
trait ScannerResolver
trait ScannerStrategy
trait ScannerBackend
trait ScannerAdapter
```

untuk semuanya.

Untuk v1:

```text
Scanner
PolicyEngine
Planner
Executor
Platform
```

sudah cukup.

Trait digunakan ketika memang ada boundary/abstraction yang bernilai.

---

# 102. Platform trait

Ini trait yang memang masuk akal:

```rust
pub trait Platform {
    fn android_version(&self) -> AndroidVersion;
    fn capabilities(&self) -> AndroidCapabilities;
    fn users(&self) -> Result<Vec<User>, PlatformError>;
    fn storage_state(
        &self,
        user: UserId,
    ) -> Result<StorageSnapshot, PlatformError>;
}
```

Kemudian implementasi:

```text
AndroidPlatform
```

---

# 103. Filesystem trait

Untuk testing, bagus jika filesystem abstraction bisa diganti.

```rust
pub trait FileSystem {
    fn open_root(
        &self,
        target: &TargetRoot,
    ) -> Result<TrustedRoot, FsError>;
}
```

Namun jangan membuat abstraction terlalu generic seperti:

```rust
trait FileSystem {
    fn do_anything(...)
}
```

API harus tetap security-specific.

---

# 104. Kernel trait

```rust
pub trait KernelMaintenance {
    fn capabilities(&self) -> KernelCapabilities;

    fn trim(
        &self,
        target: &TrimTarget,
    ) -> Result<TrimResult, KernelError>;

    fn f2fs(
        &self,
        target: &F2fsTarget,
    ) -> Result<F2fsResult, KernelError>;

    fn zram(
        &self,
        target: &ZramTarget,
    ) -> Result<ZramResult, KernelError>;
}
```

Tidak:

```rust
write_sysfs(path, value)
```

sebagai public generic API.

---

# 105. Dependency injection

`main.rs` akhirnya menyusun semuanya:

```text
main
 │
 ├── load config
 ├── initialize logger
 ├── initialize platform
 ├── initialize filesystem
 ├── initialize kernel
 ├── initialize scanner
 ├── initialize policy
 ├── initialize planner
 ├── initialize executor
 ├── initialize scheduler
 └── start runtime
```

Ini membuat dependency terlihat jelas.

---

# 106. Hindari global singleton

Jangan:

```rust
static GLOBAL_CONFIG: ...
static GLOBAL_EXECUTOR: ...
static GLOBAL_PLATFORM: ...
```

Daemon lebih mudah dites kalau semua dependency dimiliki object utama.

---

# 107. Struktur crate final

Saya sekarang lebih menyukai struktur ini:

```text
src/
├── main.rs
│
├── app/
│   ├── mod.rs
│   ├── controller.rs
│   ├── daemon.rs
│   ├── job_runner.rs
│   └── lifecycle.rs
│
├── domain/
│   ├── mod.rs
│   ├── ids.rs
│   ├── candidate.rs
│   ├── target.rs
│   ├── operation.rs
│   ├── plan.rs
│   ├── decision.rs
│   ├── policy.rs
│   ├── job.rs
│   └── error.rs
│
├── fs/
│   ├── mod.rs
│   ├── handle.rs
│   ├── path.rs
│   ├── identity.rs
│   ├── metadata.rs
│   ├── directory.rs
│   └── syscalls/
│
├── scanner/
│   ├── mod.rs
│   ├── scanner.rs
│   ├── walker.rs
│   ├── entry.rs
│   ├── budget.rs
│   └── error.rs
│
├── safety/
│   ├── mod.rs
│   ├── validator.rs
│   ├── boundary.rs
│   ├── authorization.rs
│   └── proof.rs
│
├── policy/
│   ├── mod.rs
│   ├── engine.rs
│   ├── age.rs
│   ├── runtime.rs
│   ├── storage.rs
│   ├── budget.rs
│   └── trace.rs
│
├── planner/
│   ├── mod.rs
│   ├── planner.rs
│   ├── ordering.rs
│   ├── dependency.rs
│   ├── dedup.rs
│   ├── budget.rs
│   ├── estimate.rs
│   └── validation.rs
│
├── executor/
│   ├── mod.rs
│   ├── executor.rs
│   ├── dispatch.rs
│   ├── context.rs
│   ├── filesystem/
│   └── maintenance/
│
├── scheduler/
│   ├── mod.rs
│   ├── scheduler.rs
│   ├── state.rs
│   ├── events.rs
│   ├── readiness.rs
│   ├── conditions.rs
│   ├── cancellation.rs
│   └── retry.rs
│
├── runtime/
│   ├── mod.rs
│   ├── event_loop.rs
│   ├── timer.rs
│   ├── signals.rs
│   ├── uevent.rs
│   ├── lock.rs
│   └── paths.rs
│
├── platform/
│   ├── mod.rs
│   ├── platform.rs
│   ├── android.rs
│   ├── users.rs
│   ├── packages.rs
│   ├── encryption.rs
│   ├── storage.rs
│   ├── properties.rs
│   └── selinux.rs
│
├── kernel/
│   ├── mod.rs
│   ├── capabilities.rs
│   ├── filesystem.rs
│   ├── psi.rs
│   ├── thermal.rs
│   ├── cgroup/
│   ├── scheduler/
│   ├── f2fs.rs
│   ├── trim.rs
│   └── zram.rs
│
├── ipc/
│   ├── mod.rs
│   ├── protocol.rs
│   ├── framing.rs
│   ├── server.rs
│   ├── client.rs
│   ├── auth.rs
│   └── permissions.rs
│
├── config/
│   ├── mod.rs
│   ├── config.rs
│   └── validation.rs
│
├── telemetry/
│   ├── mod.rs
│   ├── logger.rs
│   ├── metrics.rs
│   └── report.rs
│
└── cli/
    ├── mod.rs
    ├── args.rs
    ├── command.rs
    ├── output.rs
    └── diagnostics.rs
```

---

# 108. Tapi jangan langsung membuat semua file

Ini penting.

**Jangan mulai rewrite dengan membuat 80 file kosong.**

Kita implement bertahap berdasarkan dependency.

Urutan compile yang saya rekomendasikan:

```text
Phase 1
domain
   ↓
Phase 2
fs
   ↓
Phase 3
scanner
   ↓
Phase 4
safety
   ↓
Phase 5
policy
   ↓
Phase 6
planner
   ↓
Phase 7
executor
   ↓
Phase 8
platform
   ↓
Phase 9
kernel
   ↓
Phase 10
scheduler/runtime
   ↓
Phase 11
IPC
   ↓
Phase 12
CLI
   ↓
Phase 13
Android packaging
```

---

# 109. Kenapa `domain` dulu?

Karena kalau domain berubah setelah Executor selesai, kita akan membongkar semuanya.

Sedangkan:

```text
domain
 ↓
contracts
 ↓
implementations
```

membuat dependency stabil.

---

# 110. Milestone pertama

Project kosong seharusnya pertama kali hanya memiliki:

```text
src/
├── main.rs
├── lib.rs
└── domain/
```

Dan target:

```bash
cargo test
cargo clippy
cargo build
```

semuanya bersih.

Belum ada:

```text
Android
epoll
UDS
syscall
F2FS
```

Sama sekali.

---

# 111. Domain milestone

Minimal domain selesai ketika kita sudah bisa membuat:

```text
TargetRoot
Candidate
SafetyValidated
PolicyApproval
Operation
PlannedOperation
CleanupPlan
Job
```

dan mengetes:

```text
valid IDs
valid paths
valid operation types
plan dependencies
budget
state transitions
```

tanpa device Android.

---

# 112. Setelah itu baru filesystem

`fs/` menjadi fondasi security.

Kita test:

```text
RelativePath
ObjectIdentity
TrustedRoot
DirectoryHandle
NOFOLLOW
openat
fstatat
unlinkat
```

Ini adalah bagian yang paling layak mendapat **code review/security review** paling ketat.

---

# 113. Lalu Scanner

Scanner hanya perlu membuktikan:

```text
"Ini object X,
berada di target Y,
dengan identity Z,
dan tipe T."
```

Belum boleh ada:

```text
delete
trim
f2fs
zram
```

---

# 114. Kemudian Safety

Safety mengubah:

```text
Candidate
```

menjadi:

```text
SafetyValidated
```

hanya jika semua invariant terpenuhi.

Jadi secara type flow:

```text
Candidate
   │
   ├── unsafe → Reject
   │
   └── safe
        ↓
 SafetyValidated
```

---

# 115. Baru Policy

Policy:

```text
SafetyValidated
        ↓
    PolicyEngine
        ↓
 ┌──────┼───────┐
 ▼      ▼       ▼
Allow Reject   Defer
```

Ini membuat alasan penolakan dapat dijelaskan ke user.

---

# 116. Baru Planner

Planner menghasilkan:

```text
CleanupPlan
```

yang sudah:

```text
typed
bounded
ordered
deduplicated
dependency-valid
```

---

# 117. Baru Executor

Dan hanya pada titik itu kita mengaktifkan:

```text
unlinkat
```

Ini bagus secara security architecture karena **syscall destruktif muncul sangat akhir dalam dependency graph**.

---

# 118. Prinsip besar rewrite

Pada akhirnya saya ingin codebase baru memiliki property ini:

```text
Tidak ada satu function pun
yang sekaligus:

discover
+ decide
+ authorize
+ delete
```

Kalau kita menemukan function:

```rust
fn clean_cache(path: &Path) { ... }
```

yang melakukan semuanya, itu tanda architecture bocor.

Lebih sehat:

```text
scan()
validate()
evaluate()
plan()
authorize()
execute()
verify()
report()
```

---

## Blueprint akhir pipeline

```text
                         ┌──────────────┐
                         │ Android      │
                         │ Platform     │
                         └──────┬───────┘
                                │
                         Target Catalog
                                │
                                ▼
┌─────────┐              ┌──────────────┐
│ Runtime │─────────────►│   Scheduler  │
└─────────┘              └──────┬───────┘
                                │
                              Job
                                │
                                ▼
                           ┌─────────┐
                           │ Scanner │
                           └────┬────┘
                                │ Candidate
                                ▼
                           ┌────────┐
                           │ Safety │
                           └────┬───┘
                                │ SafetyValidated
                                ▼
                           ┌────────┐
                           │ Policy │
                           └────┬───┘
                                │ PolicyApproval
                                ▼
                           ┌─────────┐
                           │ Planner │
                           └────┬────┘
                                │ CleanupPlan
                                ▼
                         Plan Validation
                                │
                                ▼
                         Authorization
                                │
                                ▼
                      ┌─────────────────┐
                      │    Executor     │
                      └────────┬────────┘
                               │
                    ┌──────────┼──────────┐
                    ▼          ▼          ▼
                   FS        Kernel     Android
                    │          │          │
                    └──────────┼──────────┘
                               ▼
                          Verification
                               │
                               ▼
                             Report
```

**Ini sekarang sudah cukup matang untuk masuk ke tahap coding blueprint.**

Tahap berikutnya yang paling tepat adalah **mendesain `domain/` secara konkret**: isi setiap `struct`/`enum`, visibility (`pub` vs `pub(crate)`), constructor yang boleh membuat `SafetyValidated`/`PolicyApproval`/`CleanupPlan`, state-transition API, serta dependency `Cargo.toml`. Dari sana kita bisa mulai membangun project kosong **file demi file**, bukan lagi sekadar arsitektur konseptual.
