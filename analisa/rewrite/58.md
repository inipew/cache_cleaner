58.md — Authorization Implementation Contract, Operation-level Authorization, Capability Intersection, Request Origin, Role Mapping, Scope Restriction, Protected Target Enforcement, Budget Authorization, Generation Binding, Plan Expiration, Final Approval, Authorization Decision, Capability Downgrade, IPC/CLI/Scheduler Authorization, Audit, Fail-Closed Behavior, dan AuthorizedPlan Construction Contract

Tujuan

Dokumen ini mengunci implementasi Authorization secara konkret.

Authorization adalah boundary yang menjawab:

```
"Apakah operasi yang sudah direncanakan
 boleh benar-benar dieksekusi?"
```

Alur:

```
Request
   │
   ▼
Identity
   │
   ▼
Role
   │
   ▼
Capability
   │
   ▼
PlannedPlan
   │
   ▼
Scope Check
   │
   ▼
Policy Check
   │
   ▼
Generation Check
   │
   ▼
Budget Check
   │
   ▼
Runtime Readiness
   │
   ▼
Final Safety
   │
   ▼
AuthorizedPlan
   │
   ▼
Executor
```

1. Authorization Bukan Policy

Policy menjawab:

```
"candidate memenuhi aturan cleanup?"
```

Authorization menjawab:

```
"request ini boleh mengeksekusi operation tersebut?"
```

2. Authorization Bukan Safety

Safety menjawab:

```
"filesystem operation ini aman?"
```

Authorization menjawab:

```
"operation ini memiliki authority?"
```

3. Tiga Boundary

```
Safety
   │
   ├── scope integrity
   ├── identity
   └── filesystem safety

Policy
   │
   ├── eligibility
   ├── age
   └── exclusions

Authorization
   │
   ├── identity
   ├── capability
   ├── role
   └── operation scope
```

4. Authorization Input

Minimal:

```
RequestContext
PlannedPlan
EffectivePolicy
ConfigSnapshot
CatalogSnapshot
PlatformSnapshot
RuntimeReadiness
CapabilityCatalog
```

5. Authorization Output

Hanya dua hasil utama:

```
AuthorizedPlan
AuthorizationDenied
```

6. AuthorizedPlan

AuthorizedPlan harus:

```
immutable
```

7. AuthorizedPlan Adalah Closed Set

Jika:

```
Plan = {O1, O2, O3}
```

maka:

```
AuthorizedPlan = {O1, O2, O3}
```

atau subset yang secara eksplisit diizinkan.

Executor tidak boleh menambahkan:

```
O4
```

8. No Expansion

Authorization hanya dapat:

```
preserve
restrict
```

tidak:

```
expand
```

9. Capability

Capability adalah:

```
typed authority
```

bukan:

```
boolean arbitrary
```

10. Capability Examples

```
ExecuteAppCacheCleanup
ExecuteCodeCacheCleanup
ExecuteTemporaryCleanup
ExecuteTrim
ExecuteF2fsMaintenance
ExecuteZramMaintenance
```

11. Capability Tidak Sama Dengan Permission

Linux permission:

```
filesystem/platform constraint
```

Capability:

```
application-level authority
```

12. Both Required

```
Application Capability
      │
      +
Platform Permission
      │
      ▼
   Allowed
```

13. Capability Source

Capability tidak boleh datang dari:

```
client request
```

14. Client Cannot Say

Forbidden:

```
capability = Admin
```

15. Server Derives

```
RequestIdentity
      │
      ▼
   Role Mapping
      │
      ▼
   Capability Set
```

16. Request Origin

Origin harus diketahui:

```
CLI
IPC
Scheduler
InternalRecovery
```

17. Origin Is Not Trust

IPC client tidak otomatis:

```
trusted
```

18. CLI Is Not Root of Trust

CLI juga melewati:

```
authorization
```

19. Scheduler

Scheduler memiliki:

```
server-controlled authority
```

20. Internal Recovery

Recovery hanya boleh melakukan:

```
explicitly defined recovery actions
```

21. Role

Role ditentukan server-side.

22. Example Roles

    Operator
    Administrator
    Scheduler
    Recovery
    ReadOnly

23. Role Mapping

Conceptual:

```
Identity
   │
   ▼
RoleResolver
   │
   ▼
Role
   │
   ▼
CapabilitySet
```

24. No Client Role

Request tidak boleh membawa:

```
"role=Administrator"
```

sebagai authority.

25. Identity

Identity dapat berasal dari:

```
kernel peer credentials
trusted process context
internally generated scheduler identity
```

26. IPC Identity

IPC server mendapatkan identity dari:

```
operating system
```

27. PID

PID saja bukan:

```
sufficient authorization proof
```

28. UID/GID

UID/GID dapat menjadi:

```
input to role mapping
```

29. Supplementary Groups

Jika digunakan:

```
server evaluates
```

30. Environment Variables

Tidak boleh menjadi:

```
authority source
```

31. Config

Config tidak boleh memberikan:

```
arbitrary capability escalation
```

32. Capability Catalog

Capability set harus:

```
explicit
```

33. Capability Intersection

Effective capability:

```
RoleCapabilities
     ∩
PlatformCapabilities
     ∩
ConfiguredCapabilities
     ∩
RequestScope
```

34. Result

```
EffectiveCapabilities
```

35. Empty Intersection

Jika:

```
capability set = ∅
```

operation:

```
denied
```

36. Platform Restriction

Jika platform tidak mendukung:

```
operation denied
```

37. Config Restriction

Config dapat:

```
disable capability
```

38. Config Cannot Create Platform Capability

Jika platform:

```
Unsupported
```

config tidak dapat:

```
enable
```

39. Example

```
Config:
    EnableZram = true

Platform:
    ZRAM unsupported

Result:
    ZRAM capability unavailable
```

40. Capability Downgrade

Jika platform capability berubah:

```
old capability
     │
     ▼
invalidated
     │
     ▼
new capability set
```

41. No Capability Caching Forever

Capabilities harus:

```
generation-bound
```

42. Capability Generation

Capability state terkait:

```
PlatformGeneration
```

43. Generation

Authorization harus memeriksa:

```
ConfigGeneration
CatalogGeneration
PlatformGeneration
```

44. Config Generation

Policy/config yang dipakai Planner harus sama dengan:

```
authorization context
```

45. Catalog Generation

Target mapping harus sama.

46. Platform Generation

Capabilities/readiness harus sama.

47. Mismatch

Any critical mismatch:

```
AuthorizationDenied
reason = StaleGeneration
```

48. Replan

Stale plan:

```
cannot simply re-authorize
```

49. Correct Flow

```
Stale
  │
  ▼
fresh snapshots
  │
  ▼
rescan
  │
  ▼
reclassify
  │
  ▼
replan
  │
  ▼
reauthorize
```

50. Scope

Authorization harus memeriksa:

```
TargetId
PackageId
UserId
StorageClass
RelativePath
OperationType
```

51. Scope Tree

```
System
  │
  └── User
        │
        └── Package
              │
              └── Target
                    │
                    └── RelativePath
```

52. Scope Restriction

Capability dapat dibatasi:

```
user
package
target class
```

53. Example

Role:

```
Operator
```

Capability:

```
ExecuteAppCacheCleanup
```

Scope:

```
User 0 only
```

Maka:

```
User 10
```

ditolak.

54. Package Restriction

Capability dapat dibatasi:

```
PackageId
```

55. Example

Allowed:

```
package.a
```

Denied:

```
package.b
```

56. Target Restriction

Capability:

```
AppCache
```

tidak otomatis memberi:

```
CodeCache
```

57. Operation Restriction

Capability:

```
RemoveFile
```

tidak otomatis memberi:

```
RemoveEmptyDirectory
```

58. Principle

Capability harus:

```
narrow
```

59. Protected Target

Authorization harus mengecek:

```
target.protected
```

60. Protected

Jika:

```
Protected
```

maka:

```
Denied
```

61. Policy Cannot Override

Tidak ada:

```
force=true
```

yang dapat membatalkan protection.

62. Scope Escalation

Request tidak boleh mengubah:

```
TargetId
```

setelah authorization.

63. Operation Mutation

AuthorizedPlan tidak boleh dimodifikasi setelah approval.

64. Immutable

Gunakan immutable representation.

65. Type-state

Ideal:

```
PlannedPlan
    │
    ▼
AuthorizedPlan
```

constructor hanya melalui:

```
AuthorizationService
```

66. Private Constructor

AuthorizedPlan creation harus:

```
restricted
```

67. No Manual Construction

Executor tidak boleh membuat:

```
AuthorizedPlan
```

68. Authorization Service

Conceptual:

```
authorize(
    RequestContext,
    PlannedPlan,
    AuthorizationContext
)
    ->
AuthorizationResult
```

69. Authorization Context

```
AuthorizationContext {
    identity
    role
    capabilities
    policy
    generations
    readiness
    budget
    now
}
```

70. Time

Authorization harus memiliki:

```
explicit current time
```

untuk deterministic testing.

71. Plan Expiration

AuthorizedPlan dapat memiliki:

```
expires_at
```

72. Why

Authorization tidak boleh berlaku:

```
indefinitely
```

73. Expiration

Jika:

```
now > expires_at
```

result:

```
Expired
```

74. Expired Plan

Tidak boleh:

```
execute
```

75. Reauthorization

Expired plan:

```
normally replan + reauthorize
```

76. Clock

Expiration memakai:

```
trusted WallClock
```

77. Duration Timeout

Execution timeout memakai:

```
MonotonicClock
```

78. Authorization Lifetime

Lifetime harus:

```
bounded
```

79. Default

Authorization validity sebaiknya:

```
short-lived
```

80. Runtime Readiness

Authorization memeriksa kondisi:

```
Screen
Thermal
User
Storage
Package activity
Platform state
```

81. Readiness

States:

```
Ready
Deferred
Blocked
Unknown
```

82. Deferred

Jika policy mengizinkan retry:

```
Deferred
```

83. Blocked

Jika condition tidak memungkinkan:

```
Denied
atau
Deferred
```

sesuai semantics.

84. Unknown Readiness

Critical unknown:

```
no destructive authorization
```

85. Runtime Changes

Readiness dapat berubah setelah authorization.

86. Final Safety

Executor tetap melakukan:

```
final safety check
```

87. Authorization Is Not Final Filesystem Proof

Authorization tidak menggantikan:

```
identity revalidation
```

88. Budget Authorization

Authorization harus memastikan:

```
operations <= max_operations
```

89. Byte Budget

Ensure:

```
estimated_bytes <= authorized byte budget
```

90. Duration Budget

Ensure:

```
estimated duration <= authorized planning budget
```

91. Budget Cannot Expand

Executor tidak boleh:

```
exceed authorized budget
```

92. Actual Budget

Executor menggunakan:

```
runtime execution budget
```

93. Difference

```
Authorized Budget
     │
     ▼
Execution Budget
```

94. Budget Exhaustion

Jika budget habis:

```
stop
```

95. Partial Execution

Already executed operations tetap:

```
reported
```

96. Remaining Operations

Tidak otomatis:

```
unauthorized retry
```

97. Retry

Retry membutuhkan:

```
new authorization
```

jika generation/readiness berubah.

98. Operation-level Authorization

Authorization harus memeriksa:

```
setiap operation
```

99. Plan-level Authorization

Plan-level check:

```
identity
generation
scope
budget
```

100. Operation-level Check

Per operation:

```
capability
target
operation type
protection
scope
```

101. Both

```
Plan
   │
   ▼
Plan-level checks
   │
   ▼
Operation-level checks
   │
   ▼
AuthorizedPlan
```

102. Any Failure

One critical operation failure dapat:

```
reject entire plan
```

103. Partial Authorization

Allowed hanya jika:

```
explicitly configured
```

104. Default

```
fail closed
```

105. Why

Mixed authorization dapat menyebabkan:

```
unexpected partial cleanup
```

106. Explicit Partial Mode

Jika tersedia:

```
result harus immutable subset
```

107. Subset

```
PlannedPlan:
    O1 O2 O3 O4

Authorization:
    O1 O2 allowed
    O3 O4 denied

AuthorizedPlan:
    O1 O2
```

108. Denied Operations

Must have:

```
reason codes
```

109. Reason Examples

```
CapabilityMissing
ProtectedTarget
ScopeViolation
GenerationMismatch
BudgetExceeded
Expired
ReadinessBlocked
PlatformUnsupported
ConfigDisabled
IdentityMismatch
```

110. Security Reason

Internal reason:

```
sensitive details
```

should not always be exposed externally.

111. User-facing Reason

Can be:

```
"Operation is not authorized."
```

112. Audit Reason

Internal audit can retain:

```
typed denial reason
```

113. Request Origin Audit

Record:

```
origin
```

114. Identity Audit

Record:

```
identity reference
```

115. Capability Audit

Record:

```
capability decision
```

116. Plan Audit

Record:

```
PlanId
```

117. Operation Audit

Record:

```
OperationId
```

118. Generation Audit

Record:

```
ConfigGeneration
CatalogGeneration
PlatformGeneration
```

119. Timestamp

Record:

```
authorization time
```

120. Audit Sequence

Audit entries require:

```
monotonic sequence
```

121. No Sensitive Capability Secrets

Capability should be:

```
symbolic
```

122. Tokens

If future token-based authorization is introduced:

```
tokens must not be persisted in plaintext audit
```

123. IPC

IPC request:

```
Parse
  │
  ▼
Authenticate peer
  │
  ▼
Resolve role
  │
  ▼
Create RequestContext
  │
  ▼
ApplicationController
  │
  ▼
Authorization
```

124. IPC Cannot Bypass

Forbidden:

```
IPC
  ───────────────► Executor
```

125. CLI

```
CLI
  │
  ▼
AppCommand
  │
  ▼
Authorization
```

126. Scheduler

Scheduler creates:

```
internal RequestContext
```

127. Scheduler Authority

Scheduler capability must be:

```
explicit
```

128. Recovery

Recovery capability only covers:

```
reconciliation
state repair
stale marking
```

not:

```
arbitrary cleanup
```

129. Internal Commands

Internal commands must still have:

```
typed capability
```

130. No "Internal = Trusted"

Being internal is not:

```
universal privilege
```

131. Capability Separation

Example:

```
ExecuteCacheCleanup
```

does not imply:

```
ExecuteKernelMaintenance
```

132. Kernel Maintenance

Requires separate capability:

```
ExecuteTrim
ExecuteF2fsMaintenance
ExecuteZramMaintenance
```

133. Principle

High-risk capability:

```
separate
```

134. Capability Hierarchy

Avoid implicit:

```
Admin
   ↓
everything
```

Prefer:

```
Capability A
Capability B
Capability C
```

135. Least Authority

Every actor receives:

```
minimum required capability
```

136. Scheduler

Scheduler needs:

```
scheduled cleanup capability
```

not:

```
arbitrary filesystem capability
```

137. Recovery

Recovery needs:

```
state reconciliation capability
```

not:

```
deletion capability
```

138. ReadOnly

ReadOnly role:

```
Status
Explain
Inspect
```

139. ReadOnly Must Not

```
Cleanup
KernelMaintenance
```

140. Operator

Operator may receive:

```
approved cleanup capabilities
```

141. Administrator

Administrator may receive broader:

```
operational capabilities
```

but still:

```
safety gates remain mandatory
```

142. Admin Cannot Bypass Safety

Critical invariant:

```
Admin
  ≠
Safety bypass
```

143. Safety Override

Forbidden:

```
disable_safety=true
```

144. Policy Override

Any emergency mode must be:

```
separately typed
separately authorized
separately audited
```

145. No Boolean Bypass

Avoid:

```
force
unsafe
skip_validation
ignore_generation
```

146. Emergency Mode

If eventually required:

```
EmergencyAuthorization
```

with:

```
explicit capability
strict scope
expiration
audit
```

147. Even Emergency

Still cannot violate:

```
kernel safety
filesystem boundary
type safety
```

148. Plan Fingerprint

Authorization can bind:

```
PlanFingerprint
```

149. Why

Prevents mutation between:

```
review
execute
```

150. Fingerprint Mismatch

If plan changes:

```
authorization invalid
```

151. AuthorizedPlan Fingerprint

Store:

```
PlanFingerprint
```

152. Operation Fingerprint

Each operation can have:

```
deterministic fingerprint
```

153. Executor

Before operation:

```
verify authorized operation identity
```

154. Plan Mutation Attempt

If mutation detected:

```
reject
```

155. Persistence

AuthorizedPlan may be persisted for crash recovery.

156. Persistence Security

Do not persist:

```
raw authentication secrets
```

157. Persisted Authorization

Store:

```
identity reference
role
capabilities
generation
expiration
plan fingerprint
```

158. Capability Revalidation

After restart:

```
persisted capability must not automatically become valid forever.
```

159. Recovery

Revalidate:

```
current identity
current platform capability
current generation
```

160. If Changed

Plan:

```
Stale
```

161. No Blind Resume

```
persisted AuthorizedPlan
         X
         ▼
      Executor
```

162. Correct

```
persisted state
      │
      ▼
current snapshots
      │
      ▼
reconciliation
      │
      ▼
reauthorization if valid
      │
      ▼
execution
```

163. However

If external mutation may already have occurred:

```
do not simply re-execute.
```

164. Unknown Outcome

Unknown operation:

```
requires reconciliation
```

165. No Replay

Unknown:

```
not automatically retryable
```

166. Authorization Decision Type

Conceptual:

```
AuthorizationDecision {
    Allowed
    Denied
    Deferred
    Stale
    Expired
}
```

167. Allowed

Produces:

```
AuthorizedPlan
```

168. Denied

No execution.

169. Deferred

Scheduler may retry later.

170. Stale

Fresh snapshots required.

171. Expired

New authorization required.

172. Determinism

Same:

```
RequestContext
Plan
snapshots
time
```

must produce:

```
same decision
```

173. No Hidden State

Authorization should not depend on:

```
mutable global flags
```

174. No Randomness

Authorization decision:

```
deterministic
```

175. Authorization Failure

Fail closed.

176. Authorization Service Failure

If service itself cannot establish:

```
identity
capability
generation
scope
```

then:

```
no destructive authorization
```

177. IPC Disconnect

Disconnect does not automatically mean:

```
authorized operations cancelled
```

unless request semantics define it.

178. Cancellation

Cancellation is separate from:

```
authorization
```

179. Cancelled Job

Executor stops according to:

```
cancellation contract
```

180. New Authorization

Cancellation cannot:

```
grant capability
```

181. Scope Validation

Check all operations:

```
target exists in catalog
target belongs to request scope
relative path is valid
operation type is allowed
```

182. Path

Authorization should never parse arbitrary filesystem strings as authority.

183. Scope Token

Use:

```
TargetId
```

instead of:

```
raw absolute path
```

184. Package Scope

Use:

```
PackageId
```

185. User Scope

Use:

```
UserId
```

186. Storage Scope

Use:

```
StorageClass
```

187. Operation Scope

Use:

```
OperationType
```

188. Example

Request:

```
Cleanup
User 0
AppCache
```

Plan:

```
User 0
Package A
AppCache
RemoveFile
```

Allowed if:

```
capability includes
ExecuteAppCacheCleanup
```

189. Example

Plan:

```
User 10
AppCache
```

Request:

```
User 0
```

Result:

```
ScopeViolation
```

190. Example

Plan:

```
CodeCache
```

Capability:

```
ExecuteAppCacheCleanup
```

Result:

```
CapabilityMissing
```

191. Example

Plan:

```
Protected Target
```

Result:

```
ProtectedTarget
```

192. Example

Plan:

```
generation = 10
```

Current:

```
generation = 11
```

Result:

```
StaleGeneration
```

193. Example

Plan:

```
expires_at = T
```

Current:

```
now > T
```

Result:

```
Expired
```

194. Example

Plan:

```
estimated bytes = 3 GiB
```

Budget:

```
2 GiB
```

Result:

```
BudgetExceeded
```

195. Example

Platform:

```
ZRAM unsupported
```

Request:

```
ExecuteZram
```

Result:

```
PlatformUnsupported
```

196. Final Authorization Pipeline

```
Request
   │
   ▼
Authenticate / Identify
   │
   ▼
Resolve Role
   │
   ▼
Resolve Capabilities
   │
   ▼
Load Snapshots
   │
   ▼
Validate Generations
   │
   ▼
Validate Plan Fingerprint
   │
   ▼
Validate Scope
   │
   ▼
Validate Protection
   │
   ▼
Validate Operation Types
   │
   ▼
Validate Budgets
   │
   ▼
Validate Expiration
   │
   ▼
Validate Runtime Readiness
   │
   ▼
Construct AuthorizedPlan
   │
   ▼
Persist Authorization
   │
   ▼
Dispatch Executor
```

197. Final Safety Still Required

```
AuthorizedPlan
      │
      ▼
Final Safety Gate
      │
      ▼
   Executor
```

198. No Shortcut

Forbidden:

```
Authorization
     X
     ▼
Executor
```

199. Correct

```
Authorization
     │
     ▼
AuthorizedPlan
     │
     ▼
Final Safety
     │
     ▼
Executor
```

200. Metrics

Metrics:

```
authorization_attempts_total
authorization_allowed_total
authorization_denied_total
authorization_deferred_total
authorization_stale_total
authorization_expired_total
capability_denied_total
scope_denied_total
generation_denied_total
protected_target_denied_total
budget_denied_total
```

201. Audit

Authorization audit should capture:

```
request origin
actor identity
role
capability decision
plan id
operation count
scope
generations
expiration
final decision
reason
```

202. Audit Integrity

Audit storage mengikuti:

```
Durable Storage Contract
```

dari 51.md.

203. Audit Failure

Jika security-critical authorization audit cannot be persisted:

```
behavior must be explicitly defined
```

Default untuk destructive operation:

```
fail closed
```

204. Testing

Unit tests:

```
role mapping
capability intersection
scope validation
protected target
budget
expiration
generation
fingerprint
operation type
```

205. Property Tests

Property:

```
removing capability cannot create new authorization
```

206. Property

Property:

```
narrowing scope cannot increase allowed operations
```

207. Property

Property:

```
Protected target never becomes authorized
```

208. Property

Property:

```
generation mismatch never becomes authorized
```

209. Property

Property:

```
expired authorization never becomes authorized
```

210. Property

Property:

```
platform unsupported capability never becomes authorized
```

211. Property

Property:

```
Executor cannot receive operation outside AuthorizedPlan
```

212. Fuzzing

Fuzz:

```
request scope
operation type
generation
capability set
role input
malformed persisted authorization
```

213. Security Test

Try:

```
forged role
forged capability
forged TargetId
altered plan
altered fingerprint
altered generation
expired plan
oversized scope
```

Semua harus:

```
reject
atau
safely restrict
```

214. Race Test

Between:

```
authorization
execution
```

simulate:

```
package change
user lock
mount change
config reload
capability loss
```

Expected:

```
final safety/readiness catches change
```

215. Crash Test

Crash after:

```
authorization persisted
before executor
```

Recovery:

```
revalidate
```

216. Crash Test

Crash after:

```
operation started
```

Recovery:

```
Unknown/Reconciliation
```

217. Definition of Done

58.md selesai jika:

```
[x] Authorization vs Policy vs Safety didefinisikan
[x] Request origin didefinisikan
[x] Identity didefinisikan
[x] Role mapping didefinisikan
[x] Capability model didefinisikan
[x] Capability intersection didefinisikan
[x] Platform capability restriction didefinisikan
[x] Config restriction didefinisikan
[x] Capability downgrade didefinisikan
[x] Generation binding didefinisikan
[x] Operation-level authorization didefinisikan
[x] Plan-level authorization didefinisikan
[x] Scope enforcement didefinisikan
[x] Protected target enforcement didefinisikan
[x] Budget authorization didefinisikan
[x] Plan expiration didefinisikan
[x] Runtime readiness didefinisikan
[x] Plan fingerprint didefinisikan
[x] AuthorizedPlan immutability didefinisikan
[x] IPC/CLI/Scheduler authorization didefinisikan
[x] Recovery authorization didefinisikan
[x] Fail-closed behavior didefinisikan
[x] Audit didefinisikan
[x] Metrics didefinisikan
[x] Property testing didefinisikan
[x] Fuzzing didefinisikan
[x] Race testing didefinisikan
[x] Crash testing didefinisikan
```

218. Arah Berikutnya

Sekarang boundary utama sudah membentuk rantai:

```
Target Discovery
      │
      ▼
Classification
      │
      ▼
Planning
      │
      ▼
Authorization
      │
      ▼
Final Safety
      │
      ▼
Executor
      │
      ▼
Verifier
      │
      ▼
Durable Result
```

Tahap berikutnya adalah memperjelas Executor secara implementatif, khususnya bagaimana satu AuthorizedPlan dieksekusi operation-by-operation tanpa:

```
recursive delete
scope expansion
FD leak
unsafe retry
budget bypass
cancellation race
unknown-outcome replay
```

Fokus 59.md:

```
59.md — Cleanup Executor Implementation Contract,
       AuthorizedPlan Consumption,
       Operation State Machine,
       Per-operation Final Validation,
       FD Acquisition,
       Parent Directory Resolution,
       Identity Revalidation,
       unlinkat Execution,
       Empty Directory Removal,
       Operation Dependencies,
       Cancellation,
       Timeout,
       Budget Enforcement,
       Unknown Outcome,
       Retry Rules,
       Crash Boundary,
       Durable Operation State,
       Executor Concurrency,
       Resource Locks,
       Error Mapping,
       Verifier Handoff,
       dan Secure Cleanup Execution Algorithm
```
