55.md — Android Cache Target Discovery, Package Cache Mapping, App Cache, Code Cache, Temporary Data, Device-Protected vs Credential-Protected Storage, User Scope, Package Identity, Cache Eligibility, Protected Paths, Storage Mapping, OEM/Android Version Differences, Discovery Fallback, Unknown Layout Handling, and Android Cache Catalog Contract

Tujuan

Dokumen ini mengunci bagaimana sistem menemukan target cache Android secara konkret.

Dokumen ini menjadi penghubung antara:

```
Android Platform
      │
      ▼
Package/User Discovery
      │
      ▼
Storage Mapping
      │
      ▼
Android Cache Adapter
      │
      ▼
Target Catalog
      │
      ▼
Scanner
      │
      ▼
Safety
      │
      ▼
Policy
```

Prinsip terpenting:

```
Android path knowledge
    TIDAK BOLEH
masuk ke Domain.
```

Domain hanya menerima:

```
TargetDescriptor
TargetId
PackageId
UserId
StorageClass
TrustedRoot
TargetIdentity
```

1. Android Cache Bukan Arbitrary Directory

Sistem tidak boleh menganggap:

```
"directory yang kelihatan seperti cache"
```

sebagai cache.

Target harus berasal dari:

```
Platform Adapter
      │
      ▼
Target Catalog
```

2. Target Catalog Adalah Authority

Scanner tidak boleh menebak target dari path.

Scanner hanya menerima:

```
TargetDescriptor
```

3. Path Discovery

Path konkret Android/Linux menjadi tanggung jawab:

```
Android Platform Adapter
```

4. Domain

Domain tidak boleh memiliki:

```
/data/
/cache/
/storage/
/data/user/
/data/user_de/
```

5. Package Identity

Package harus direpresentasikan sebagai:

```
PackageId
```

bukan:

```
raw String
```

6. User Identity

User harus direpresentasikan sebagai:

```
UserId
```

7. Package + User

Target aplikasi harus memiliki scope:

```
PackageId
    +
UserId
```

8. Mengapa

Package yang sama dapat memiliki data/cache berbeda untuk:

```
user 0
user 10
user 11
dst.
```

9. Package Discovery

Platform Adapter memperoleh:

```
installed package
package identifier
package state
user association
storage availability
```

10. Package Discovery Bukan Cleanup

Discovery hanya:

```
read
```

tidak:

```
delete
modify
repair
```

11. Package State

Package dapat berada dalam kondisi:

```
Installed
Uninstalled
Updating
Disabled
Suspended
Unknown
```

12. Unknown Package State

Default:

```
conservative
```

13. Updating Package

Jangan menghapus target berdasarkan asumsi bahwa package sudah stabil.

Jika identity belum konsisten:

```
TargetUnavailable
atau
Deferred
```

14. Uninstalled Package

Target cache orphaned dapat menjadi kandidat khusus hanya jika:

```
policy mengizinkan
platform mengonfirmasi ownership
safety boundary tetap valid
```

15. Default

Orphaned data bukan otomatis:

```
delete
```

16. Package Reinstall

Skenario:

```
package A
   │
   ▼
uninstall
   │
   ▼
reinstall
   │
   ▼
package A
```

String package tetap sama, tetapi storage identity dapat berubah.

17. Package Generation

Catalog dapat menyimpan:

```
package generation
```

atau identity snapshot yang setara.

18. Stale Detection

Jika package identity berubah:

```
CatalogGeneration mismatch
    ↓
Job Stale
```

19. App Cache

App cache adalah target yang secara semantik:

```
disposable
```

tetapi tetap membutuhkan:

```
package scope
user scope
trusted root
identity validation
```

20. Code Cache

Code cache merupakan kategori terpisah.

Jangan menyamakan:

```
AppCache
CodeCache
```

21. Why

Kebijakan dapat berbeda untuk:

```
application cache
executable/code cache
```

22. Temporary Data

Temporary data hanya menjadi target jika:

```
target type
ownership
storage scope
```

sudah diketahui.

23. Unknown Temporary Directory

Directory bernama:

```
tmp
temp
temporary
```

tidak otomatis menjadi:

```
cleanup target
```

24. Explicit Target Class

Gunakan:

```
TargetClass::AppCache
TargetClass::CodeCache
TargetClass::Temporary
```

25. Future Target

Jika nanti ada:

```
DownloadCache
CompilerCache
SystemCache
```

harus menjadi:

```
typed TargetClass
```

26. Generic Target

Hindari:

```
TargetClass::OtherDeleteable
```

27. Credential-Protected Storage

Credential-protected storage memiliki lifecycle yang terkait:

```
user unlock
```

28. Device-Protected Storage

Device-protected storage memiliki lifecycle berbeda.

Sistem tidak boleh memperlakukan keduanya identik.

29. Locked User

Jika storage credential-protected:

```
locked
```

maka:

```
target unavailable
```

30. No Unlock Bypass

Cleanup daemon tidak boleh mencoba:

```
memaksa membuka CE storage
```

31. Locked Storage Result

Gunakan:

```
Deferred
TargetUnavailable
```

sesuai konteks.

32. Destructive Operation

Tidak boleh dijalankan pada target yang:

```
identity
availability
protection state
```

tidak jelas.

33. User Scope

Default target discovery:

```
explicit users
```

34. Users = All

"All users" berarti:

```
enumerate known users
```

bukan:

```
arbitrary numeric IDs
```

35. User Enumeration

User list berasal dari:

```
Platform Adapter
```

36. Unknown User

Unknown user:

```
tidak menjadi target
```

37. Removed User

Removed user harus menyebabkan:

```
catalog invalidation
```

38. User Changed Event

Event:

```
UserChanged
```

mengakibatkan:

```
catalog refresh
```

39. Storage Mapping

Platform Adapter memetakan:

```
Package
   │
   ├── User
   │
   ├── CE storage
   │
   └── DE storage
```

40. Target Root

Target root harus dibangun dari:

```
platform-discovered trusted information
```

bukan:

```
concatenated arbitrary string
```

41. Root Construction

Konseptual:

```
PackageId
   +
UserId
   +
TargetClass
   │
   ▼
Platform Adapter
   │
   ▼
TrustedRoot
```

42. Domain View

Domain hanya melihat:

```
TargetDescriptor
```

43. TargetDescriptor

Minimal:

```
TargetId
TargetClass
PackageId?
UserId?
StorageClass
TrustedRoot
availability
protection
identity
```

44. TargetId

TargetId harus stabil dalam:

```
CatalogSnapshot
```

45. TargetId Tidak Harus Global Permanent

TargetId dapat bergantung pada:

```
catalog generation
```

46. Catalog Generation

Jika target mapping berubah:

```
new CatalogGeneration
```

47. Old Catalog

Old catalog tetap immutable.

Active jobs referencing old catalog:

```
detect stale
```

48. Target Availability

Target dapat:

```
Available
Locked
Missing
ReadOnly
PermissionDenied
Unsupported
Unknown
```

49. Unknown

Unknown tidak berarti:

```
Available
```

50. Read-only

Read-only target:

```
scan mungkin boleh
```

tetapi:

```
destructive cleanup tidak boleh
```

51. PermissionDenied

Tidak boleh diperbaiki dengan:

```
chmod
chown
policy bypass
```

52. SELinux

SELinux denial adalah:

```
platform/security constraint
```

bukan:

```
temporary path problem
```

53. Protected Targets

Catalog harus dapat menandai:

```
Protected
```

54. Protected Target

Protected target:

```
never eligible
```

55. Protected Examples

Kategori protected dapat mencakup:

```
system-critical data
package manager state
security state
platform metadata
boot-critical data
```

Daftar konkret harus berasal dari:

```
platform contract
```

56. Protected Path

Jangan hanya menggunakan:

```
string prefix
```

sebagai proof.

57. Protected Target Identity

Protection harus melekat pada:

```
typed TargetDescriptor
```

58. Exclusion

Target exclusion dapat berasal dari:

```
Platform
Config
Policy
```

59. Platform Protection

Jika Platform mengatakan:

```
Protected
```

Policy tidak boleh mengubahnya menjadi:

```
Eligible
```

60. Config Exclusion

Config dapat mempersempit scope:

```
Target A
   ↓
excluded
```

61. Config Tidak Dapat Membuka Protection

Forbidden:

```
protected = true
   +
config.force = true
   ↓
eligible
```

62. Policy

Policy hanya memutuskan berdasarkan:

```
validated TargetDescriptor
Candidate
EffectivePolicy
```

63. Scanner

Scanner hanya scan:

```
Available
Authorized TargetDescriptor
```

64. Scanner Tidak Menemukan Target Baru

Jika scanner menemukan directory lain:

```
bukan berarti target baru
```

65. Directory Discovery

Scanner menemukan:

```
candidates
```

bukan:

```
new targets
```

66. Target vs Candidate

Target:

```
"area yang authorized untuk diperiksa"
```

Candidate:

```
"entry di dalam area tersebut"
```

67. Contoh

```
Target: AppCache
   │
   └── Candidate: foo.tmp
```

68. Candidate Tidak Boleh Keluar Target

Candidate path:

```
RelativePath
```

selalu relatif terhadap:

```
TrustedRoot
```

69. Storage Boundary

Jika traversal mencapai:

```
different filesystem
different mount
forbidden storage
```

scanner harus:

```
stop/reject according to policy
```

70. External Storage

External/shared storage bukan otomatis:

```
AppCache
```

71. Shared Storage

Default:

```
excluded
```

72. External Cache

Jika suatu platform menyediakan cache target yang jelas:

```
typed TargetClass
```

73. Unknown External Layout

Jika ownership atau boundary tidak dapat dibuktikan:

```
Unsupported
```

74. OEM Variation

OEM dapat mengubah:

```
path layout
mount arrangement
service implementation
```

75. Domain Tidak Peduli

Domain menerima:

```
same semantic TargetDescriptor
```

76. Adapter Responsibility

Adapter menangani:

```
Android version
OEM variation
platform service differences
mount differences
```

77. Normalization

Semua hasil platform harus dinormalisasi sebelum masuk:

```
CatalogSnapshot
```

78. Normalized Storage

Storage class:

```
CredentialProtected
DeviceProtected
Shared
External
Unknown
```

79. Unknown Storage

Unknown storage:

```
destructive cleanup disabled
```

80. Android Version

Android API level hanya:

```
contextual information
```

81. Capability

Actual capability harus berasal dari:

```
runtime observation
```

82. Example

Jangan:

```
API >= X
   ↓
feature guaranteed
```

Gunakan:

```
detect capability
   ↓
validate
   ↓
advertise capability
```

83. Package Path Knowledge

Jika Android version memiliki layout berbeda:

```
adapter chooses correct mapping
```

84. No Layout Guessing

Forbidden:

```
if path exists:
    assume cache
```

85. Existence

Existence adalah:

```
evidence
```

bukan:

```
semantic authorization
```

86. Ownership

Target harus memiliki:

```
package ownership
user ownership
target class
```

87. Ownership Mismatch

Jika:

```
expected package A
```

tetapi:

```
actual ownership B
```

result:

```
reject/stale
```

88. Ownership Unknown

Default:

```
no destructive operation
```

89. Package Manager

Jika package metadata diperoleh melalui platform service:

```
service adapter
```

90. Filesystem Cross-check

Metadata package service dan filesystem identity dapat dibandingkan bila platform menyediakan mekanismenya.

91. Cross-check Failure

Mismatch:

```
TargetUnavailable
Stale
atau
SecurityMismatch
```

92. Package State Snapshot

Catalog dapat menyimpan:

```
package generation
version
install state
user association
```

93. Version Change

Version change tidak selalu berarti:

```
target invalid
```

tetapi dapat memicu:

```
catalog refresh
```

94. Package Update

Active cleanup:

```
jangan blind resume
```

95. Catalog Refresh

Flow:

```
PackageChanged
    │
    ▼
invalidate Catalog
    │
    ▼
rebuild Catalog
    │
    ▼
new CatalogGeneration
```

96. Atomic Catalog Replace

```
OldCatalog
     │
     ├── active jobs
     │
     ▼
NewCatalog
```

New catalog dipublish secara atomic.

97. Failed Refresh

Jika refresh gagal:

```
old catalog tetap immutable
```

98. Destructive Job

Job yang bergantung pada data lama:

```
Stale
```

atau:

```
Deferred
```

99. Catalog Partial State

Jika sebagian package discovery gagal:

```
CatalogHealth::Degraded
```

100. Degraded Catalog

Scanner boleh memiliki behavior berbeda untuk:

```
read-only inspection
```

tetapi cleanup harus:

```
conservative
```

101. Partial Discovery

Misalnya:

```
User 0    → discovered
User 10   → discovered
User 11   → unknown
```

Jangan menganggap:

```
User 11 has no packages.
```

102. Unknown vs Empty

Ini penting:

```
Empty
   ≠
Unknown
```

103. Empty

Discovery berhasil dan hasil:

```
zero
```

104. Unknown

Discovery gagal atau tidak lengkap.

105. Cleanup Rule

Unknown:

```
never interpreted as safe-to-delete
```

106. Package Enumeration Budget

Discovery harus memiliki:

```
max_packages
max_users
max_duration
```

107. Discovery Failure

Failure pada satu package tidak harus menghentikan seluruh catalog jika:

```
partial catalog aman
```

108. Security Failure

Jika failure menyentuh:

```
trust boundary
```

catalog dapat:

```
fail closed
```

109. Catalog Health

Minimal:

```
Healthy
Degraded
Unavailable
```

110. Health + Cleanup

```
Healthy
   ↓
normal operation

Degraded
   ↓
restricted operation

Unavailable
   ↓
no destructive cleanup
```

111. Catalog Refresh Triggers

Refresh dapat dipicu:

```
startup
package change
user change
storage change
mount change
permission change
platform event
periodic reconciliation
```

112. Refresh Coalescing

Jika terjadi:

```
PackageChanged
PackageChanged
MountChanged
StorageChanged
```

boleh digabung menjadi:

```
CatalogInvalidated
```

113. Broad Invalidation

Jika impact tidak diketahui:

```
invalidate broader scope
```

114. Narrow Invalidation

Jika event memiliki identity:

```
PackageId
UserId
```

refresh dapat dibatasi.

115. Event Tidak Menjadi Authority

Event hanya:

```
invalidation hint
```

116. Authority

Authority tetap:

```
fresh platform discovery
```

117. Startup

Urutan:

```
Process Start
    │
    ▼
Platform Capability Discovery
    │
    ▼
User Discovery
    │
    ▼
Package Discovery
    │
    ▼
Storage/ Mount Discovery
    │
    ▼
Target Mapping
    │
    ▼
Catalog Validation
    │
    ▼
CatalogSnapshot
    │
    ▼
Scheduler Ready
```

118. Catalog Before Scanner

Scanner tidak boleh berjalan tanpa:

```
valid CatalogSnapshot
```

119. Catalog Before Planner

Planner menggunakan:

```
target metadata
```

120. Catalog Before Authorization

Authorization memverifikasi:

```
catalog generation
```

121. Catalog Before Executor

Executor final gate memeriksa:

```
target root
identity
availability
mount
filesystem
```

122. End-to-end

```
User Request
     │
     ▼
ApplicationController
     │
     ▼
CatalogSnapshot
     │
     ▼
TargetDescriptor
     │
     ▼
Scanner
     │
     ▼
Candidate
     │
     ▼
Safety
     │
     ▼
Policy
     │
     ▼
Planner
     │
     ▼
Authorization
     │
     ▼
Executor
     │
     ▼
Verifier
```

123. Target Construction Invariant

TargetDescriptor hanya boleh dibuat melalui:

```
validated catalog builder
```

124. Private Constructor

Jika menggunakan Rust:

```
TargetDescriptor::new(...)
```

sebaiknya:

```
private
```

125. Builder

Konseptual:

```
PlatformSnapshot
     │
     ▼
TargetCatalogBuilder
     │
     ├── validate package
     ├── validate user
     ├── validate storage
     ├── validate root
     ├── validate ownership
     ├── validate protection
     └── capture identity
     │
     ▼
TargetDescriptor
```

126. Invalid Target

Tidak boleh masuk catalog.

127. Target Snapshot

TargetDescriptor harus immutable.

128. Snapshot Consistency

Semua target dalam satu catalog memiliki:

```
CatalogGeneration
```

129. Target Generation

Target juga dapat membawa:

```
target generation
```

untuk mendeteksi perubahan lebih sempit.

130. Protected Target Precedence

Protection order:

```
Platform Protection
    │
    ▼
Safety Boundary
    │
    ▼
Config Exclusion
    │
    ▼
Policy Eligibility
    │
    ▼
Authorization
```

Tidak ada layer bawah yang dapat membatalkan layer atas.

131. Cache Eligibility

"Cache" secara semantic tidak cukup.

Eligibility:

```
Target recognized
   +
Target available
   +
Ownership valid
   +
Storage state valid
   +
Not protected
   +
Within configured scope
   +
Policy allows
   +
Authorization allows
   +
Final safety passes
```

132. Unknown Eligibility

Jika satu kondisi critical:

```
Unknown
```

maka:

```
not eligible
```

133. Safety vs Eligibility

Target dapat:

```
safe filesystem boundary
```

tetapi:

```
policy excluded
```

134. Policy vs Platform

Platform protected:

```
policy cannot allow
```

135. Authorization

Authorization dapat:

```
further restrict
```

tetapi tidak:

```
expand platform scope
```

136. Catalog Persistence

Catalog boleh disimpan sebagai:

```
rebuildable cache
```

137. Catalog Persistence Is Not Authority

Startup harus dapat:

```
rebuild catalog
```

138. Stale Persisted Catalog

Persisted catalog tidak boleh dipakai untuk destructive cleanup jika:

```
generation unknown
platform state changed
storage changed
mount changed
```

139. Crash Recovery

Setelah crash:

```
rebuild or validate catalog
```

sebelum:

```
destructive execution
```

140. Permission

Catalog harus merekam jika target:

```
inaccessible
```

141. Inaccessible Target

Jangan menyimpulkan:

```
empty
```

142. Read-only Discovery

Catalog builder tidak melakukan:

```
unlink
rename
chmod
chown
```

143. Side-effect Free

Discovery sebaiknya:

```
no filesystem mutation
```

144. Platform Service Failure

Jika package service gagal:

```
CatalogUnavailable
atau
CatalogDegraded
```

145. Shell

Forbidden:

```
shell command
```

untuk menemukan package/cache.

146. Generic Command

Forbidden:

```
execute("pm ...")
execute("find ...")
execute("rm ...")
```

147. Typed Adapter

Gunakan:

```
PackageProvider
UserProvider
StorageProvider
MountProvider
```

148. Provider Separation

Contoh:

```
PackageProvider
      │
      ▼
PackageInfo

StorageProvider
      │
      ▼
StorageInfo

MountProvider
      │
      ▼
MountInfo
```

149. Catalog Builder

Builder menggabungkan semuanya:

```
PackageInfo
UserInfo
StorageInfo
MountInfo
PlatformCapabilities
```

150. No Provider Mutation

Provider hanya:

```
observe
```

151. Testability

Setiap provider harus memiliki:

```
fake implementation
```

152. Fake Package Provider

Test:

```
installed
uninstalled
updating
duplicate
unknown
```

153. Fake Storage Provider

Test:

```
CE locked
CE unlocked
DE available
read-only
missing
mount changed
```

154. Fake Mount Provider

Test:

```
same fs
different fs
bind mount
mount disappearance
```

155. Property Test

Invariant:

```
Unknown
   ↓
never Eligible
```

156. Property Test

Invariant:

```
Protected
   ↓
never Eligible
```

157. Property Test

Invariant

```
CatalogGeneration mismatch
   ↓
destructive execution forbidden
```

158. Property Test

Invariant:

```
Package/User mismatch
   ↓
target rejected
```

159. Fuzzing

Fuzz:

```
package identifiers
user identifiers
platform metadata
mount records
storage records
path mapping input
```

160. Path Mapping Fuzz

Arbitrary input tidak boleh menghasilkan:

```
path escape
```

161. Mapping Output

Only:

```
validated TrustedRoot
```

162. Security Property

No platform input can create:

```
arbitrary deletion target
```

163. Example Unsafe Design

```
package_name = user_input
     │
     ▼
"/data/user/" + package_name
     │
     ▼
delete
```

Forbidden.

164. Correct Design

```
PackageId
```

```
   │
   ▼
PackageProvider
   │
   ▼
validated PackageInfo
   │
   ▼
TargetCatalogBuilder
   │
   ▼
TargetDescriptor
   │
   ▼
Safety
   │
   ▼
Authorization
   │
   ▼
Executor
```

165. OEM Compatibility

OEM-specific logic harus berada di:

```
platform/android
```

bukan:

```
domain
```

166. Android Version Compatibility

Version-specific implementation:

```
adapter layer
```

167. Capability Matrix

PlatformSnapshot dapat memuat:

```
PackageDiscovery
UserDiscovery
StorageDiscovery
MountDiscovery
CacheMapping
SecurityObservation
```

masing-masing:

```
Supported
Unsupported
Unknown
Restricted
```

168. Unsupported

Tidak boleh fallback ke:

```
unsafe generic path discovery
```

169. Unknown

Tidak boleh berubah menjadi:

```
supported
```

170. Restricted

Restricted berarti:

```
platform tersedia
tetapi daemon tidak memiliki authority/access
```

171. Catalog Decision

```
Supported + Valid
      ↓
   Target

Unsupported
      ↓
   Skip

Unknown
      ↓
   Conservative

Restricted
      ↓
   Unavailable
```

172. Observability

Metrics:

```
catalog_refresh_total
catalog_refresh_failure_total
catalog_degraded_total
packages_discovered_total
users_discovered_total
targets_discovered_total
protected_targets_total
unavailable_targets_total
locked_targets_total
stale_catalog_total
```

173. Audit

Catalog event dapat mencatat:

```
generation
trigger
health
target count
failure reason
```

174. Privacy

Do not expose unnecessary:

```
package metadata
filesystem details
user details
```

175. Explain

User-facing explanation dapat:

```
"target unavailable because user storage is locked"
```

tanpa:

```
leaking sensitive internal path information
```

176. Catalog API

Konseptual:

```
trait TargetCatalog {
    fn snapshot() -> CatalogSnapshot;
    fn refresh(reason) -> CatalogRefreshResult;
}
```

177. Snapshot API

Snapshot:

```
immutable
generation-bound
deterministic
```

178. Refresh API

Refresh:

```
atomic publish
```

179. Refresh Failure

Old snapshot:

```
remains valid for readers
```

tetapi destructive jobs harus memeriksa:

```
current generation
```

180. Catalog Lifecycle

```
Created
   │
   ▼
Discovering
   │
   ▼
Validating
   │
   ├───────────────┐
   ▼               ▼
Healthy          Degraded
   │               │
   ▼               ▼
Published       Restricted
   │
   ▼
Invalidated
   │
   ▼
Rebuilding
```

181. Invalidated Catalog

Invalidated catalog tidak boleh dipakai untuk:

```
new destructive job
```

182. Existing Job

Existing job:

```
may complete read-only phases
```

tetapi:

```
final authorization/safety must revalidate
```

183. Catalog and Job

```
Job
 │
 ├── ConfigGeneration
 ├── CatalogGeneration
 ├── PlatformGeneration
 └── Target identities
```

184. Generation Consistency

Semua harus compatible saat:

```
authorization
execution
```

185. Generation Mismatch

Result:

```
Stale
```

186. Definition of Done

55.md selesai jika:

```
[x] Android cache target discovery didefinisikan
[x] Package identity didefinisikan
[x] User identity didefinisikan
[x] Package/User scope didefinisikan
[x] AppCache didefinisikan
[x] CodeCache didefinisikan
[x] Temporary target didefinisikan
[x] CE/DE distinction didefinisikan
[x] Locked storage behavior didefinisikan
[x] Protected target didefinisikan
[x] Shared/external storage policy didefinisikan
[x] OEM/version abstraction didefinisikan
[x] Package/storage/mount providers didefinisikan
[x] Catalog generation didefinisikan
[x] Catalog health didefinisikan
[x] Partial discovery didefinisikan
[x] Unknown vs Empty didefinisikan
[x] Catalog refresh didefinisikan
[x] Event invalidation didefinisikan
[x] Atomic catalog publication didefinisikan
[x] Crash recovery didefinisikan
[x] Persistence bukan authority didefinisikan
[x] Security boundaries didefinisikan
[x] Fuzz/property testing didefinisikan
[x] Observability didefinisikan
```

187. Arah Berikutnya

Setelah:

```
54.md
Filesystem Primitive
```

dan:

```
55.md
Android Target Discovery
```

lapisan berikutnya adalah aturan tentang apa yang benar-benar boleh disebut:

```
"cache"
```

dan bagaimana candidate dari AppCache/CodeCache/Temporary diproses tanpa menghapus data yang secara semantik bukan cache.

Fokus 56.md:

```
56.md — Cache Classification and Eligibility Contract,
       Cache Type Semantics,
       File/Directory Eligibility,
       Age Rules,
       Size Rules,
       Protected Files,
       Active/Recently Used Data,
       Package State,
       Cache Ownership,
       Temporary File Semantics,
       Code Cache Semantics,
       Directory Deletion Eligibility,
       Empty Directory Rules,
       Unknown Metadata,
       Timestamp Reliability,
       Candidate Classification,
       Exclusion Reasons,
       dan Formal Cache Eligibility Model
```

Alur:

```
TargetDescriptor
      │
      ▼
   Scanner
      │
      ▼
   Candidate
      │
      ▼
Classification
      │
      ├── Cache
      ├── NotCache
      ├── Protected
      ├── Unknown
      └── Deferred
      │
      ▼
   Policy
      │
      ▼
PolicyApprovedCandidate
      │
      ▼
   Planner
```
